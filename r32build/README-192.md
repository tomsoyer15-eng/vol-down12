# ADD-FUT v1.6.0-draft, ред. 32 — кодовая редакция

Ред. 32 добавляет к документарной ред. 31 два механизма в коде и переиздаёт нормативный документ
(`ADD-FUT-v1_6_0-r32.pdf`): §8а — трёхфазность, §8б — два условия открытия перехода, §9б — пункт (1)
закрыт, §6 и §10 — дополнены.

**1. Акт заказчика на переход** (решение 09.08.2026). `SWITCH_SIGNAL` — рекомендация. Открытие
перехода требует журнального события `OWNER_APPROVE,<цель>|<sid>`, привязанного к той же цели и
тому же sid. Команда: `python3 mr_engine.py --approve-switch --asof ДАТА` (идемпотентна; при книге
в состоянии MIXED отклоняется). Без одобрения исполнитель отказывает и не подаёт ни одной заявки.
Аварийный REDUCE (сокращение до L=1) подписи НЕ требует.

**2. Маржа и число заявок по отображённой книге** (закрывает открытое замечание §3 ред. 30).
До открытия перехода считается фактическая книга маршрута-цели: внутренняя MES-сетка
раскладывается в целые ES плюс остаток MES; фьючерсам присваивается требование на контракт
(§7 v1.5.3.1), ETF — поддерживающее требование 25% от стоимости позиции. Проверяется запас
О-3 (≥2,00×) для маршрута Ф и О-3-Е (≥1,40×) для маршрута Е, а также число заявок против лимита
Priority Customer (390/день). Нарушение — отказ до первой заявки; сводка пишется в состояние перехода.

Контрольная сверка: книга 26 ES + 101 ZN на 10 млн даёт маржу $1 122 960 = 11,23% капитала и
запас 8,91× — совпадает с §7. Переход Ф→Е на 10 млн: запас 2,01×, 9 лотов, 18 заявок штатно
(27 с компенсациями) — 7% дневного лимита.

Маршрут Ф, кап плеча, сигнальное правило, конвенция, мандат риска и контрольные цифры обоих
маршрутов НЕ изменялись.

## Проверка
1. paket-ADD-FUT-v1_5_3_1.zip: `pip install -r requirements.txt && python run_all_v13.py` — 10 PASS.
2. Все файлы настоящего архива в корень; `sha256sum -c MANIFEST-192.txt`; `python3 selfcheck_v192.py`.
3. Живой запуск движка: `python3 mr_engine.py --asof ДАТА --nlv NLV [--emerg файл] [--dry-run]`.

## Состав (15 манифестируемых)
ADD-FUT-v1_6_0-r31.pdf · sim_v164.py · sim_etf.py · mr_engine.py (МР v7.7) · transition.py (исполнитель v7.7) ·
mr_inputs_retro.csv · mr_tariff.csv · mr_state.csv · mr_journal.csv · selfcheck_v192.py ·
nav_v164_modern.csv · nav_v164_splice.csv · nav_etf_modern.csv · instruments.csv · README-192.md

## Развёртывание (единожды)
1. `python3 mr_engine.py --provision-journal /абсолютный/путь/mr_journal.csv` — единственная конфигурация
   в фиксированном каталоге установки `~/.add-fut`: путь журнала, (st_dev, st_ino), journal_uuid, эпоха.
2. Одновременно удерживаются ДВА замка: фиксированный файл `lock.ADD-FUT-v1_6.IBKR-PRIMARY` в каталоге
   установки и дескриптор самого журнала (открыт с O_NOFOLLOW, личность сверяется). Подмена одного из
   объектов не даёт второго владельца. Файловый слой реентерабелен внутри потока.
3. Смена журнала (`--force-reprovision`) сериализуется по СТАРОМУ журналу: пока исполнитель работает,
   провизия ждёт; после смены эпоха растёт и старые процессы инвалидируются на первой же операции.
4. Перед каждым чтением и записью сверяется, что удерживаемый дескриптор всё ещё соответствует пути
   (dev+ino): запись в отсоединённый inode невозможна. Исполнитель перепроверяет эпоху перед каждым лотом.
5. Все записи идут с fsync файла и каталога. Механизм блокировки — BSD flock(2), advisory; поддерживается
   только локальная файловая система (NFS/CIFS не поддерживаются).
6. Самопроверка работает в изолированном каталоге прогона, CLI-тесты — в изолированном HOME; боевая
   конфигурация не изменяется, параллельные запуски независимы.

## Граница доверия (явно)
Блокировки и сверки личности рассчитаны на конкурентные экземпляры СОБСТВЕННОГО ПО: параллельный запуск
движка и исполнителя, аварийный перезапуск, гонки при смене журнала. Они НЕ рассчитаны на враждебный
локальный процесс, имеющий право записи в каталог развёртывания и в журнал: такой процесс может синхронно
подменять объекты блокировки, а в пределе — переписать журнал и сам код. Для этого класса угроз требуется
внешняя мера: запуск под супервизором с единственным писателем и файловые права, закрывающие каталог
развёртывания и журнал от прочих процессов пользователя.

## Что изменено в ред. 32 (документарная редакция)
Код, самопроверка и контрольные цифры маршрутов Ф/Е НЕ изменялись против ред. 30; пересчитан манифест.
Внесены принятые замечания владельца:
- §8а: уточнён смысл порога 1,24 п.п. — это порог по спреду ноги акций при нулевом спреде ZN, мёртвая
  зона 0,25 п.п.; при s_eq = 0,81 сигнал появится при s_zn ≳ 0,50.
- §8в: обязательный перечень к роллу 26.08 — снять ТРИ величины (спред ES, спред ZN, базис SOFR3M→EFFR)
  и заполнить fix_date полной датой ISO; значение вида «2026-07» датой не является и даст INVALID.
  В mr_inputs_retro.csv полудата убрана, строка Q2-2026 остаётся в статусе PENDING_26.08_P2.
- §8б: добавлены стрессовая цифра маршрута Е (30% → 1,24×, срабатывает О-3-Е; 35% → 1,06×, грань колла)
  и маржинальный след Е ≈ 50% капитала против 11,2% на Ф.
- О-3-Е: отмечено, что при штатных допущениях критерий сработать не может (минимум по окну 1,956 против
  порога 1,40); оживает только при house-требовании выше 34,9%.
- Дивидендный drag: норматив 0,20%/год признан заниженным (по ряду Шиллера ≈0,25%/год, издержки Е хуже
  примерно на 0,05 п.п.); пересчёт констант — отдельным решением заказчика.
- Пилот: прямо сказано, что пилотный масштаб экономику маршрута Е НЕ подтверждает.
- §5: мандат объявлен связывающим — запас 2,1 п.п., предельное плечо 2,15×, текущие 2,00× — потолок.
- §9б: открытые предпусковые пункты, включая незакрытый расчёт маржи и числа заявок по отображённой книге.
- §9в: что проверено с цифрами и что замечанием НЕ является (порог 1,24, буфер 5%, двухфазность МР).
