# CLAUDE.md — проект vol-down12 (ADD-FUT v1.6.0 ред. 33)

## При старте новой сессии

1. Прочитать **[PROJECT-STATE.md](PROJECT-STATE.md)** — он самодостаточен.
2. Кратко изложить пользователю текущее состояние проекта и следующий шаг.
3. **Дождаться подтверждения пользователя** и только после него начинать работу —
   не запускать сессий, рецензий, выпусков и правок до явного «да».

Текущее состояние, статус счёта, открытые вопросы и ближайшие шаги — в
**[PROJECT-STATE.md](PROJECT-STATE.md)**.

## Как выполнять задания

- **Выполнять все пункты задания подряд, до конца**, не останавливаясь после каждого.
- **Не запрашивать подтверждение между пунктами.**
- **Не докладывать промежуточные результаты** — только итог по всем пунктам сразу.
- **Если пункт невыполним — записать это и перейти к следующему**, не прерывая работу.

Это правило о ПОРЯДКЕ работы, а не об осторожности: оно не отменяет ни правило 11
(защита от удаления), ни требование подтверждать необратимые действия вне списка
заданий, ни правило «дождаться подтверждения» при старте новой сессии.

## ТЗ ЗАВЕРШЕНИЯ (решение заказчика 28.08.2026) — ГЛАВЕНСТВУЕТ НАД ПРОТИВОРЕЧАЩИМИ ПРАВИЛАМИ

Записано дословно по указанию заказчика. При противоречии с правилами ниже действует ТЗ;
в самих правилах противоречащие места помечены. Цель: НЕ улучшать архитектуру ADD-FUT и НЕ
стремиться устранить все потенциально возможные замечания, а минимальным количеством
изменений довести систему до надёжной эксплуатации и ЗАВЕРШИТЬ разработку.

ГЛАВНЫЙ ПРИНЦИП: если существующий код можно безопасно оставить без изменения — оставить
без изменения. Новая строка кода сама является источником риска, поэтому добавление кода
требует более сильного обоснования, чем сохранение уже работающего решения.

### 1. Замороженная область

Считать замороженными: торговую логику стратегии; параметры; расчётные движки; правила
SMA-12; правила включения/выключения ног; торговую арифметику; полосу ребалансировки; кап
плеча; правила роллирования (если не обнаружена воспроизводимая ошибка); уже закрытые и
проверенные слои. Не проводить их повторный аудит без НОВОГО конкретного свидетельства
ошибки. Не заниматься рефакторингом замороженной области.

### 2. Что разрешено исправлять

Изменение production-кода допускается только при хотя бы одном из условий:
- A. Есть воспроизводимый сценарий неправильной позиции или заявки.
- B. Возможна потеря контроля над фактической позицией брокера.
- C. Система может ошибочно продолжить торговлю после состояния, которое должно её
  остановить.
- D. Система может необоснованно остановить торговлю или не восстановиться после штатно
  предусмотренного сбоя.
- E. Есть воспроизводимая проблема с данными, сигналом, роллом, маржой или IBKR.
- F. Ошибка препятствует сборке, запуску, выпуску или восстановлению системы в
  предусмотренной спецификацией ситуации.

Если ни одного из A–F нет — production-код НЕ менять.

### 3. Что НЕ является основанием для изменения

Красота кода; дублирование; несовершенство архитектуры; возможность дополнительной
защиты; теоретический сценарий без доказанной достижимости; предложение рецензента
улучшить; возможность увеличить покрытие; устаревший комментарий без операционного риска;
возможность вынести функцию; возможность сократить или переписать; более элегантная
реализация. Такие находки записываются в BACKLOG и не трогаются до окончания пилота.

### 4. Внешняя рецензия (заменяет прежние правила 8-до-нуля и 8г)

Цель «гонять внешние рецензии до нуля замечаний» ОТМЕНЯЕТСЯ. Рецензия используется только
для поиска дефектов классов A–F. Каждая находка получает статус:
- BLOCKER — препятствует эксплуатации;
- MATERIAL — способна изменить позицию, деньги или устойчивость контура;
- BACKLOG — всё остальное.
Исправляются только BLOCKER и MATERIAL. Число замечаний BACKLOG не влияет на выпуск.
Повторно проверять уже закрытый участок без новой причины запрещено.

### 5. Сначала воспроизведение, потом исправление

Перед изменением production-кода: (1) описать конкретный дефект; (2) показать путь его
достижения; (3) воспроизвести тестом/стендом либо другим объективным способом;
(4) объяснить практическое последствие; (5) только после этого менять код.
Если воспроизвести невозможно — изменение запрещено. Исключение: очевидная
детерминированная ошибка, непосредственно препятствующая запуску/выпуску и доказуемая без
изменения production-кода.

### 6. Минимальная правка

Выбирать минимальное изменение, устраняющее именно этот дефект. Приоритет: (1) удалить
ненужный механизм; (2) исправить существующую логику; (3) лишь в последнюю очередь
добавлять новый механизм. Не использовать исправление одного дефекта как повод для
соседнего рефакторинга.

### 7. Текущий открытый список

Разобрать открытые проблемы и классифицировать по BLOCKER / MATERIAL / BACKLOG. В первую
очередь: tz.py и параллельный импорт; backup_push.sh и ложный код успеха; session.py и
широкое подавление ошибок; самодостаточность release/selfcheck. Остальное — в BACKLOG.
Не открывать новые широкие аудиты всего проекта до закрытия этого списка.

### 8. Критерий технической готовности

Версия готова, когда одновременно: (1) замороженное ядро проходит нормативную регрессию;
(2) основная батарея зелёная; (3) проверены основные денежные пути — сигнал, открытие,
закрытие, ребалансировка, ролл, кап, восстановление после отказа/неопределённого исхода
заявки; (4) книга сверяется с фактическим состоянием IBKR; (5) release/selfcheck проходит
в предусмотренной нормативом среде; (6) нет известных BLOCKER; (7) нет известных MATERIAL
без принятого явного решения; (8) остальное в BACKLOG.
После выполнения этих условий разработка версии ЗАВЕРШАЕТСЯ. Новые улучшения — в
следующую версию.

### 9. Paper pilot

После технической готовности — преимущественно наблюдение, а не разработка. Цель пилота —
проверить реальное поведение написанной системы. Во время пилота production-код меняется
только при BLOCKER или MATERIAL. Наблюдать особенно: соответствие целевой и фактической
книги; месячное переключение сигнала; реальные исполнения IBKR; ролл; восстановление
после сбоя; корректность журналирования. Любой инцидент сначала фиксируется и
воспроизводится, затем принимается решение.

### 10. Запрет на бесконечную разработку

Не ставить целью доказать отсутствие любых ошибок — критерий недостижим для ПО такого
размера. Цель: достаточно простой, проверенный и наблюдаемый контур, правильно
реализующий замороженную стратегию и безопасно прекращающий работу при действительно
опасной неопределённости. Не увеличивать сложность ради доказательства надёжности, если
добавленная сложность сама создаёт сопоставимый или больший риск.

### 11. Формат каждой предлагаемой правки

ПРОБЛЕМА (что конкретно неправильно) / ВОСПРОИЗВЕДЕНИЕ (как доказано) / ПОСЛЕДСТВИЕ (что
будет без исправления) / КЛАСС (BLOCKER, MATERIAL, BACKLOG) / ИЗМЕНЕНИЕ (минимальная
правка) / НОВЫЕ И УДАЛЁННЫЕ СТРОКИ (сколько) / РИСК ПРАВКИ (какой новый риск создаёт).
Если класс BACKLOG — код не менять.

### 12. Главная задача

Не сделать ADD-FUT ещё совершеннее. ЗАКОНЧИТЬ ADD-FUT.

## РЕЖИМ PAPER PILOT / НАБЛЮДЕНИЕ (решение заказчика 28.08.2026) — ДЕЙСТВУЮЩИЙ РЕЖИМ

Записано дословно по указанию заказчика после завершения разработки v1.6.0-r33 (выпуск
33fd32e6). Дополняет «ТЗ ЗАВЕРШЕНИЯ»; при противоречии со старыми правилами действует
этот раздел. Разработка версии ЗАВЕРШЕНА; базовая версия пилота — выпуск 33fd32e6.

### 1. Production-код заморожен
Не продолжать разработку, рефакторинг, расширение тестовой инфраструктуры, аудит
архитектуры или улучшение существующего кода по собственной инициативе. Не пытаться
довести систему до состояния «невозможно найти никаких замечаний» — такого критерия
завершения больше нет.

### 2. Не трогать BACKLOG
Все замечания класса BACKLOG (PROJECT-STATE §5г) остаются как есть до окончания пилота.
Дублирование, некрасивая архитектура, недостаточное покрытие, старый код, лучшая
реализация, теоретический сценарий, лишняя ветка, возможность дополнительной защиты — НЕ
основание менять production-код. BACKLOG — запись на будущее, а не список текущих задач.

### 3. autopilot.sh НЕ переписывать
Оболочка остаётся самым проблемным и хуже покрытым слоем — и всё равно НЕ начинать её
рефакторинг, перенос на питон, расширение покрытия или дополнительный аудит потому, что
слой «выглядит подозрительно». Она работает в текущем виде. Менять — только при
конкретном воспроизводимом дефекте BLOCKER/MATERIAL. Сам факт низкого покрытия дефектом
не является.

### 4. Единственное основание менять production-код
Конкретный дефект хотя бы одного из классов:
A — неправильная позиция или заявка; B — потеря контроля над фактической позицией
брокера; C — продолжение торговли после состояния, требующего остановки; D —
необоснованная остановка или невозможность штатного восстановления; E — ошибка данных,
сигнала, ролла, маржи или взаимодействия с IBKR, способная повлиять на стратегию; F —
ошибка, реально препятствующая запуску, выпуску или предусмотренному восстановлению.
Всё остальное — BACKLOG.

### 5. Сначала доказательство, потом код — КАРТОЧКА ДЕФЕКТА
Даже обнаружив потенциальную проблему A-F, НЕ исправлять немедленно. Сначала карточка:
ПРОБЛЕМА (что именно неправильно) / ПУТЬ ДОСТИЖЕНИЯ (какое реальное состояние или
последовательность приводит к дефекту) / ВОСПРОИЗВЕДЕНИЕ (тест, стенд, лог, иной
объективный способ) / ПОСЛЕДСТВИЕ (что практически произойдёт в paper/live) / КЛАСС
(BLOCKER, MATERIAL, BACKLOG) / МИНИМАЛЬНАЯ ПРАВКА / РИСК ПРАВКИ (что она может сломать).
Решение о правке принимается только после карточки. Не воспроизводится, путь не доказан —
код НЕ менять.

### 6. Пилот — наблюдение, а не продолжение разработки
Следить прежде всего за: соответствием целевой книги фактической позиции IBKR;
корректностью заявок и исполнений; месячным сигналом; включением/выключением каждой ноги;
ребалансировкой; капом плеча; роллом; восстановлением после реальных сбоев; корректностью
состояния после перезапуска; журналом и тревогами; поведением при отказах IBKR.
НЕ создавать искусственную работу потому, что долго ничего не происходит. Месяц работы
без исправлений — хороший результат, а не повод искать задачу.

### 7. Инциденты
Любое необычное поведение СНАЧАЛА фиксировать, НЕ чинить на ходу. Сохранить: состояние
системы, внутреннюю книгу, состояние IBKR, заявки и исполнения, журнал, входные данные,
время, сигнал, сообщения об ошибках. Затем воспроизвести отдельно. Только подтверждённый
BLOCKER/MATERIAL даёт основание менять текущую версию.

### 8. Минимальность исправлений
Предпочтение по порядку: (1) ничего не менять, если проблема безопасно принимается как
ограничение; (2) удалить ненужный механизм; (3) исправить существующую логику; (4) лишь в
последнюю очередь добавить новый механизм. Запрещено чинить один дефект и заодно «чистить
соседний код». Одна проблема -> одна минимальная правка -> проверка -> остановка.

### 9. Внешние рецензии
Не гонять рецензии ради нуля замечаний. Задача рецензии, если она проводится, — найти
новый воспроизводимый BLOCKER/MATERIAL классов A-F. Не интересуют: стиль, архитектура,
дублирование, комментарии, красота, абстрактное покрытие, гипотетические сценарии без
доказанного пути. Повторный аудит проверенной или замороженной области без нового
конкретного свидетельства запрещён.

### 10. Замороженная торговая часть
Без нового конкретного доказательства ошибки не пересматривать: параметры, SMA-12,
правила сигнала, торговую арифметику, полосу, кап, правила ролла, расчётные движки,
исторический backtest, закрытые слои. Повторную оптимизацию параметров не проводить.

### 11. Принятые ограничения версии — не открытые задачи
Не возвращаться к ним без нового фактического события, показывающего, что ограничение
создаёт MATERIAL/BLOCKER. В частности: НЕ возобновлять работу над внешним selfcheck/WORM
(ограничение M4/№13) и над недостижимой веткой капа (№16) потому, что «технически можно
улучшить».

### 12. Что считать успехом пилота
Искусственного критерия («60 дней без ошибок = готово») НЕТ. Смотреть на реальные
события. За пилот должны быть наблюдены либо безопасно воспроизведены: обычный торговый
день; получение/обновление сигнала; месячный переход; изменение позиции при
необходимости; ролл; перезапуск; отказ/неопределённый исход заявки; сверка книги с IBKR;
предусмотренная аварийная остановка и восстановление. Редкие сценарии допустимо
подтверждать существующими стендами, не дожидаясь их случайного возникновения на счёте.

### 13. Главный принцип
Риск чрезмерной разработки сопоставим с риском недостаточной. Проект уже измерил: большой
объём нового защитного и проверочного кода породил большое число новых дефектов. Рост
сложности сам по себе НЕ повышает надёжность. Надёжность теперь доказывается ПОВЕДЕНИЕМ
существующей системы.

### 14. Штатный результат
Если BLOCKER/MATERIAL отсутствуют, правильный результат работы: «Изменений не требуется.
Paper pilot продолжается». Это нормальный и желательный итог. Главная задача — не
улучшать ADD-FUT, а наблюдать, подтверждает ли эксплуатация, что законченный ADD-FUT
работает как задумано. При инциденте — сначала карточка дефекта по п.5, заказчику.

## Что это

Фьючерсная стратегия ADD-FUT (нога А — ES/MES на S&P 500, нога Б — ZN на 10-летние
трежерис; месячный сигнал 12-SMA, полоса 10%, кап плеча 2,00, квартальные роллы) на этапе
БУМАЖНОГО пилота: живой контур торгует на IBKR paper-счёте под автопилотом из системного
cron. Нормативный документ — `docs/addfut-v1.6.0-r33.txt` (§12 — журнал всех решений и
инцидентов); PDF — только рендер. Код расчёта заморожен регрессией 1e-12; весь живой слой —
в `r33build/live/`.

## Ключевые термины

- **Нога А / Б** — акции (ES/MES, сетка в MES, 10 MES = 1 ES, единица 500 × SPY) /
  трежерис (ZN, модельная единица 112 000 × 0,88 × D_fix).
- **Строка сигнала = месяц ДЕЙСТВИЯ**: решение закрытием месяца M действует в M+1;
  исполняется переключение ВТОРОЙ сессией месяца (strict_states).
- **Полоса / кап** — 10% капитала на выравнивание ноги; плечо ≤ 2,00 по плечу фактического
  ЗАКРЫТИЯ предыдущей сессии (замыкание — отдельный запуск `--close` после 16:00 Чикаго).
- **Ролл** — за 3 сессии до последнего рабочего дня фев/мая/авг/ноя, праздники сдвигают;
  «в месяц поставки не входим»; пропущенный день ролла навёрстывается немедленно.
- **Маршрут Ф / Е** — фьючерсы / UCITS-фонды CSPX+CBU0 (тикер CSBGU0 на EBS) с займом;
  переход — только `transition.py`, книга передаётся `hand_over_book`.
- **Книга / намерение / замок** — состояние в `~/.addfut/book-*.json`; intent пишется ДО
  первой заявки; один замок на счёт (`state.lock_dir`).
- **О-5** — ручной разбор: автоматика ничего не лечит сама; ALARM-файл останавливает
  автопилот целиком.
- **Режим рецензий** — с 13.08.2026 (после 15-го круга) ПО УМОЛЧАНИЮ максимум:
  gpt-5.6-sol, mode=pro, effort=max — зашито в `tools/openai_review.py`. Прежнее правило
  («ультра» только по разовой команде, умолчание medium) отменено заказчиком.

## Правила работы (нарушение любого — стоп)

1. **Параметры априори, подгонка запрещена.** Отвергнутое — в §9а, не удалять.
2. **Замороженные движки** (`sim_v13`, `sim_v164`) не менять: регрессия 1e-12 нормативна.
3. **Выпуск пакета — только `tools/release.py`**: манифест → архив → распаковка в чистый
   каталог → selfcheck; корневой хэш пишется только после этого.
4. **Каждая правка подтверждается** грепом/прогоном — «правка без проверки не правка»
   (инцидент §12). Новые утверждения обязаны иметь ненулевое покрытие; новые защиты —
   парную мутацию; мутационные прогоны — с чистым базлайном.
5. **Стенды не трогают машинное состояние**: ADDFUT_LOCK_DIR/ADDFUT_BOOK_PATH/
   ADDFUT_SIGNALS — на временные пути; selfcheck сверяет lstat-снимок `~/.addfut`.
6. **Секреты не в репозитории** (только `~/.addfut/`, права 600). Живой (не paper) контур
   запрещён без явного ADDFUT_LIVE_OK; paper определяется портом 4002.
7. **Статус заявки IBKR ≠ факт**: исполнение — только по permId-отчётам, запасных путей нет;
   повтор заявки при неизвестном исходе запрещён.
8. **[ЦЕЛЬ «ДО НУЛЯ» ОТМЕНЕНА ТЗ 28.08.2026 — см. «ТЗ завершения» п.4; дисциплина разбора (каждое замечание проверить по коду) ОСТАЁТСЯ.]** Прежняя редакция: Внешние рецензии гонять до НУЛЯ замечаний (решение заказчика 13.08.2026). Каждый
   круг: обновить BRIEF в `tools/review_new_code.py`, прогнать, КАЖДОЕ замечание проверить
   по коду (не верить на слово) и либо исправить с доказательством, либо мотивированно
   отклонить в §12.
8а. **САМОРЕЦЕНЗИЯ ПЕРЕД ВНЕШНЕЙ, В ДВА ПРОХОДА (решение заказчика 17.08.2026).** Разобрав
   круг и починив все замечания, я обязан СНАЧАЛА отрецензировать сам себя — тем же
   враждебным взглядом и по тому же списку классов, что и внешний рецензент, — найденное
   исправить, затем пройти ВТОРОЙ раз по правкам первого прохода, и только ПОСЛЕ этого
   отправлять пакет наружу. Отправка без двух проходов запрещена.
   **ВТОРОЙ ПРОХОД ПРОВЕРЯЕТ НЕ КОД, А ДОКАЗАТЕЛЬСТВА (уточнено 17.08 после 39-го круга).**
   Каждый зонд, прогон и стенд, которым я подтвердил правку, обязан сначала доказать, что
   он ДОСТИГАЕТ проверяемой ветки: печатью пройденного пути, счётчиком заявок, отличием
   результата от базлайна. Иначе получается то, что уже случилось: правку «STOP запрещает
   замыкание» я подтвердил зондом, вставлявшим STOP ПОСЛЕ ветки, которая замыкает день и
   делает return, — проверил не то место и объявил доказанным. Зелёный прогон недостижимой
   проверки хуже отсутствия проверки: он снимает вопрос.

   **ПЯТЬ УГЛОВ — ОБЯЗАТЕЛЬНАЯ ПРОЦЕДУРА ПЕРЕД КАЖДОЙ ОТПРАВКОЙ (решение заказчика
   18.08.2026, по итогам 42-го круга).** Повторять одни и те же вопросы по кругу
   малополезно: они все начинаются ОТ МОЕЙ ПРАВКИ и смотрят наружу, а пропущенное находится,
   когда заходишь с другой стороны и смотришь НА правку. Поэтому саморецензия идёт с пяти
   разных точек входа. Каждый угол ниже назван вместе с находкой, которая его породила, —
   чтобы процедура не выродилась в ритуал.

   (1) ОТ СЕМЬИ ДЕФЕКТА, МЕХАНИЧЕСКИ. Не «где ещё я такое помню», а грепом по КЛАССУ:
       `environ.setdefault`, `ls -1t`, `st_mtime`, `-mtime`, `$(( $(`, «проверка только
       непустого значения» и т.п. Память выбирает знакомое, греп — все. ПОВОД: так нашлось
       третье место класса «порядок по изменяемой величине» (уборка отметок traded-/closed-
       по -mtime), которого не назвал ни рецензент, ни я, — после ротации архивов и выбора
       последнего якоря.
   (2) ОТ ПРОТИВОПОЛОЖНОГО ЗНАКА. Каждое УЖЕСТОЧЕНИЕ: какой законный путь оно закрыло?
       Каждое ПОСЛАБЛЕНИЕ: какой незаконный впустило? ПОВОД: правка preview заперла
       АВАРИЙНЫЙ выход Е→Ф ровно при маржинальном стрессе — план, освобождающий маржу,
       запрещался из-за того, что маржи сейчас мало.
   (3) ОТ ЖИВОГО СОСТОЯНИЯ И РЕАЛЬНОГО КАЛЕНДАРЯ, А НЕ ОТ ФИКСТУР. Прогнать изменённые пути
       на НАСТОЯЩЕЙ книге и на конкретных датах (ближайший ролл, порог выпуска, край окна).
       ПОВОД: ролл 26.08 — фикстуры его не показывали, живая книга показала сразу.
   (4) ОТ ЧУЖОЙ ПРАВКИ. Не ломает ли правка А допущение правки Б? ПОВОД: перенос last_session
       при откате ролла и календарный сторож следующего дня; нулевой ИТОГ и выборка издержек
       §7 (проверено, что такая дата в выборку не попадает).
   (5) ОТ ОТРИЦАНИЯ. Какую мутацию новый стенд НЕ поймает? ПОВОД: условие `is None`
       короткозамыкало парную мутацию, и защита была ненаблюдаема, пока правило не вынесли в
       отдельную функцию с ОДНОЙ точкой мутации.

   **УГЛЫ ПРОХОДЯТСЯ НА КАЖДОЙ ПРАВКЕ, А НЕ ПАЧКОЙ В КОНЦЕ (решение заказчика 19.08.2026).**
   Правка проверяется углами СРАЗУ, пока она одна и её последствия ещё видны, а не через
   десяток других правок. ПОВОД, измеренный в тот же день: рецензия кода дала 15 находок, и
   ВСЕ 15 пришлись на правки, сделанные за эти сутки; мой проход углами шёл в конце и по
   построению не покрывал правки, сделанные ПОСЛЕ него, — а именно они и оказались
   дефектными. Трижды правка воспроизводила ровно тот дефект, который закрывала (кэш
   якорей воскресил самонегацию №13; `_release_by_measure` перехватывал ошибки кода раньше
   моего же запрета; причина отказа предпросмотра жила в замыкании и потому не мутировалась).
   Минимум на каждую правку: зонд достижимости, угол «от противоположного знака» и угол «от
   отрицания» (какую мутацию новый стенд НЕ поймает). Полные пять углов — перед отправкой.

   **РЕЦЕНЗИЯ КОДА (/code-review) — ОБЯЗАТЕЛЬНЫЙ ЭТАП ПЕРЕД ВНЕШНИМ КРУГОМ; ПРОСИТЬ ЕЁ —
   МОЯ ОБЯЗАННОСТЬ, НЕ ЗАКАЗЧИКА (решение заказчика 19.08.2026, подтверждено 20.08).**
   Запустить навык может только заказчик, поэтому я обязан ЗАПРОСИТЬ рецензию сам — в конце
   каждого круга правок и перед каждым выпуском, не дожидаясь напоминания. Забыть запрос —
   то же самое, что пропустить этап. Разобрав круг и пройдя углы, я обязан ПОПРОСИТЬ заказчика
   запустить `/code-review` по всем правкам (сам запустить не могу — навык вызывается
   заказчиком), разобрать КАЖДУЮ находку по дисциплине круга — проверить по коду, исправить
   с доказательством или мотивированно отклонить в §12, — и только ПОСЛЕ этого отправлять
   внешний круг. Отправка внешнего круга без пройденной рецензии кода запрещена. Основание:
   19.08 рецензия кода нашла 15 действительных дефектов в правках, которые уже прошли мою
   саморецензию в два прохода и пять углов; это не замена внешнему кругу, а фильтр перед ним.

   **МЕСТО РЕЦЕНЗИИ В ПОРЯДКЕ — МЕЖДУ ПРАВКАМИ И УГЛАМИ, А НЕ ПОСЛЕ УГЛОВ (21.08.2026).**
   Прогон 21.08 по правкам 45-го круга дал 31 дефект в коде, который я уже прошёл двумя
   проходами и пятью углами; исправляя тринадцать замечаний круга, я завёл дефектов больше,
   чем закрыл. Причина видна по составу находок: почти все лежат на СТЫКЕ правки с кодом,
   которого правка не касалась (min_prev против допуска в чужой функции; net_positions
   против контракта refresh; имя счётчика против его читателя; порог против аварийного
   признака этажом ниже). Мои углы заходят ОТ правки и смотрят наружу — стык они видят
   только с одной стороны. Поэтому порядок такой: правки -> `/code-review` -> разбор ВСЕХ
   находок -> пять углов ЗАНОВО, уже по правкам разбора -> отправка. Углы после рецензии, а
   не до неё: иначе они проверяют код, которого после разбора уже нет.

   **УГЛЫ — МОИ, А НЕ РЕЦЕНЗЕНТА (решение заказчика 18.08.2026).** В BRIEF рецензенту эти
   пять углов НЕ передаются, и это осознанно. Они выведены из МОИХ ошибок, то есть служат
   картой моих слепых мест, а не его; отдав их, я привязал бы его к своей рамке — а вся его
   ценность в том, что он не разделяет моего представления о коде. Хуже того, углы я прохожу
   ДО отправки, поэтому он нашёл бы уже починенное, и круг ушёл бы в подтверждение вместо
   открытия. Вместо углов BRIEF несёт раздел «УЖЕ ПРОВЕРЕНО МНОЮ И ЧЕМ ИМЕННО» — перечень
   ЗАКРЫТЫХ полей с результатом, чтобы он искал там, где я не смотрел. Плюс ему передаются
   ровно два угла, где он сильнее по устройству: (а) от противоположного знака — что закрыло
   моё ужесточение; (б) взаимное влияние правок круга, потому что он видит весь diff разом, а
   я смотрю правки по одной. Проверить решение можно по составу находок: если они начнут
   повторять мои — наведение перевесило, и раздел надо сокращать.

   ВНУТРИ УГЛОВ ОСТАЮТСЯ ТРИ ВОПРОСА (17.08, по разбору 40-го круга — ими были пропущены три
   его замечания):
   а) ТРАЕКТОРИЯ, А НЕ СНИМОК. Новые ворота, способные остановить контур, проверяются на
      БУДУЩЕМ состоянии: что накапливается, что ротируется, что чистится по расписанию. Если
      проверка на сегодняшнем состоянии молчит, её рабочий режим НЕ испытан вовсе. Повод:
      проверка WORM-якоря требовала наличия КАЖДОГО архива при ротации до 60 — отказ контура
      примерно после 62 замыканий.
   б) ПРАВИЛО, А НЕ ПРИМЕР. Починив случай из замечания, сформулировать правило одной фразой
      и найти ВТОРОЙ, непохожий случай того же правила. Повод: «на ролле упаковка бесплатна»
      было реализовано как «канонизировать исходную книгу» — совпадает с правилом лишь пока
      количество не переходит границу упаковки.
   в) ЗОНД ДОСТИЖИМОСТИ — НА КАЖДЫЙ стенд, а не на подозрительные. Повод: свой только что
      написанный зелёный стенд дважды подряд не достигал проверяемой ветки.

8б. **ПОСЛОЙНЫЙ АУДИТ СТАРОГО КОДА — ПАРАЛЛЕЛЬНО РАЗБОРУ КРУГА (решение заказчика
   20.08.2026).** Тройная проверка свежего кода ловит то, что я только что написал, и по
   построению не касается того, что лежит давно. Цифры 45-го круга: из тринадцати замечаний
   лишь ТРИ пришлись на код последних двух суток, а остальные десять — на слои, которых не
   трогала ни одна проверка: широкий except предполёта живёт с 25-го круга, fail-open
   поставочного сторожа — с 13-го, дефект дописывания в пустой журнал — с 10-го. Пока эти
   слои без стендов, рецензент будет находить в них дефекты сколько угодно кругов подряд, и
   ряд замечаний не сойдётся к нулю никогда.
   ПОРЯДОК: КАЖДЫЙ круг, помимо разбора замечаний, закрывается ОДИН слой из реестра
   (PROJECT-STATE, §5б). Не «все сразу» — один, до конца.
   ПРОЦЕДУРА СЛОЯ (иначе аудит выродится в чтение по диагонали):
   (1) перечислить ВСЕ ветки слоя механически — грепом, AST, обходом функций, а не по
       памяти: память показывает знакомое;
   (2) на каждую ветку ответить тремя фактами — есть ли стенд, есть ли парная мутация,
       ДОСТИЖИМА ли она (зонд, а не намерение);
   (3) непокрытые ветки СНАЧАЛА получают стенды, и только потом идёт враждебное чтение:
       иначе найденное нечем доказывать и нечем удержать;
   (4) враждебное чтение слоя целиком по пяти углам;
   (5) итог слоя — в §12: сколько веток, сколько было покрыто, сколько стало, что найдено.
   Слой считается закрытым не когда «прочитан», а когда каждая его ветка наблюдаема
   мутацией.

8в. **ЧЕТВЕРО МЕХАНИЧЕСКИХ ВОРОТ И БЮДЖЕТ СТРОК (решение заказчика 22.08.2026).**
   Основание — измерение, а не ощущение. Три подряд круга правок: 1023 строки → 31 дефект,
   1828 → 20, 986 → 15, то есть один дефект на 33–91 строку. Мои пять углов и два прохода
   саморецензии нашли из этих шестидесяти шести практически НОЛЬ — всё нашла рецензия.
   Значит рычаг «проверять внимательнее» исчерпан: внимание не масштабируется, а строки я
   пишу пачками по тысяче. Число находок следует за числом НАПИСАННЫХ СТРОК, и уменьшать
   надо две величины независимо: плотность дефекта на строку и число строк.

   ВОРОТА (работают без моего внимания; после каждых — прогон и коммит):
   (1) КАЖДАЯ НОВАЯ ВЕТКА ИСПОЛНЕНА В ОБЕ СТОРОНЫ. Трассировщик на `sys.settrace` собирает
       во время батареи множество взятых ветвей и сверяет с `git diff`; ветка, не взятая ни
       разу или взятая в одну сторону, — отказ. Ловит самый дешёвый и самый дорогой класс:
       код, ни разу не исполнявшийся до объявления готовым (сегодня — 4 из 15, включая
       NameError на аварийном пути и флаг, тождественно ложный по построению).
   (2) СТЕНД, КОТОРОГО НЕ УБИВАЕТ НИ ОДНА МУТАЦИЯ, — НЕ СТЕНД. Сейчас спрашивается «поймана
       ли каждая мутация»; обратный вопрос — «убита ли каждая проверка» — не задаётся, и
       проверка замолкает незаметно. Данные уже печатает мутационный прогон, нужно
       перевернуть таблицу стенд×мутация и объявить отказом строку без единого убийцы.
   (3) ПРИЧИНЫ — ПО КОДУ, А НЕ ПО ПРОЗЕ. Каждый производитель тревоги печатает машинную
       метку `ADDFUT-ПРИЧИНА: <код>`, диагност сопоставляет КОДЫ. Ровно так уже устроен
       `ADDFUT-VERDICT` в оболочке — и там этот класс исчез. Свободный текст сопоставлять
       перечнем слов бесполезно: перечень отстаёт от производителей каждый круг.
   (4) ОДНА ПРАВКА — ОДИН ГРЕП ПО КЛАССУ, СПИСОК ПОПАДАНИЙ В КОММИТ. Не «я помню, где ещё
       так», а команда и её ПОЛНЫЙ вывод; каждое непокрытое попадание названо явно.

   (5) ГРЕП ПО КЛАССАМ СОБСТВЕННЫХ ДЕФЕКТОВ — ПЕРЕД КОММИТОМ, А НЕ ПОСЛЕ ОТКАЗА ВОРОТ
       (решение заказчика 26.08.2026). Основание измерено за один аудит слоёв: ворота
       поймали меня ПЯТЬ РАЗ на одном и том же классе — лишнее плечо `except` в зонде,
       недостижимое потому, что проверяемый вызов падает предсказуемым типом. Каждый раз
       я честно исправлял названное место и честно воспроизводил класс в следующем. Когда
       вместо этого я запустил ГРЕП по классу, он нашёл ЧЕТЫРЕ места сразу, из которых
       ворота успели назвать одно; остальные три дошли бы до отказа ещё через несколько
       кругов, по одному за раз. Разница не во внимании, а в порядке: ворота показывают
       ОДНО место, греп — ВСЕ.
       ПОРЯДОК: перед каждым коммитом прогнать греп по классам, которые уже ловили меня
       в этом круге, и выправить ВСЕ попадания разом. Вывод команды — в коммит, как
       требуют ворота (4).
       ИЗВЕСТНЫЕ КЛАССЫ СОБСТВЕННЫХ ДЕФЕКТОВ (список пополняется, не сокращается):
        * развилка восстановления окружения в `finally` (`if _keep... is None/is not None`):
          в среде батареи переменной нет НИКОГДА, поэтому любое `if/else` держит одно плечо
          мёртвым. Лечение — снять ветвление: `pop`, затем `update` пустым словарём;
        * разбор исходов НЕСКОЛЬКИМИ `except` в зонде: достижимо ровно одно плечо.
          Лечение — не классифицировать заранее, а спросить ФАКТ одним плечом
          (`except BaseException` -> тип) и сравнить снаружи;
        * стенд без судьи: случай написан и зарегистрирован, зеленеет — а `rinv` на него
          нет, и батарея не проверяет ничего;
        * плечо зонда, недостижимое потому, что проверяемый вызов падает ВСЕГДА
          («без исключения», «принят», «прошло»);
        * СИГНАЛЬНОЕ ИСКЛЮЧЕНИЕ ЗОНДА РОДСТВЕННО ТОМУ, ЧТО ЛОВИТ ПРОВЕРЯЕМЫЙ КОД
          (27.08.2026). Проверяя самоблокировку flock, я сигналил по SIGALRM классом
          TimeoutError — подклассом OSError, — и его сглотнул `except OSError` вокруг
          захвата замка, поставленный мною же тремя часами раньше. Зонд доложил «дефекта
          нет» о дефекте, который час назад остановил батарею. Лечение механическое:
          перед написанием зонда посмотреть, какие `except` стоят на пути сигнала, и взять
          тип ВНЕ их иерархии (свой подкласс BaseException).
        * КИРИЛЛИЧЕСКОЕ ИМЯ ПЕРЕМЕННОЙ В ОБОЛОЧКЕ (добавлено 26.08.2026). bash не
          поддерживает нелатинские ИДЕНТИФИКАТОРЫ: `имя=значение` он считает не
          присваиванием, а КОМАНДОЙ — «command not found», подстановка пуста, и проверка
          молча мерит пустоту, оставаясь зелёной. Правило 10 требует русского в
          комментариях и документах, но к идентификаторам оболочки неприменимо (имена
          ФУНКЦИЙ при этом работают — проверено исполнением). Класс исключительно живуч:
          за один заход 26.08 я вступил в него ЧЕТЫРЕ раза, включая случай, когда написал
          предупреждение о нём десятью строками выше по тому же файлу, и два раза — в
          собственных разовых командах, где грепа по файлам мало. Практический вывод:
          в bash идентификаторы ТОЛЬКО латиницей, без исключений и без «здесь же видно».
          СТРУКТУРНЫЙ ВЫВОД 27.08.2026, ПОСЛЕ СЕДЬМОГО ПОПАДАНИЯ: дело не во внимании.
          Класс записан в этот самый список, я его перечитывал — и всё равно вступал в него
          в разовых командах, где грепа по файлам нет. ЗОНДЫ И РАЗОВЫЕ ПРОВЕРКИ ПИСАТЬ НА
          PYTHON, А НЕ НА BASH: Python кириллические имена поддерживает, и класс исчезает
          вместе с языком, а не удерживается дисциплиной. Bash оставить там, где он нужен
          по существу (боевая оболочка, хуки), и писать в нём только латиницей.
          ЦЕНА ОШИБКИ ИЗМЕРЕНА: седьмой раз пришёлся на зонд, проверявший корень доверия
          глазами аудитора, — он измерил ПУСТОТУ и напечатал правдоподобный результат.
       САМ ГРЕП ВЫВОДИТСЯ МЕХАНИЧЕСКИ, А НЕ ПО ПАМЯТИ — уточнено в тот же день, по
       первому же запуску правила. Я искал случаи без судьи по ОДНОМУ написанию ключа
       (`r['case'] ==`) и получил три ложных попадания; в коде написаний ЧЕТЫРЕ
       (`r['case']`, `r.get('case')`, `r['kind']`, `r.get('kind')`), и полный перебор даёт
       159 судей на 31 случай, то есть БЕЗ СУДЬИ — НОЛЬ. Дефект здесь ровно тот, против
       которого правило заведено: механику я построил на воспоминании о соглашении. Прежде
       чем верить выводу грепа, перечислить написания искомого ТОЖЕ грепом (или разбором),
       и только потом считать. Иначе ворота дают ложную тревогу, а ложная тревога стоит
       дороже молчания: на неё тратится круг.
       ЧЕГО ЭТО НЕ ОТМЕНЯЕТ: ворота остаются — они ловят то, чего в списке ещё нет. Правило
       добавляет к ним МОЙ ход, а не заменяет их.

   БЮДЖЕТ: НЕ БОЛЬШЕ ~200 ИЗМЕНЁННЫХ СТРОК ЗА ПОДХОД, дальше — прогон, рецензия, коммит.
   При нынешней плотности это ~3 находки вместо 15–20. Общий объём работы тот же, но ряд
   начинает сходиться, а не колебаться. Тысяча строк за день — это два десятка дефектов
   арифметически, а не по небрежности.

   ЧИНИТЬ УДАЛЕНИЕМ, А НЕ ДОБАВЛЕНИЕМ, ГДЕ ВОЗМОЖНО: два дефекта 21.08 пришли из защит,
   которые я добавил, а через несколько часов сам же снял как неработающие.

   ВОРОТА И УГОЛ «ОТ ОТРИЦАНИЯ» ЗАКРЫВАЮТ РАЗНОЕ, И ОДНО НЕ ЗАМЕНЯЕТ ДРУГОГО
   (наблюдение 26.08.2026, сохранено по решению заказчика). ВОРОТА ЛОВЯТ НЕИСПОЛНЕННОЕ:
   строка, до которой не дошли, ветка, взятая в одну сторону. УГОЛ «ОТ ОТРИЦАНИЯ» ЛОВИТ
   ИСПОЛНЕННОЕ, НО НЕНАБЛЮДАЕМОЕ: строка исполняется, ветка берётся, прогон зелёный — а
   мутация в этом месте не убивает ни одного утверждения.
   ПОВОД, измеренный в тот же день. В стенде часов пары третье утверждение («вторая пара
   не сбрасывает первую») я дописал НЕ потому, что увидел непокрытую строку, — покрытие
   было полным. Я спросил: какую мутацию этот стенд НЕ поймает? Ответ — «словарь часов
   создаётся заново на каждом mark_pair»: она обнуляла бы часы ПЕРВОЙ пары при пуске
   второй, то есть ровно на ролле, когда пар две и одна нога уже закрыта. Ворота этого не
   показали бы никогда: строка исполняется, ветка берётся.
   ПРАКТИЧЕСКИ: зелёные ворота — НЕ основание пропустить угол, а зелёный угол — не
   основание пропустить ворота. В тот день оба сработали по разу и нашли РАЗНОЕ.

   ЧЕГО ЭТО НЕ ДАЁТ: ноль замечаний от одних ворот не наступит — они закрывают около
   десяти из пятнадцати сегодняшних, остальное суждение, а не механика. Но при бюджете в
   200 строк «остальное» — одна-две находки на круг.

8г. **[ЗАМЕНЁН ТЗ 28.08.2026 — см. «ТЗ завершения» п.4: рецензия ищет только классы A–F, чинятся только BLOCKER и MATERIAL.]** Прежняя редакция: КРИТЕРИЙ ОТПРАВКИ ВНЕШНЕГО КРУГА (решение заказчика 23.08.2026). Внутренние
   прогоны /code-review повторяются ДО ТЕХ ПОР, пока НЕСКОЛЬКО прогонов подряд не дадут
   НОЛЬ находок классов «остановка контура» и «дыра в боевом коде». Пока в сводке есть хоть
   одна находка этих двух классов — чинить и прогонять снова; косметика и «ложная
   уверенность» (проверка, которая врёт зелёным) отправке не препятствуют, но разбираются
   как обычно. Основание: ряд 31 → 20 → 15 → 19 при плотности ~1 дефект на 40 строк —
   внешнему рецензенту отдаётся код, в котором внутренний фильтр уже не находит дорогого.
   Заморозка производства ОТКЛОНЕНА заказчиком тогда же: продолжаем чинить, меряем состав
   находок по трём шкалам (деньги сейчас / остановка контура / ложная уверенность).

8д. **РЕШЕНИЯ — В ПИТОН, ОБОЛОЧКА ОСТАЁТСЯ ЗАПУСКАЛКОЙ (решение заказчика 27.08.2026).**
   Живой контур написан на двух языках: питон — 6900 строк (вся арифметика денег), командный
   файл — 1427 строк (часы, замки, запуск шлюза, 35 вызовов питона). Разделение историческое
   и само по себе нормальное: cron запускает КОМАНДУ, и что-то обязано ею быть.
   ПРОБЛЕМА НЕ В ДВУХ ЯЗЫКАХ, А В ТОМ, ЧТО В «РУКАХ» ЗАВЕЛАСЬ «ГОЛОВА». Оболочка сегодня не
   только зовёт питон — она сама решает: торговый ли день, не торговали ли уже, не стоит ли
   тревога, пора ли замыкать. Только в tick — 29 развилок. Эти решения живут в слое, который
   ворота 8в-1 НЕ ВИДЯТ: sys.settrace понимает лишь питон, поэтому про 1427 строк ворота не
   сказали ни слова ни разу, и молчание читалось как «здесь всё в порядке». Слой простоял
   непокрытым дольше всех именно поэтому.
   ПЕРЕПИСЫВАНИЕ ОТКЛОНЕНО, И ПО СОБСТВЕННОЙ АРИФМЕТИКЕ: 1400 новых строк при измеренной
   плотности 1 дефект на 33-90 строк дают 15-40 новых дефектов — ровно в том слое, который
   останавливает контур и подаёт заявки, взамен кода, пережившего 45 кругов рецензий и месяц
   живой работы. Это ровно то «много нового кода», против которого заведено правило 8в.
   ПОРЯДОК: новые развилки и новые решения писать ТОЛЬКО в питоне, где их видят ворота;
   в оболочке — вызов. Существующее не переписывать, а покрывать стендами по процедуре 8б.
   Так опасная часть уменьшается, не прибавляя сегодня ни одной строки.

8е. **ПРАВКА В ЕЖЕДНЕВНОМ КОДЕ РАДИ ДЕФЕКТА В РЕДКОМ — ТОЛЬКО ПОСЛЕ ВОСПРОИЗВЕДЕНИЯ
   (решение заказчика 28.08.2026).** Основание измерено за одни сутки. Из 27 находок
   рецензии 28.08 в бою наблюдалась ОДНА; остальные 26 — рассуждения о том, что могло бы
   случиться. Чиня их, я дважды сделал хуже ровно там, где было хорошо:
    * `tz.py` — прежний код был кривой, но безобидный («racy but idempotent and never
      raised»); мой «правильный атомарный» уронил процесс в 14 случаях из 240 при
      параллельном импорте, а импортируют его шесть боевых модулей;
    * замок сигнального ряда — гонка была теоретическая; после правки контур сначала
      завис на час на самоблокировке, затем получил гонку другого рода и риск повесить
      торговую сессию на чужом замке.
   ОБЩЕЕ У ОБОИХ: дефект жил на пути, который исполняется раз в год, а правка легла на
   путь, который исполняется каждый день. ПРАВКА ОКАЗАЛАСЬ ОПАСНЕЕ ДЕФЕКТА, и это не
   случайность, а арифметика: ежедневный путь получает в сотни раз больше попыток
   сломаться.
   ПОРЯДОК. Прежде чем править код, исполняющийся ежедневно, ради дефекта в коде,
   исполняющемся редко, — ВОСПРОИЗВЕСТИ дефект: прогон, в котором он случился, а не
   описание, в котором он мог бы случиться. Нет воспроизведения — находка записывается в
   реестр с пометкой «не воспроизведена» и НЕ чинится.
   КАК СЧИТАТЬ «ЕЖЕДНЕВНЫЙ» И «РЕДКИЙ»: по числу исполнений в год. tick — 6 000 раз в год,
   замыкание — 250, ролл — 4, аварийный откат — 0-2, путь первого запуска — 1. Если путь
   правки исполняется ЧАЩЕ пути дефекта, требуется воспроизведение.
   ЧЕГО ЭТО НЕ ОТМЕНЯЕТ: находку всё равно записать и измерить. Запрет на ПРАВКУ, а не на
   знание. И это не индульгенция редкому коду: дефект, воспроизведённый стендом, чинится
   как обычно — стенд и есть воспроизведение.
   ПРЕДПОЧТИТЕЛЬНЫЙ ВИД ПРАВКИ — УДАЛЕНИЕ МЕХАНИЗМА, А НЕ ДОБАВЛЕНИЕ ЗАЩИТЫ. Замок,
   которого нет, не самоблокируется; временный файл, которого нет, не остаётся хвостом.
   Если дефект уходит вместе с механизмом — убрать механизм.

9. **Итоги каждого круга и инцидента — в §12 спецификации**, затем перевыпуск пакета.
10. Язык проекта — русский (код, комментарии, документы, отчёты).
11. **Защита от удаления — МЕХАНИЗМ, А НЕ ОБЕЩАНИЕ (с 17.08.2026).** Никаких `rm -rf`/
    массовых удалений в каталоге проекта и в `~/.addfut*` — удалять файлы проекта можно
    только поштучно, с явного согласия заказчика, и после git-коммита текущего состояния.
    Правило ИСПОЛНЯЕТСЯ ХУКОМ: `tools/guard_destructive.sh` подключён как PreToolUse на
    Bash (`.claude/settings.json`) и ОТКАЗЫВАЕТ до выполнения команды — рекурсивное и
    принудительное удаление, любое удаление в машинном состоянии и точках восстановления,
    массовый откат дерева (`git reset --hard`, `clean`, `checkout --`, `restore`),
    `find -delete`, перенос корневых каталогов. Согласие заказчика оформляется меткой в
    самой команде: `# РАЗРЕШЕНО-ЗАКАЗЧИКОМ` — тогда команда проходит, а согласие остаётся
    доказуемым в переписке. Удаление под `/tmp` не охраняется: сторож, мешающий обычной
    работе, приучает обходить себя меткой.
    Восстановление: git-история (`git log`), локальное зеркало
    `~/git-mirrors/vol-down12.git`, датированные выпуски в `releases/`, бэкапы состояния в
    `~/.addfut-backups/` и `~/state-mirror/`, побайтовая копия на Google Drive
    (`vol-down12-backups`, подключена 17.08).
12. **Коммит и пуш — СВОЕВРЕМЕННО, а не в конце круга (решение заказчика 19.08.2026):**
    после каждого закрытого замечания или логически цельной правки — git-коммит и сразу
    пуш (зеркало + GitHub). После каждого выпуска — обязательно. Механическая страховка —
    `tools/wip_push.sh` из системного cron (7,37 * * * *): пушит незапушенные коммиты и
    копирует незакоммиченные файлы побайтово на Drive в `wip/<дата>/`; журнал —
    `~/.addfut/wip-push.log`. Страховка НЕ коммитит и не заменяет ручной дисциплины.
    Полная выгрузка — `tools/backup_push.sh` (зеркало, GitHub `tomsoyer15-eng/vol-down12`,
    Drive).

## Грабли среды (см. также §7 PROJECT-STATE.md)

`pkill -f` убивает собственную сессию — только kill по PID. НО И `pgrep -f` НЕНАДЁЖЕН: он
совпадает с ТЕКСТОМ СОБСТВЕННОЙ команды-обёртки, поэтому цикл ожидания вида
`while pgrep -f X; do sleep; done` сторожит сам себя и выходит немедленно, доложив ложное
«завершено». Это стоило мне двадцати часов ложного доклада о выпуске (§12) и повторилось
18.08 на ожидании мутаций. Ждать процесс только так: взять PID через
`ps -eo pid,comm,args | grep <файл> | grep -v grep`, УБЕДИТЬСЯ, что `comm` — это python
(а не bash-обёртка), и крутить `while [ "$(ps -o comm= -p PID)" = python ]`. FRED С ЭТОЙ МАШИНЫ ДОСТУПЕН — ПРЕЖНЯЯ ЗАПИСЬ «ЗАБЛОКИРОВАН» УСТАРЕЛА
(проверено живьём 21.08.2026: DNS резолвится, fredgraph.csv?id=DGS10 отдаёт 268 КБ за 0,06 с,
ключ API не нужен; так же скачались семь рядов ставок OECD для соседнего проекта). Про stooq
проверки не было — его считать закрытым до опыта, а не по аналогии. Урок общий: запись о
среде стареет молча, и «нельзя» надо перепроверять опытом, прежде чем строить на нём решение.
Основные данные — по-прежнему IBKR (сроки >12 мес запрашивать в ГОДАХ, иначе пустой ответ без
ошибки). `reqAccountUpdates` на paper-шлюзе виснет — использовать `accountSummary`.
**ОТКАЗ ШЛЮЗА ВЫГЛЯДИТ ПУСТОТОЙ, ПОТОМУ ЧТО ПОЛЕ ПЕРЕКЛЮЧАЕТ МАРШРУТ ОБСЛУЖИВАНИЯ** (два
случая за 18.08, семья одна): (1) `whatIfOrder` отдаёт пустой СПИСОК, если TIF заявки не
задан явно — пресет счёта переопределяет TIF (предупреждение 10349); лечится тем, что поле
надо ЗАДАТЬ. (2) Комбинация (`secType='BAG'`) молчит без ошибки, если у `ComboLeg` задать
`exchange='CBOT'`: запрос уходит на маршрут РЕАЛЬНОГО ВРЕМЕНИ (`ticker.marketDataType=1`),
подписки нет; лечится тем, что поле надо НЕ ЗАДАВАТЬ (тогда тип 3, задержанные). Направление
противоположное, вывод общий: НЕ ОБЪЯСНЯТЬ пустоту догадкой. Смотреть `ticker.marketDataType`,
слушать `errorEvent` БЕЗ фильтра (там нашлась ошибка 200) и печатать тип возвращённого
объекта — оба раза ответ был, и оба раза я его не слушал. Часы
машины ≠ биржа: всё торговое время считать в America/Chicago явно.
