#!/usr/bin/env python3
"""Рецензия кода, написанного после шестого круга.

Седьмой круг отличается тем, что вся проверочная машинерия шестого круга сама написана
за один заход и никем, кроме автора, не смотрена. Рецензенту это сообщается прямо: главная
цель — не код стратегии, а ДОКАЗАТЕЛЬНАЯ СИЛА проверок, которыми он объявлен верным.
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
import openai_review as R

# НОМЕР КРУГА — В ОДНОМ МЕСТЕ (15.08): метка джоба была захардкожена отдельно от брифа и
# разошлась с ним — джоб 23-го круга ушёл в реестр как «двадцать второго». Реестр джобов
# служит доказательством оплаты и очерёдности, поэтому расхождение здесь не косметика.
KRUG_N = 32                          # номер: имя файла рецензии
KRUG_IM = 'тридцать второй'          # именительный: заголовок брифа
KRUG_ROD = 'тридцать второго'       # родительный: метка джоба в реестре

BRIEF_TPL = r"""
Ты — враждебный рецензент кода торговой системы. Найди дефекты, способные стоить денег, и
ЛОЖНЫЕ ДОКАЗАТЕЛЬСТВА. Не пересказывай и не хвали. По-русски, файл и функция, по приоритету:
неверная позиция, поставка, рассинхронизация с брокером, порча состояния.

КОНТЕКСТ. Нога А — ES/MES (10 MES = 1 ES, единица 500 x SPY), нога Б — ZN (модельная единица
112000 x 0,88 x D). Сигнал §1 МЕСЯЧНЫЙ: решение закрытием месяца M действует в M+1; строка
ряда помечена месяцем ДЕЙСТВИЯ. Полоса 10%, кап 2,00 по фактическому закрытию (замыкание —
отдельный запуск), ролл за 3 сессии до конца фев/мая/авг/ноя по праздничной таблице. Маршрут
Е — CSPX/CBU0; переходный исполнитель, лимит непарной дельты 1%.

{KRUG_IM_UP} КРУГ. Прошлые: 20, 16, 17, 22, 27, 28, 14, 18, 28, 12, 10, 8, 10, 10,
7, 6, 16, 21, 22, 22, 20, 24, 26, 29, 19, 22, 23, 18, 18, 20, 18. Тридцать первый разобран
ЦЕЛИКОМ: ВСЕ 18 замечаний признаны действительными и исправлены, отклонённых нет.
ЦЕЛЬ — НОЛЬ. Каждое замечание обязано называть денежный путь или ложное доказательство.

ЧТО ПОВТОРЯТЬ НЕ НУЖНО — ЗАКРЫТО РАЗБОРОМ, НЕ ОТГОВОРКОЙ:
(1) strict_states «исполняет сигнал не тем месяцем» — ОТКЛОНЁН седьмой раз подряд: строка
ряда помечена месяцем ДЕЙСТВИЯ, переключение исполняется ВТОРОЙ сессией месяца. Это
конвенция норматива, проверенная на 9 554 сессиях, а не дефект.
(2) SHORT_SESSIONS пуста — закрыть нечем: расписание сокращённых сессий даёт биржа,
выдумывать его в коде запрещено. Заполнится при получении таблицы CME.
(3) 30-й №2 (ib_insync.reqAccountSummary) отклонён по ИСХОДНИКУ библиотеки 0.9.86.
(4) 30-й №10 — ПРЕДЕЛ, вынесенный заказчику: проверка пропущенного торгового дня стоит ДО
step(), поэтому живое навёрстывание пропущенного ролла недостижимо по построению; снятие
отказа означало бы торговлю после пропущенного дня и отмену О-5. Выбор между двумя нормами
делает заказчик, а не код.

ПЯТЬ ДОЛГОВ ОТКРЫТЫ — ЗНАЯ ИХ, ИЩИ ДРУГОЕ:
28-й №1 сверка маршрута остаётся FAIL-OPEN (mr_engine.configure пинует путь журнала и
конфликтует с конфигурацией переходного исполнителя в том же процессе).
28-й №3 отчёты дня не сопоставляются с заявками намерения: intent хранит пары
«инструмент-количество», todays_executions отдаёт только permId.
28-й №4 разведение корня и полного имени не доведено до сквозного пути (load_registry).
28-й №7 день перехода не имеет штатного пути через автопилот.
30-й №17 исполнения ПЕРЕХОДА не попадают в §7: интерфейс брокера перехода возвращает номер
заявки и количество, без цены и комиссии. Ежедневный контур это УЖЕ пишет (31-й круг, №11);
переходный — нет, и заглушка была бы ложным доказательством.

ЖИВОГО ПОДТВЕРЖДЕНИЯ НЕТ: торговля на бумажном счёте идёт, но переход Ф<->Е ни разу не
исполнялся вживую; всё вокруг исполнителя перехода проверено ТОЛЬКО стендами. Оболочка
автопилота стендами не исполняется (признанный предел): её правки проверяются грепом,
bash -n и поведением вызываемого Python. Замер маржи обеих серий — 17.08, 08:00 Чикаго;
с 20.08 его отсутствие роняет выпуск (ворота смотрят на машинную пару). Подписка на
реальные котировки не куплена сознательно: на бумажном счёте исполнения СИМУЛИРОВАНЫ.

СДЕЛАНО ПОСЛЕ 31-го (проверяй ПОЛНОТУ этих правок в первую очередь — половина замечаний
прошлых кругов была про неполноту предыдущих):
(а) §8 «сокращение до L=1 В ТУ ЖЕ СЕССИЮ» доведён до внутридневной вахты: заведён
session.py --o3e (do_o3e_cut) — только маршрут Е, только сегодняшняя отторгованная и ещё не
замкнутая книга, только сокращение, только внутри окна; вахта автопилота зовёт его и ставит
тревогу в ЛЮБОМ исходе. Прежде вахта писала ALARM, который сам же запрещал сокращение.
(б) тревога после состоявшегося среза О-3-Е больше не зависит от повторного замера
(удачный срез поднимает запас выше порога — тревоги не возникало вовсе).
(в) срез О-3-Е идёт через намерение и restore_to, а его исполнения пишутся в §7
(o3e_journal), ИТОГ пересчитывается.
(г) живая полоса unit_ref: единица MES считалась как у ES (вдесятеро выше истины) — правильный
план Е->Ф отвергался бы; полосы разведены по классам (фонд 3%, нога А 5%, нога Б широкая как
признанный предел).
(д) digest ЖИВОГО сигнального ряда обязателен: прежде удаление .sha256 выключало сверку
целиком. На машине файла НЕ БЫЛО — ряд заверен после сверки с тремя бэкапами.
(е) серия имени сверяется и на границе заявки: три проверки личности собраны в
contracts.identity_bad, её зовут и адаптер, и сборщик.
(ж) знак комиссии среза капа исправлен (cut_cost_delta): срез растущей цели ВОЗВРАЩАЕТ
комиссию, а не списывает вторую.
(з) содержимое журнала МР заверяется digest в конфигурации развёртывания и попадает в
WORM-якорь: прежде пин сторожил только путь и inode, и одна валидная строка CSV разрешала
переход маршрута.
(и) предполёт передачи берёт согласованный снимок (_snapshot_pair), метки незавершённой
передачи ставятся ДО журнального OPEN и снимаются при чистом отказе, сбои _atomic и
EMERGENCY_OVERRIDE идут через fail().
(к) автопилот: отказ маркера traded-* — тревога во всех ветках, «день отторгован» имеет
второй источник (дата книги), тревоги пишутся в namespace $ST/ALARM-*, diagnose видит
autopilot.STOP.
(л) ворота машинной пары маржи спрашивают transition._live_margins, а не только имена серий;
стенд успешного снимка WORM сверяет ЗНАЧЕНИЕ sha256 архива с байтами архива; фикстуры resume
в самопроверке пишут состояние перехода С digest (без него выпуск был невозможен).

СОСТОЯНИЕ ДОКАЗАТЕЛЬНОЙ МАШИНЕРИИ: батарея 186 утверждений, мутаций 121 — все ловятся, ни
одной «НЕ ПОЙМАНА», базлайны всех групп чисты; эквивалентность живого контура замороженному
движку 0.00e+00 при допуске 1e-12; contour_test пройден.

ИЩИ ШЕСТЬ КЛАССОВ: (1) проверка не на своём месте относительно необратимого шага;
(2) проверка на месте, но НЕДОСТИЖИМА (перехвачена более ранней веткой); (3) стенд
закрепляет дефект как норму; (4) исправлена ОДНА точка вызова из нескольких;
(5) УСПЕШНЫЙ путь не исполняется никем — стенд проверяет только отказ, и поломка
производственной ветки невидима; (6) утверждение с ненулевым покрытием, которое НЕ
НАБЛЮДАЕТ свою защиту: перебор не достаёт до состояния, где защита срабатывает, и таблица
покрытия печатает зелёное за счёт постороннего условия.
"""
BRIEF = BRIEF_TPL.replace("{KRUG_IM_UP}", KRUG_IM.upper())


if __name__ == '__main__':
    ROOT = Path(__file__).resolve().parent.parent
    items, sk = R.collect([ROOT / 'r33build/live', ROOT / 'r33build/transition.py',
                           ROOT / 'r33build/mr_engine.py', ROOT / 'r33build/sim_v13.py',
                           ROOT / 'r33build/sim_v164.py', ROOT / 'tools/release.py',
                           ROOT / 'r33build/selfcheck_v192.py',
                           ROOT / 'tools/autopilot.sh'])
    txt = BRIEF + '\n' + ''.join(f'\n===== ФАЙЛ: {n} =====\n{t}\n' for n, t in items)
    print(f'к отправке: файлов {len(items)}, знаков {len(txt):,}; пропущено {sk}')
    out, sec = R.ask(R._client(), txt, f'рецензия {KRUG_ROD} круга')
    # ИМЯ ФАЙЛА — ИЗ НОМЕРА КРУГА, И ЗАТИРАТЬ ЧУЖОЕ ЗАПРЕЩЕНО (15.08). Хардкод 'krug22'
    # пережил обновление брифа, и рецензия 23-го круга ЗАТЁРЛА текст 22-го — файл не был
    # под git, спасла только копия для выгрузки. Номер берётся из KRUG_N, а существующий
    # файл не перезаписывается молча.
    dst = ROOT / f'docs/kritika-krug{KRUG_N}.md'
    if dst.exists():
        raise SystemExit(f'{dst} уже существует — рецензия {KRUG_N}-го круга затёрла бы '
                         f'чужой текст; переименуйте старый файл или поправьте KRUG_N')
    # ЗАГОЛОВОК — ТОЖЕ ИЗ KRUG_ROD (16.08): имя файла давно бралось из номера, а заголовок
    # внутри остался хардкодом «двадцать второго» и врал в файлах кругов 23-30.
    dst.write_text(f'# Рецензия {KRUG_ROD} круга ред. 33 ({R.MODEL}, pro+max)\n\n{out}\n',
                   encoding='utf-8')
    print(f'\nзаписано: {dst} ({len(out):,} знаков) за {sec} с')
