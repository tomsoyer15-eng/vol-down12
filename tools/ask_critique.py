#!/usr/bin/env python3
"""Отправка методики и выводов на внешнюю критику (OpenAI API).

Отправляется ОПИСАНИЕ ИЗМЕРЕНИЙ И ЧИСЛА, без маршрутной логики и исполнительского контура.
Задача внешней модели — искать ошибки метода и необоснованные переходы, а не хвалить.
"""
import json, sys, os
from pathlib import Path
from urllib.request import Request, urlopen

ROOT = Path(__file__).resolve().parent.parent
KEY = (Path.home() / '.openai-key').read_text().strip()
MODEL = sys.argv[1] if len(sys.argv) > 1 else 'gpt-5-pro'

BRIEF = r"""
Ты — придирчивый рецензент количественного исследования. Твоя задача НЕ хвалить, а найти
ошибки метода, необоснованные переходы от данных к выводам и пропущенные проверки. Отвечай
по-русски, конкретно, со ссылкой на конкретный пункт. Если находишь ошибку — покажи, чем
именно она грозит и как её проверить. Если пункт корректен, не пиши об этом.

КОНТЕКСТ. Торговая стратегия на фьючерсах: две ноги — E-mini S&P 500 (ES) и 10-летняя
казначейская нота (ZN). Сигнал: месячное закрытие выше средней из 12 месячных закрытий →
нога включена на следующий месяц, исполнение в следующую сессию. Включённая нога = 1.0 x
капитал; суммарный номинал ограничен 2.00 x капитал. Полоса 3% (сделка при отклонении
экспозиции ноги от цели больше 3% капитала). Издержки 5 б.п. на изменение экспозиции,
роллы 1 б.п., спред 0.5 п.п. годовых вычитается из дохода каждой ноги. Бэктест 1963-2026
идёт на СИНТЕТИКЕ: нога А — индекс полной доходности США со склейкой на SPY, нога Б —
реконструкция доходности 10-летней бумаги со склейкой на ETF IEF. Опубликованные цифры:
CAGR 11.41%, макс. просадка -33.68%.

Появились фактические биржевые данные (Norgate): ES с 09.1997, ZN с 05.1982, дневные
OHLC + объём + открытый интерес, отдельные контракты и непрерывные ряды (сырые и
обратно-скорректированные). Ниже — что я измерил и какие сделал выводы. Ищи ошибки.

--- ИЗМЕРЕНИЕ 1: дюрация контракта ZN ---
Метод: регрессия дневных логарифмических изменений цены переднего контракта ZN на дневное
изменение 7-летней бескупонной доходности (кривая ФРС, Gurkaynak-Sack-Wright). Дни смены
контракта исключены. Передний контракт определялся тремя способами: ближайший срок,
максимальный открытый интерес, и их собственный обратно-скорректированный ряд.
Результат: подразумеваемая дюрация 5.82 / 5.71 / 5.87 соответственно, R2 = 0.93, n = 713
(на двухлетнем окне). На полной истории с 1982: дюрация устойчива в диапазоне 5.5-6.7,
тогда как дюрация 10-летней ПАРНОЙ бумаги (D_ref, считается по формуле от доходности)
меняется от 5.33 до 9.73. Отношение k = D_подраз/D_ref: 0.856 при доходностях выше
условного купона контракта (6%, до 2000 года 8%) и 0.731 ниже него. В спецификации
зафиксировано k = 0.88.
Вывод, который я сделал: опцион на выбор дешевейшей к поставке СТАБИЛИЗИРУЕТ дюрацию
контракта, поэтому формула, делающая DV01 пропорциональным D_ref, моделирует зависимость
от уровня ставок, которой у контракта нет.

--- ИЗМЕРЕНИЕ 2: корзинный расчёт дешевейшей к поставке ---
Метод: список нот с остаточным сроком 6.5-10 лет на первый день месяца поставки
(TreasuryDirect), конверсионные факторы по формуле CME, цены и дюрации бумаг посчитаны
по бескупонной кривой ФРС, дешевейшая определена по минимуму отношения цены к
конверсионному фактору. Ставки репо не учитывались.
Результат: дешевейшая — 7-летняя нота, DV01 контракта $65.88 против $64.69 из регрессии
(измерение 1) и $79.28 по формуле спецификации.

--- ИЗМЕРЕНИЕ 3: спред и издержки ---
Оценка эффективного спреда методом Корвина-Шульца по дневным максимумам/минимумам дала
для ES 22 б.п. при реальном порядке 0.3 б.п. Я объявил метод неприменимым к
круглосуточным фьючерсам и отказался от него.
Чувствительность результата (полное окно): издержки 5→10 б.п. дают -0.19 п.п. CAGR,
5→20 б.п. -0.56, ролл 1→10 б.п. -0.61, спред 0.5→1.0 п.п. -0.85, спред 0.5→0 п.п. +0.85.

--- ИЗМЕРЕНИЕ 4: синтетика против фактических фьючерсов ---
Метод: у обратно-скорректированного непрерывного ряда ход цены есть P&L непрерывно
роллируемой позиции, то есть уже избыточный доход над денежной ставкой. Поэтому
фактический доход ноги = множитель x d(скорректированная цена) / (знаменатель), где для
ноги А знаменатель = 50 x СЫРАЯ цена ES, для ноги Б = контрактный эквивалент модели
(112000 x 0.88, доллары на контракт). Модель для сравнения даёт (доход ряда - ставка).
При подстановке фактической ноги спред 0.5 п.п. с неё снимается (иначе двойной счёт).
Результаты: нога А (1997-2026) корреляция дневных ходов с моделью 0.9914, наклон
регрессии 0.991, отношение годовой качки 0.999, накопленное отставание факта 0.61 п.п.
годовых. Нога Б (1982-2026) корреляция 0.9407, наклон 0.894, качка 0.950.
Подстановка в стратегию: 1997-2026 обе ноги фактические дают CAGR 10.92% против модельных
10.63% (+0.29). 1982-1997 нога Б фактическая: 19.96% против 20.66% (-0.70). 1982-2026
нога Б фактическая: 13.99% против 14.01% (-0.02).
Вывод, который я сделал: за 44 года суммарное расхождение синтетики с биржей равно нулю,
реконструкция ноги Б точна; отклонения по подпериодам объясняются тем, что модель держит
цену контракта постоянной (112000), тогда как фактическая цена ZN менялась от 61 до 140.

--- ИЗМЕРЕНИЕ 5: перенос на непокрытый период 1963-1982 ---
Фьючерсов тогда не существовало. Я перенёс измеренное расхождение назад двумя способами:
по худшей декаде (-1.85 п.п. на уровне ноги Б, декада 1982-1989) и по среднему (-0.6 обе
ноги). Полное окно 1963-2026: модель 11.43%, жёсткий перенос 11.04%, мягкий 11.34%,
просадка -32.8% во всех вариантах против -32.9%. Итог сформулирован диапазоном 11.0-11.4%
со смещением к верхней границе; жёсткий перенос назван необоснованным на том основании,
что за 44 года суммарное расхождение нулевое.

--- ИЗМЕРЕНИЕ 6: роллы и ликвидность ---
Переезд ликвидности определялся как первый день, когда открытый интерес дальнего контракта
превысил ближний два дня подряд. 176 роллов ZN за 1982-2026. Медиана расхождения с
правилом спецификации: -1 сессия в 1982-1989, -2 в 1990-х, -1 в 2000-х, +1 в 2010-х,
+3 в 2020-х. Доля дневного объёма, остающаяся в торгуемом контракте в день ролла, падает
с 67% до 43%. Вывод: правило не было ошибочным, оно разъехалось с рынком в последнее
десятилетие. Книга 26 ES + 101 ZN на 10 млн составляет 3.27% дневного объёма ZN в 1982,
0.92% в 1985, 0.006% в 2024.

ЗАДАНИЕ. Найди в этом: (а) ошибки метода; (б) выводы, которые данные не поддерживают;
(в) пропущенные контрольные проверки, которые могли бы опровергнуть мои выводы;
(г) места, где я мог обмануться совпадением. Будь конкретен и приоритизируй по важности.
"""

if __name__ == '__main__':
    body = {'model': MODEL,
            'input': BRIEF,
            'reasoning': {'effort': 'high'}}
    req = Request('https://api.openai.com/v1/responses',
                  data=json.dumps(body).encode(),
                  headers={'Authorization': f'Bearer {KEY}',
                           'Content-Type': 'application/json'})
    import time, re as _re
    d = None
    for attempt in range(1):
        try:
            with urlopen(req, timeout=3600) as f:
                d = json.load(f)
            break
        except Exception as e:
            detail = getattr(e, 'read', lambda: b'')().decode('utf-8', 'replace')
            if '429' in str(e) or 'rate_limit' in detail:
                m = _re.search(r'try again in ([\d.]+)s', detail)
                w = max(float(m.group(1)) + 20, 70) if m else 70
                print(f'лимит, попытка {attempt+1}, жду {w:.0f} с', flush=True)
                time.sleep(w); continue
            print('ОШИБКА:', e, detail[:800]); raise SystemExit(1)
    if d is None:
        print('не удалось после 12 попыток'); raise SystemExit(1)
    out = []
    for item in d.get('output', []):
        for c in item.get('content', []) or []:
            if c.get('type') in ('output_text', 'text'):
                out.append(c.get('text', ''))
    st = d.get('status'); inc = (d.get('incomplete_details') or {}).get('reason')
    if st != 'completed': print(f'[статус {st}, причина {inc}]', flush=True)
    txt = '\n'.join(out).strip() or json.dumps(d)[:2000]
    (ROOT / 'docs/kritika-vneshnyaya.md').write_text(
        f'# Внешняя критика методики ({MODEL})\n\n' + txt + '\n')
    print(txt)
