# PROJECT-STATE — ADD-FUT v1.6.0 ред. 33 (бумажный пилот)

Обновлено: 19.08.2026 (см. §4а — ИНЦИДЕНТ 19.08: контур простоял ночь из-за собственной
проверки, восстановлен; 44-й круг разобран наполовину, см. §6). Ранее — 17.08 (круги 31-43 разобраны ЦЕЛИКОМ: 18, 18, 14, 14, 11, 12, 18, 11, 13, 6, 11, 10, 12
замечаний — 168 подряд, все действительны, все исправлены, отклонённых нет. С 17.08 введена
САМОРЕЦЕНЗИЯ ПЕРЕД ВНЕШНЕЙ В ДВА ПРОХОДА — правило 8а CLAUDE.md, см. §6). Документ
самодостаточен: новая сессия должна понять положение дел
без доступа к переписке. Нормативный источник истины — `docs/addfut-v1.6.0-r33.txt`
(PDF — рендер того же текста); история всех решений и инцидентов — §12 спецификации.

## 1. Постановка задачи

Стратегия ADD-FUT: две ноги — А (акции, ES/MES, сетка в MES, 10 MES = 1 ES, единица
расчёта 500 × SPY) и Б (десятилетние трежерис, ZN, модельная единица 112 000 × CTD_RATIO
0,88 × D_fix). Сигнал §1 — МЕСЯЧНЫЙ: закрытие месяца против 12-месячной SMA; решение
закрытием месяца M действует в месяце M+1 (строка ряда помечена месяцем ДЕЙСТВИЯ, а
исполняется переключение ВТОРОЙ сессией месяца — так делает замороженный движок через
strict_states). Полоса выравнивания 10% капитала, кап суммарного плеча 2,00 по плечу
ФАКТИЧЕСКОГО закрытия предыдущей сессии, ролл обеих ног за 3 сессии до последнего рабочего
дня фев/мая/авг/ноя (праздники CME сдвигают: ноябрь-2026 — 24-е, не 25-е). Маршрут Ф —
фьючерсы; маршрут Е — UCITS-фонды CSPX и CBU0 (биржевой тикер CSBGU0, EBS) с маржинальным
займом; выбор маршрута — движок МР v7.7, переход в Е только по акту заказчика.

Текущий этап: БУМАЖНЫЙ ПИЛОТ на счёте IBKR paper `DUR833748` (NLV ~1 млн). Порог §8
(3 млн) на бумажном этапе явно исключён и журналируется. План: ежедневные сессии на Ф →
первый живой ролл 26.08.2026 → переход Ф→Е → торговля на Е.

## 2. Ключевые решения заказчика (не пересматривать без него)

- Полоса 3% → 10% (принята по рискосопоставимому сравнению); сдвиг ролла — отказ.
- Порог счёта 3 млн; мандатный хвост −58,5% признан; базовая валюта USD.
- Пилот в два этапа: paper полный размер → live 10%. Маршруты последовательно: Ф → переход → Е.
- Дивидендный шлейф 0,20% → 0,25%; статус порога блока-2 PASS → REVIEW.
- Параметры АПРИОРИ, подгонка запрещена. Отвергнутые надстройки копятся в §9а (включая
  фильтр асимметрии пробоев).
- Внешние рецензии гонять ДО НУЛЯ ЗАМЕЧАНИЙ (решение 13.08.2026). С 13.08.2026 режим
  ПО УМОЛЧАНИЮ — максимум: gpt-5.6-sol + mode=pro + effort=max (зашито в openai_review.py).
- «Всё, что можешь сам, — делай сам»; Сергей (сотрудник) больше не нужен ни для чего.

## 3. Архитектура (все пути от корня проекта)

```
r33build/ADD-FUT-v1_6_0-r33.txt   нормативный текст (копия docs/), .pdf — рендер
r33build/sim_v13.py, sim_v164.py  ЗАМОРОЖЕННЫЕ движки (регрессия 1e-12 — не трогать)
r33build/live/daily.py            решение §1: полоса, кап, ролл, серии; run_session
r33build/live/feed.py             входы: SPY/ES/TNX, календарь+праздники, сигнал, коды отказов
r33build/live/signal_update.py    месячный сигнал из IBKR (SPY/IEF), сверка 12 мес перекрытия
r33build/live/ib_broker.py        адаптер IBKR: permId, барьер выгрузки, счёт-пин, orderRef
r33build/live/state.py            книга, намерение (intent), замок, book_path, сверка
r33build/live/session.py          запуск: --dry / --live / --close (+ --route)
r33build/live/journal.py          журнал §7 (цепочка хэшей; якорь docs/zhurnal-yakor.txt)
r33build/transition.py            переход Ф↔Е, лимит непарной дельты 1% (сумма модулей)
r33build/mr_engine.py             движок выбора маршрута, нормативный журнал МР
r33build/live/invariants.py       ВСЯ доказательная машинерия (см. §5)
r33build/live/mutation.py         мутации всех уровней + обязательные чистые базлайны
r33build/live/ib_stub.py          подстановка IBKR (повадки: cancelled_but_filled и т.д.)
r33build/live/autopilot.sh        КАНОНИЧЕСКИЙ диспетчер (tools/autopilot.sh — симлинк)
r33build/selfcheck_v192.py        самопроверка пакета + снимок машинного состояния
tools/release.py                  ЕДИНСТВЕННЫЙ путь выпуска: манифест→zip→распаковка→selfcheck
tools/openai_review.py            вызовы рецензента (Responses API, защита от двойной оплаты)
tools/review_new_code.py          задание текущего круга рецензии
tools/spec_drift.py               обзор нестационарности спецификаций (SP/ES/MES, тики, календарь)
docs/kritika-krug{5..9}.md        рецензии; docs/r33-sha256.txt — корень доверия
```

Машинное состояние (НЕ в репозитории): `~/.addfut/` — book-F.json (книга), signals_live.csv
(живой ряд сигналов), journal-F.csv (§7), route.txt, autopilot.log, отметки traded-/closed-,
ALARM-*.txt (тревога = полная остановка), ibgw.env (логин/пароль шлюза, 600), zoneinfo/.

## 4. Живой статус на 19.08.2026

- Книга: 26 единиц ноги А (2 ES + 6 MES) / 0 ZN, серия U26, сессия №5 замкнута 18.08
  (NLV закрытия 992 796,48, плечо закрытия 1,0049 — триггер капа сегодняшней сессии №6).
  Сигнал августа (строка 2026-08-31) = А:1, Б:0, поэтому нога Б закрыта 13-14.08.
  Предпросмотр 19.08: заявок нет, книга остаётся 26/0.
- Ролл U26→Z26 — 26.08, через неделю.
- Автопилот: системный cron `*/10 * * * * flock … tools/autopilot.sh tick`; торговое окно
  08:45–15:30 Чикаго, замыкание после 16:05; праздники — daily.holidays_for (2026–2028);
  месячное обновление сигнала перед первой торговлей месяца; единственный штатный отказ —
  «день уже отторгован», всё прочее → ALARM-файл и стоп до ручного разбора.
- Шлюз: IB Gateway 10.45 + IBC, порт 4002 (paper), поднимается автопилотом; рукопожатие
  API проверяется, три отказа подряд → ALARM. Маржи фактические: ES 34 910 / MES 3 491 /
  ZN 2 157 (нач.), поддерживающие 25 059 / 2 506 / 1 876.
- Пакет: `paket-ADD-FUT-v1_6_0-r33.zip` — актуальный SHA-256 всегда в docs/r33-sha256.txt,
  датированная история выпусков — releases/INDEX.txt; распакованный архив проходит
  selfcheck целиком, release сам сверяет хэш КАЖДОГО файла до запуска проверяющего.
- Копии: git (3+ коммитов) → GitHub `tomsoyer15-eng/vol-down12` (private, пуш работает) и
  локальное зеркало `~/git-mirrors/vol-down12.git`; бэкапы состояния `~/.addfut-backups/`
  (+ `~/state-mirror/`). Автовыгрузка — tools/backup_push.sh после каждого замыкания.
- **Google Drive, папка `vol-down12-backups`.** До 16.08 туда попадали только ПЕРЕПЕЧАТКИ
  через сессию помощника: текст проходил через модель, а не через файловую систему, поэтому
  копия не была побайтовой и доказательством служить не могла. Поставлен rclone 1.75.0
  (`~/bin/rclone`), backup_push.sh умеет побайтовую выгрузку с `--checksum`. Состав
  (утверждён заказчиком 16.08): корень доверия, releases/INDEX.txt, PROJECT-STATE.md,
  нормативный текст, все `docs/kritika-krug*.md`, датированные `paket-r33-*.zip`, сжатые
  бэкапы состояния счёта и bundle всего репозитория.
  РЕПОЗИТОРИЙ ВЫГРУЖАЕТСЯ ОДНИМ BUNDLE, а не пофайловой копией bare-каталога: обход по
  файлам даёт несогласованный снимок (паки и loose-объекты меняются во время копирования),
  bundle снимается атомарно и сам себя проверяет `git bundle verify`. Разворачивается
  обычным `git clone <файл>` — проверено, 39 коммитов, HEAD совпал.
  **ВКЛЮЧЕНО 17.08.2026: rclone авторизован в Google, удалёнка `vol-down12-drive`.**
  Браузера на машине нет (`DISPLAY` пуст, из иксов только Xvfb :97 под шлюз), поэтому
  авторизация сделана пробросом порта: в Termius на iPad правило Local Forwarding
  53682 → 127.0.0.1:53682 на этот хост, затем `rclone config` здесь и вход в Google из
  Safari на iPad. ПОВТОРИТЬ ТАК ЖЕ, если токен когда-нибудь протухнет. Первая полная
  выгрузка сверена: 29 рецензий, 17 архивов выпусков, корень доверия, нормативный текст,
  bundle — 0 расхождений SHA-256 между источником и копией. Имя удалёнки переопределяется
  `ADDFUT_DRIVE_REMOTE`. Если удалёнка отвалится, backup_push.sh пишет в журнал
  `drive ПРОПУЩЕН: ...` — молчать при неснятой копии запрещено (класс дефекта route.txt, §7).
  СРОК ГОДНОСТИ, ЗАПИСАН 17.08: rclone на КАЖДОЙ выгрузке предупреждает, что общий
  client_id Google выводится из обращения «в течение 2026 года». Значит эта копия однажды
  отвалится сама — не аварией, а тихо, по чужому расписанию. Лечение известно и не срочно:
  завести собственный client_id в Google Cloud Console и прописать в `rclone config`.
  Пока НЕ СДЕЛАНО; при отказе журнал выгрузки будет громким, поэтому незамеченным не пройдёт.

- **ЗАЩИТА ОТ УДАЛЕНИЯ — НЕ ПРАВИЛО, А ЗАСТАВА (17.08.2026).** Правило 11 CLAUDE.md
  запрещало массовые удаления, но исполнялось только вниманием помощника, а именно оно и
  отказывает. Теперь запрет исполняется машиной: `.claude/settings.json` вешает хук
  PreToolUse на каждую команду Bash, а `tools/guard_destructive.sh` ОТКАЗЫВАЕТ в запуске
  (`rm -r`/`rm -f`, любые удаления в `~/.addfut*`, `~/state-mirror`, `~/git-mirrors`,
  `releases/`, `anchors/`, `git reset --hard`, `git clean`, `git checkout --`,
  `git restore`, `find -delete`, перенос каталогов проекта). Отказ происходит ДО запуска
  команды, а не после. Осознанное исключение — маркер `# РАЗРЕШЕНО-ЗАКАЗЧИКОМ` в самой
  строке; `/tmp` выведен из-под заставы. ПРОВЕРЕНО ЖИВЬЁМ: собственный `rm -rf` по каталогу
  проекта был отклонён заставой. Именно этот класс (ошибочный `git checkout` соседней
  сессии, откативший docs/) уже происходил здесь 16.08.

## 4а. ИНЦИДЕНТ 19.08.2026 — контур остановлен своей же проверкой (разобран, §12)

Замыкание 18.08 прошло, но снимок состояния отказал: `anchors_without_archive` получала
каталог копий СТРОКОЙ из argv и падала TypeError на `bdir / имя`; внешний `except Exception`
переводил ошибку кода в вердикт «считать недоказанной», snap отказывал, closed-* не
ставился, автопилот стоял. Отказ был бы вечным. Дефект жил с кругов 36-42 НЕДОСТИЖИМЫМ:
проверка смотрит только на якоря со строкой «sha256 архива», а первый такой якорь создан
замыканием 18.08 в 00:05 — он и взвёл мину. Третий случай семьи «молчит на сегодняшнем
состоянии, отказывает на будущем».

Причина отказа была НАПЕЧАТАНА И СТЁРТА: общая тревога run_close писала в тот же файл через
`>`. Теперь дописывает (alarm_keep), и появился первый стенд шеллового слоя вообще.

Восстановлено 19.08 в 02:28 Чикаго: копия addfut-2026-08-18-022836.tgz, якорь
worm-2026-08-18.txt в HEAD, отметка closed-2026-08-18 стоит, тревога переименована в
.resolved-19.08. Попутно найдены и починены два дефекта, каждый остановил бы контур:
правка 44-го круга №6 ссылалась на несуществующий `J` и запрещала ЛЮБОЙ штатный повтор дня
(та же подмена: ошибка кода в доменном вердикте), и батарея была КРАСНОЙ ещё до правок —
SAME_API падал на аварийном выходе Е→Ф, потому что подтверждение разгрузки (44-й круг, №2)
требует машинного пина, которого нет в изолированном каталоге стендов.

## 5. Доказательная машинерия (числа на 17.08.2026, после 36-го круга)

Батарея зелёная, мутаций 130 — все ловятся, ни одной «НЕ ПОЙМАНА», базлайны всех групп
чисты. Эквивалентность живого контура замороженному движку 0.00e+00 при допуске 1e-12
(replay_check), contour_test — все проверки пройдены.

НОВЫЕ ЗАЩИТЫ 31-го КРУГА И ИХ ПАРНЫЕ МУТАЦИИ: единица MES в живом unit_ref (полоса MES не
равна полосе ES); серия имени на границе заявки (contracts.identity_bad — одна точка вызова
для адаптера и сборщика); digest ЖИВОГО сигнального ряда обязателен; знак комиссии среза
капа (cut_cost_delta); тревога после состоявшегося среза О-3-Е; строки среза в журнале §7
(o3e_journal); содержимое журнала МР (digest в конфигурации развёртывания).

НОВЫЕ ЗАЩИТЫ КРУГОВ 32-36: совпадение позиций (не только даты) при коротком пути намерения;
пер-ножный срок ролла при РАСХОДЯЩИХСЯ сериях (leg_roll_due) — при совпадающих сериях
поведение бит в бит, иначе рвётся эквивалентность; живой брокер перехода в IBBroker
(preview/sell_units/buy_units/minutes_since/gross) и расширенный на него SAME_API; снимок
счёта непосредственно перед первой заявкой перехода; долговечные тревоги (fsync файла и
каталога, запасное имя, STOP) во ВСЕХ ветках перехода и О-3-Е; тревога пишется ДО
сохранения состояния; согласованный Decision после пост-трейдового среза (расходы и запас
пересчитываются, не остаются от досрезной книги); WORM-архив публикуется под рабочим именем
только ПОСЛЕ git-заверения (до этого `.tgz.pending`); чистый отказ ворот окна отделён от
неизвестного исхода подачи.

## 5а. Доказательная машинерия — как устроена

Уровни (каждое утверждение обязано иметь НЕнулевое покрытие; вакуумность = провал):
решение 34 560 × 17; сессии при сбоях 94 × 9; адаптер 19 × 20; намерение 5 × 7; сборщик
11 × 11; запуск 24 × 32; переход 23 × 23; ролл-последовательности 8 × 9 (вкл. смешанную
книгу и пер-ножный откат); обновлятор сигналов 15 × 15 (уровни, границы, бутстрап,
котировочный срез); сверка §7 5 × 5; упаковка и ворота капа 2 × 3 (точечные стенды PACK —
перебор до нарушенной упаковки и до границы капа не достаёт). Мутаций 110 — все ловятся;
у КАЖДОГО мутационного прогона обязательный чистый базлайн (грязный = провал). Эквивалентность
живого контура замороженному движку: NAV и КНИГИ на 15 964 сессиях (двух арифметик на общих
входах; живой стык доказывается стендами). Selfcheck в конце сверяет рекурсивный lstat-снимок
настоящего ~/.addfut — стенды не смеют трогать машинное состояние.

## 6. Внешние рецензии

**ПОРЯДОК С 17.08.2026 (решение заказчика): САМОРЕЦЕНЗИЯ ПЕРЕД ВНЕШНЕЙ, В ДВА ПРОХОДА.**
Разобрав круг и починив все замечания, помощник обязан сначала отрецензировать сам себя — тем
же враждебным взглядом и по тому же списку классов, — найденное исправить, затем пройти
ВТОРОЙ раз по правкам первого прохода, и только ПОСЛЕ этого отправлять пакет наружу
(правило 8а CLAUDE.md). Второй проход проверяет ДОКАЗАТЕЛЬСТВА: каждый зонд обязан сперва
показать, что достигает проверяемой ветки. Повод — 39-й круг, №10: правку «STOP запрещает
замыкание» я подтвердил зондом, который вставлял STOP ПОСЛЕ ветки, замыкающей день и
делающей return; ветка была недостижима, а прогон зелёный.
ПЯТЬ УГЛОВ — ОБЯЗАТЕЛЬНАЯ ПРОЦЕДУРА ПЕРЕД КАЖДОЙ ОТПРАВКОЙ (решение заказчика 18.08, полный
текст — правило 8а CLAUDE.md): (1) ОТ СЕМЬИ дефекта, механически грепом по классу, а не по
памяти; (2) ОТ ПРОТИВОПОЛОЖНОГО ЗНАКА — что закрыло ужесточение и что открыло послабление;
(3) ОТ ЖИВОГО СОСТОЯНИЯ и реального календаря, а не от фикстур; (4) ОТ ЧУЖОЙ ПРАВКИ — не
ломает ли одна правка допущение другой; (5) ОТ ОТРИЦАНИЯ — какую мутацию новый стенд НЕ
поймает. Внутри углов остаются три вопроса 40-го круга: траектория, а не снимок; правило, а не
пример; зонд достижимости на каждый стенд.
ПРОЦЕДУРА СЕБЯ ОПРАВДАЛА В ПЕРВЫЙ ЖЕ РАЗ: угол «от семьи» нашёл третье место класса «порядок
по изменяемой величине» (уборка отметок traded-/closed- по -mtime), которого не назвал ни
рецензент, ни помощник.
Разницу с внешним рецензентом это сужает, но не снимает: он читает код как текст и
спрашивает, что текст ДОПУСКАЕТ, а я смотрю своим же замыслом. Причина в числах: в 37-м круге шесть замечаний из
восемнадцати пришлись на код, написанный накануне, в 38-м — девять из одиннадцати. Рецензент
раз за разом тратился на то, что можно было найти самому, а каждый круг стоит денег и часов.

**Состояние на 19.08.2026: 44 круга отправлено, 43 разобрано целиком, 44-й — НАПОЛОВИНУ.**
44-й круг дал 14 замечаний, 6 ложных доказательств и отдельный разбор шести непокрытых
методов IBBroker (`docs/kritika-krug44.md`). ЗАКРЫТО: P0 №1 (нога А считалась по ES, а не по
SPY), №2 (ложное «все приращения неположительны» — теперь разгрузка подтверждается измеренной
маржой), №3 (серия цели перехода против календаря ролла), №4 (resume предпросматривал весь
план вместо остатка), P1 №5 (отложенный ролл без строки ИТОГ), №6 (неполная сессия отличается
от штатного повтора — доделано 19.08, см. §4а); ложные доказательства №1, №3, №4.
ЗАКРЫТА И ВТОРАЯ ПОЛОВИНА 19.08: №7 (переход в ранее работавший маршрут не писал итог сессии —
тот же отказ якоря, что остановил контур), №8 (календарь CME навязывался фондам), №9 (замок
книги переведён с файла на КАТАЛОГ: сверка имени и inode задачу не решала, и это показал
собственный зонд), №10 (монотонные часы пары), №11 (лимит 390 считается по счёту за день),
№12 (возраст сердцебиения — только по содержимому, запись атомарна), №13 (обязательность
заверенного берётся из истории якорей), №14 (фильтр ответов IBKR снят в ЧЕТЫРЁХ местах,
причина отказа предпросмотра называется, диагнозы §8 и О-3-Е разведены); ложные доказательства
№2, №5, №6. Из шести непокрытых методов IBBroker закрыты два (mark_pair, minutes_since).

**Прежнее состояние на 18.08.2026: 43 круга отправлено и 43 разобрано ЦЕЛИКОМ. Круги 31-43 дали
18, 18, 14, 14, 11, 12, 18, 11, 13, 6, 11, 10, 12 замечаний — 168 подряд, ВСЕ действительны и ВСЕ исправлены,
отклонённых нет. В 38-м круге ДЕВЯТЬ замечаний из одиннадцати пришлись на код, написанный
накануне в 37-м; отсюда правило 8а (саморецензия перед внешней), и оно сразу окупилось —
саморецензия нашла в моих же правках 38-го круга ДВЕ регрессии, каждая опаснее исходного
замечания (голый raise уводил разорванную книгу мимо restore_to; жёсткий отказ предпросмотра
заблокировал бы аварийный выход Е→Ф, который маржу освобождает). Drive РАБОТАЕТ (rclone авторизован 17.08 через проброс порта из Termius;
сверка 29 рецензий и 17 архивов — 0 расхождений хэшей). СЛЕДУЮЩИЙ ШАГ — отправить
38-й круг и разбирать по коду до нуля.**

ТРИДЦАТЬ СЕДЬМОЙ КРУГ, ЧТО ЗНАТЬ НОВОЙ СЕССИИ (полный разбор — §12): ШЕСТЬ замечаний из
восемнадцати — по живому брокеру перехода, написанному НАКАНУНЕ в 36-м круге. Код, созданный
ради закрытия замечания, не имеет презумпции правильности: он моложе всех и не проверен
ничем, кроме собственного намерения. Главное по деньгам: gross() считал плечо по СЕРЕДИНЕ
проверочной полосы (для ZN занижение примерно на треть при CLOSE_CAP=2,00 — запаса на такую
ошибку нет); unit_ref не опознавал фонды, потому что живой реестр отдаёт их классом 'STK';
лимит 15 минут не работал никогда (часы заводились ПОСЛЕ пары); preview обещал whatIf и не
вызывал его. Всё это SAME_API не видел, потому что проверял ПРИСУТСТВИЕ метода и число
аргументов, а не поведение, — теперь он оба брокера ВЫЗЫВАЕТ.

РЯД ЗАМЕЧАНИЙ ПО КРУГАМ (1-43): 20, 16, 17, 22, 27, 28, 14, 18, 28, 12, 10, 8, 10, 10, 7,
6, 16, 21, 22, 22, 20, 24, 26, 29, 19, 22, 23, 18, 18, 20, 18, 18, 14, 14, 11, 12, 18, 11, 13, 6, 11, 10, 12.
Эпоха
medium (1-16) сошла к 6 — это оказался ЛОЖНЫЙ ПОЛ: смена на pro+max (с 17-го) сразу дала
16, 21, 22 и держала плато ~20 двадцать кругов. Спад 33-36 (14, 14, 11, 12) выглядел
устойчивым, но 37-й вернулся к 18 — и это не откат: шесть замечаний из восемнадцати
относятся к коду, написанному в 36-м круге. Плато держится не потому, что дефекты не
кончаются, а потому что КАЖДЫЙ круг правок производит новый код (22-й: +720 строк; 36-й:
целый живой брокер перехода), а новый код рецензируется наравне со старым. Пока правки
крупные, ряд не сойдётся к нулю — сходиться он начнёт, когда правки станут мелкими.

ЗАМЕР МАРЖИ ПОЛУЧЕН 18.08, ДОЛГ 37-го КРУГА №15 ЗАКРЫТ. margins_live.json несёт ОБЕ серии
(ESU26 34 786/24 903, ESZ26 35 050/25 116, MESU26 3 479/2 490, MESZ26 3 506/2 512,
ZNU26 2 155/1 874, ZNZ26 2 149/1 869 — начальная/поддерживающая), _meta с con_ids и счётом
DUR833748; transition._live_margins() замер принимает и сворачивает по корням КОНСЕРВАТИВНО
(максимум из серий: ES 35 050, MES 3 506, ZN 2 155).

ГИПОТЕЗА О ВРЕМЕНИ СУТОК БЫЛА НЕВЕРНА — НЕ ИДТИ ПО ЭТОМУ СЛЕДУ. Прежняя запись советовала
«повторять замер около 03:45 Чикаго», опираясь на одно совпадение: удачный замер 13.08 в
03:46 против неудачных в 06:00/06:05. Четвёртая неудача 18.08 в 03:52 — в том же часе —
гипотезу похоронила. ФАКТИЧЕСКАЯ ПРИЧИНА: whatIfOrder отдаёт ПУСТОЙ СПИСОК вместо OrderState,
когда TIF заявки не задан явно, потому что пресет счёта переопределяет TIF (предупреждение
шлюза 10349). С `tif` явно — ответ приходит в любой час. Разгадка лежала В ТОМ ЖЕ ФАЙЛЕ, на
166 строк ниже: place() уже ставил TIF явно и объяснял в комментарии, почему.

ЭТО ЖЕ КАСАЛОСЬ ПЕРЕХОДА, И ЭТО ВАЖНЕЕ САМОГО ЗАМЕРА. IBBroker.preview() звал whatIfOrder БЕЗ
явного TIF, то есть на боевом шлюзе ВСЕГДА получал пусто и ВСЕГДА возвращал «отложить»: три
подряд — инцидент, и законный переход уходил бы в ABORT. Вся работа кругов 37-43 над
предпросмотром в бою не сработала бы ни разу; стенды этого не показывали, потому что стаб
отвечает всегда. Исправлено 18.08; заявка предпросмотра приведена к форме настоящей заявки
place() (GTC+outsideRth), стаб научен отдавать пустой список при заявке без TIF, заведена
парная мутация. Планировать переход на утренние часы Чикаго БОЛЬШЕ НЕ ТРЕБУЕТСЯ.

СОРОК ЧЕТВЁРТЫЙ КРУГ НЕ ОТПРАВЛЕН: КОНЧИЛИСЬ КРЕДИТЫ OpenAI (18.08.2026, 12:05 UTC).
Пакет к отправке готов и собран (30 файлов, 1 431 687 знаков), BRIEF перестроен под 44-й круг,
запрос ушёл и получил response_id resp_0393305e8ff3..., но через 5 секунд вернулся
status=failed с кодом `credit_balance_exhausted`: «You have no credits remaining».
Файл docs/kritika-krug44.md НЕ создан, чужой текст не затёрт, ничего не потеряно.
ЧТО НУЖНО ОТ ЗАКАЗЧИКА: пополнить баланс на platform.openai.com/settings/organization/billing.
После пополнения отправка повторяется одной командой: `.venv/bin/python tools/review_new_code.py`
(номер круга уже стоит 44, повторная отправка ничего не перезапишет — скрипт отказывается
затирать существующий файл рецензии).
СОСТОЯНИЕ КОДА НА МОМЕНТ ОСТАНОВКИ: выпуск 67e3f024 состоялся, все проверки распакованного
архива пройдены, батарея 206/0, адаптерные мутации без непойманных, коммиты ed96088 и 3f95f40
выгружены в зеркало и на GitHub.

РЕШЕНИЕ ЗАКАЗЧИКА 18.08.2026: СОРОК ПЯТЫЙ КРУГ — БЕЗ ДОБРОВОЛЬНЫХ ДОБАВЛЕНИЙ.
Повод: счёт замечаний вырос до 20 (после 12 в 43-м), и встал вопрос, не потому ли, что BRIEF
указывает рецензенту направления и вскрывает новые пласты. Разбор происхождения 20 замечаний:
4 — прямые ответы на мои вопросы, 6 — проверка МОИХ утверждений из раздела «уже проверено
мною», 10 — совсем не наведённые (включая P0 про ES вместо SPY, замок по inode, лимит 390).
Свои девять находок рецензент НЕ повторил — то есть проверка «наведение перевесило» из
правила 8а не сработала в плохую сторону. Вторая причина роста честнее: дифф был велик, и
6 из 20 замечаний — про код, написанный в тот же день.
ОТСЮДА ОПЫТ 45-го КРУГА: дифф строго «правки найденных дефектов + ТОЛЬКО те парные мутации и
утверждения, которых требует правило 4 ДЛЯ ЭТИХ правок». Ничего по своей инициативе: ни новых
стендов из моих углов, ни закрытия шести непокрытых методов, ни упреждающего усиления. Мои
собственные находки записываются в §12 как ОТЛОЖЕННЫЕ. Цель — отделить «новый код рождает
замечания» от «рецензент находит давнее», и получить сравнимый счёт.

ВЫПУСК НЕЛЬЗЯ ЗАПУСКАТЬ ТАК, ЧТОБЫ ОН ПЕРЕСЁК ТОРГОВОЕ ОКНО (найдено 18.08 углом «от живого
состояния»). selfcheck снимает lstat-снимок настоящего ~/.addfut при импорте и сверяет его в
конце, а прогон длится около часа. Из снимка исключены только ДИСПЕТЧЕРСКИЕ файлы
(autopilot.log, tick-heartbeat, autopilot.lock, push-fails) — книга, журнал §7, намерение,
реестры, тревоги и отметки traded-/closed-/sigup- остаются в нём СТРОГО и намеренно
(27-й круг, №21). Торговая сессия в 08:45 Чикаго и замыкание после 16:00 пишут именно их.
Значит выпуск, начатый менее чем за час до окна, упадёт на ЗАКОННОЙ записи живого контура и
будет выглядеть дефектом кода. Тот же класс уже случался с тиками cron — потому диспетчерские
файлы и исключили. ПРАВИЛО: запускать выпуск сразу ПОСЛЕ того, как сессия отработала и книга
сохранена, либо в окно между сессией и замыканием.

ОТКРЫТЫЙ РИСК ЦЕПИ ВОССТАНОВЛЕНИЯ (замечено 18.08 при выгрузке). rclone предупреждает на
КАЖДОЙ операции: копия на Google Drive работает через ОБЩИЙ client_id rclone, который Google
выводит из строя в течение 2026 года. Когда это случится, выгрузка `vol-down12-drive:` начнёт
отказывать — и звено «побайтовая копия вне машины» из правила 11 тихо выпадет, оставив
git-историю, локальное зеркало и releases/. Отказ будет виден в ~/.addfut/autopilot.log
строкой «drive ОШИБКА выгрузки», но НИЧЕГО не остановит: выгрузка по построению не роняет
контур. Что делать: завести собственный client_id (rclone.org/drive/#making-your-own-client-id)
ДО того, как отключат общий. Пока не сделано.

ЖИВАЯ АВАРИЯ ШЛЮЗА 16-17.08 И ЕЁ РАЗБОР. Шлюз простоял с 02:45 16.08 на модальном окне
«Unrecognized Username or Password» — порт 4002 не слушал ~27 часов. Тревоги не было и быть
не могло: ensure_gw зовётся только на торговых путях, а 15-16.08 — выходные. Пароль ни при
чём: после остановки зависшего экземпляра шлюз поднялся и залогинился теми же учётными
данными, API отдал счёт DUR833748. Причина — воскресное окно обслуживания IBKR, поймавшее
принудительный релогин. ЧТО ИЗ ЭТОГО СЛЕДУЕТ: живость шлюза не наблюдается ничем до первого
торгового тика понедельника; если бы это случилось в день ролла, ensure_gw начал бы попытки
логина уже внутри окна. Стоит проверять шлюз ДО открытия окна, а не в нём.

БЛИЖАЙШЕЕ ПО ЧАСАМ: замер сделан 18.08, ворота выпуска его принимают — пункт закрыт.
Ниже сохранена история требования, потому что она объясняет, ЗАЧЕМ ворота устроены так.
ПОДТВЕРЖДЕНО РЕЦЕНЗЕНТОМ НЕЗАВИСИМО (37-й круг, №15), на состоянии ДО 18.08: в приложенном
`live/margins_live.json` только U26, нет ESZ26/MESZ26/ZNZ26 и нет `_meta.con_ids`, а живой
реестр несёт обе серии — `_live_margins()` откажет сначала по неполному покрытию, затем по
отсутствию поколения. То есть успешный путь перехода на ПРИЛОЖЕННОМ состоянии недостижим:
переходные стенды используют свою полную временную фикстуру, поэтому зелёный выпуск этого
не ловит. Замер — не формальность, а условие исполнимости перехода (с 20.08 отсутствие полной машинной пары роняет выпуск). ВАЖНО: с 32-го круга замер
пишет con_id серий в _meta и сверяется с текущим реестром, а ворота выпуска спрашивают
transition._live_margins на МАШИННЫХ реестре, счёте и замере — старый файл (только U26, без
con_ids) ворота не пройдёт, нужен именно свежий полный замер.

КРУГИ 33-36, ЧТО ОБЯЗАНА ЗНАТЬ НОВАЯ СЕССИЯ (полный разбор — §12 спецификации).

1. САМОЕ ДОРОГОЕ — 36-й №1: КАЛЕНДАРНЫЙ РОЛЛ РОЛЛИЛ НОГУ, ЧЕЙ СРОК НЕ НАСТУПИЛ. Признак дня
   ролла применялся к ОБЕИМ ногам, хотя срок — свойство серии. Смешанная книга (А в Z26, Б в
   U26) в августовский день отправляла ногу А в H27 — квартал вперёд, лишний оборот, риск
   одноногого разрыва. Первый живой ролл 26.08, то есть это не теория. Стенд _i17 ТРЕБОВАЛ
   роллить все ненулевые ноги и закреплял дефект как норму. ВАЖНО ДЛЯ ПРАВОК: первая
   редакция правки сломала эквивалентность (2,85e-03 и 5,10e-02 при 1e-12) — в реплее Market
   не несёт праздничной таблицы, срок серии считался без сдвигов, ролл пропадал целиком.
   Уточнение сужено до РАСХОДЯЩИХСЯ серий; при совпадающих — бит в бит.
2. 36-й №6: В ПАКЕТЕ НЕ БЫЛО ЖИВОГО БРОКЕРА ПЕРЕХОДА. Исполнитель зовёт preview, sell_units,
   buy_units, minutes_since, gross — у IBBroker не было НИ ОДНОГО. Первый штатный переход
   упал бы на broker.preview() ещё до OPEN: ни плановый Ф→Е, ни аварийный выход Е→Ф текущим
   комплектом были невыполнимы. Написано и включено в SAME_API; ЖИВОГО ПОДТВЕРЖДЕНИЯ НЕТ.
3. КЛАСС, КОТОРЫЙ ТЕПЕРЬ ИЩЕТ РЕЦЕНЗЕНТ, — РАСХОЖДЕНИЕ МЕЖДУ ОБЪЯВЛЕННЫМ И СДЕЛАННЫМ (три
   замечания 35-го круга разом): сверка отчётов перехода 34-го круга оказалась логически
   ИНВЕРТИРОВАНА (доказанное отсутствие исполнений само запрещало чистый ABORT) и сравнивала
   ПОДСТРОКОЙ; «тревога до состояния» была вставлена только в одну из двух веток; «все
   записи ALARM переведены на alarm_write» было ложью — оставались три голые, включая сторож
   замка, который после неудачной записи всё равно завершался нулём. Лекарство одно и
   механическое: ГРЕП ПО ФАКТУ после КАЖДОЙ правки, а не память о ней.
4. 36-й №5 отменил правку 35-го: «точное совпадение номеров» сравнивает РАЗНЫЕ пространства
   идентификаторов (состояние хранит orderId, todays_executions отдаёт permId) — равенство
   невозможно в принципе. Теперь читается как «сопоставить нечем» (не ABORT), со ссылкой на
   открытый долг 28-го круга №3.

ТРИДЦАТЬ ВТОРОЙ КРУГ — ЗЕРКАЛО ТРИДЦАТЬ ПЕРВОГО: семь замечаний из восемнадцати по
`do_o3e_cut`, то есть по коду, написанному накануне. Новая живая ветка сама стала денежным
путём: срез мог оставить ОДНОНОГУЮ книгу (нет restore_to, ворота окна вне try), шёл мимо
ворот paper/live, его итоговая строка журнала ДЕТЕРМИНИРОВАННО ломала следующую сессию
(run_session требует префикс «итог сессии»), ИТОГ писался раньше сохранения книги, а
предторговый срез step_e вообще не оставлял следа для тревоги. Плюс: замер маржи не был
привязан к поколению реестра (имя серии переживает исправление con_id), у MaintMarginReq
проверялся только NaN (inf и отрицательное молча запускали ликвидацию), вахта писала «книга
СОКРАЩЕНА» по коду возврата 0, старое развёртывание журнала МР оставалось fail-open,
`session.py --live` не проверял торговый день и начало окна.

МОИ ДВА ПРОМАХА КРУГА (оба поймала батарея в тот же прогон) — ОДНОГО КЛАССА, КОТОРЫЙ КРУГ И
ИЩЕТ: новая проверка перехватывала старую и делала её недостижимой. Ворота дня/окна на входе
отвечали раньше ворот перед заявкой (стенд «окно ушло за время сессии» перестал проверять
своё — часы стенда сделаны ИДУЩИМИ); сверка поколения реестра стояла раньше проверки
покрытия серий («замер прежней серии» стал отвечать про con_id). Третий промах поймал
мутационный прогон: новая защита №13 (ярлык намерения по позициям, а не по дате) не имела
наблюдающего стенда — мутация «НЕ ПОЙМАНА»; заведён стенд обрыва посреди среза, а раннер
группы намерения научен патчить названный модуль, а не только `_resume_intent`.

ПЕРВЫЙ ВЫПУСК 31-го КРУГА НЕ СОСТОЯЛСЯ, И ЭТО ПРАВИЛЬНО: новая защита содержимого журнала
МР (№13) поймала фикстуры самопроверки, которые писали журнал руками мимо append_event.
Фикстуры провизионированы (M.test_configure), второй заход прошёл целиком.

ТРИДЦАТЬ ПЕРВЫЙ КРУГ, ЧТО ВАЖНО ЗНАТЬ НОВОЙ СЕССИИ (полный разбор — §12 спецификации).
(1) №16 БЫЛ БЫ СТОПОРОМ ВЫПУСКА: фикстуры resume в самопроверке писали состояние перехода
без digest, который _execute_locked отвергает первым делом (правка 30-го круга) — прогон
на дереве ДО правок падает исключением ровно там. То есть выпуск после 30-го круга был
невозможен, и это нашла рецензия, а не выпуск.
(2) ЖИВОЕ СОСТОЯНИЕ, ВТОРОЙ РАЗ ЗА 31 КРУГ: у `~/.addfut/signals_live.csv` НЕ БЫЛО файла
`.sha256`, а sigup-2026-08 стоит с 14.08 — весь август контур торговал по сигнальному ряду
без защиты целостности (№7). Ряд заверен вручную после сверки с тремя датированными
бэкапами (побайтово совпал); читатель теперь требует digest у ЖИВОГО ряда.
(3) НОРМАТИВ §8 БЫЛ ВЫПОЛНЕН НАПОЛОВИНУ (№1): run_session сокращает книгу после исполнений
(30-й круг), а внутридневная вахта автопилота только писала ALARM — и им же запрещала
запуск, способный сократить. Заведён `session.py --o3e`; вахта зовёт его внутри окна и
ставит тревогу в любом исходе.
(4) МОИ ОШИБКИ КРУГА, ОБЕ ПОЙМАНЫ ПРОГОНАМИ: заверение журнала МР считалось по сырым
байтам, а сверялось с текстом (CRLF против LF) — буквально класс №11 тридцатого круга,
повторённый в соседнем файле; и вынесенная функция `o3e_journal` обращалась к алиасу `J`,
который в daily.py существует только внутри run_session (NameError в четырёх утверждениях).
(5) НОРМАТИВНЫЙ ТЕКСТ В ПАКЕТЕ ОТСТАВАЛ: `r33build/ADD-FUT-v1_6_0-r33.txt` не содержал
записей §12 кругов 23-30 (они были только в `docs/`), то есть последний выпуск ушёл со
спецификацией без них. Копия синхронизирована; синхронизация — часть выпуска.
(6) ПАРАЛЛЕЛЬНАЯ СЕССИЯ: 16.08 около 23:00 в этом же репозитории работала ВТОРАЯ живая
сессия Claude (от разбора 30-го круга). Её фоновое задание запустило release.py по
незаконченному дереву и дважды откатило правки PROJECT-STATE.md и docs/. Процесс
остановлен, тексты восстановлены. Перед работой проверять `pgrep -af release.py` и чужие
сессии: две сессии на одном дереве — это потерянные записи.

ТРИДЦАТЫЙ КРУГ, ЧТО ВАЖНО ЗНАТЬ НОВОЙ СЕССИИ. Одна поломка была ЖИВОЙ и моей: №11 —
worm_anchor.snap() сверял не то тело (в файл уходил body_full, на сверку шёл body),
хэши не совпали бы никогда, и первое же замыкание пометило бы архив .rejected, подняло
ALARM и остановило автопилот. Внесено правкой 29-го круга 16.08, последнее замыкание было
14.08 — выстрелило бы в понедельник. №1 — норматив §8 (О-3-Е, сокращение до L=1 В ТУ ЖЕ
СЕССИЮ) НЕ выполнялся: в 29-м круге я закрыл замечание правкой ТЕКСТА тревоги вместо
поведения. Теперь run_session сокращает книгу в ту же сессию.

ЧЕТЫРЕ РАЗА ЗА КРУГ ПРОВЕРКИ ПОЙМАЛИ ИСПОЛНИТЕЛЯ, А НЕ РЕЦЕНЗЕНТА: правка №8 списала
несписываемый избыток перекладки в NAV (replay: «ЕСТЬ РАСХОЖДЕНИЯ»); конструктор фикстур
завёл книгу без журнала (поймал выпуск); мутация №3 била в пустоту — раннер патчил не тот
модуль; проверку мутации №1 прогнал не тем раннером и получил ложное «НЕ ПОЙМАНА».

Счёт замечаний по кругам: 20, 16, 17, 22, 27, 28, 14, 18, 28, 12, 10, 8, 10, 10, 7, 6,
16, 21, 22, 22, 20, 24, 26, 29, 19, 22, 23, 18, 18, 20, 18, 18, 14, 14, 11, 12. Разобраны
целиком круги 1-26 и 30-36; 27-й — 20 из 23, 28-й — 15 из 18, 29-й — 16 из 18. Разбор — §12.

ДВАДЦАТЬ ДЕВЯТЫЙ КРУГ (16.08): закрыты №3 (цены плана перехода сверяются с независимой
рыночной полосой — брокер без unit_ref к переходу не допускается), №5 (свежая доходность
получила приоритет над d_fix старой книги Ф), №6 (замер избытка перекладки считал оборот
ролла — ложная тревога каждый роллный день; исправлен, плюс ворота капа по капиталу за
вычетом избытка), №8 (метки передачи перенесены за ворота окна). Батарея выросла
167 -> 175, мутаций 103 -> 110, эквивалентность 1e-12 сохранена (0.00e+00).

УРОК КРУГА (мой): первые две новые защиты №6 снимались БЕССЛЕДНО. Утверждения держались
на 52 и 5 688 состояниях — и не доказывали ничего: перебор не достаёт ни до нарушенной
упаковки (вся сетка в MES при es_held=0 бывает только после передачи книги из Е), ни до
границы капа. Ненулевое покрытие НЕ равно наблюдаемости защиты. Заведены точечные стенды
PACK и своя группа мутаций run_pack_mutations; обе мутации ловятся.

(история) ДЕВЯТНАДЦАТЬ кругов разобраны ЦЕЛИКОМ: 20, 16, 17, 22, 27, 28, 14, 18, 28, 12, 10, 8,
10, 10, 7, 6, 16, 21, 22. Сводки — в §12; полные тексты — docs/kritika-krug*.md. 17-й — ПЕРВЫЙ
на pro+max (27 мин против 4 у medium): рост числа замечаний — смена глубины, вскрыты
пласты вне поля medium (resume перехода, полнота журнала §7, деньги О-3-Е). Главное:
прогресс внутри прерванного лота и остаток непарной дельты переживают resume (плюс
вскрытый стендом «or 1» продавал долю сверх лимита — отказ ДО заявки); разбор намерения
опирается на БАРЬЕР исполнений, а не на снимок позиций; неизвестность компенсаций не
глотается; ворота Е у автопилота динамические (DST); NaN-запас О-3-Е = отказ; полнота
замера маржи — по каждой серии реестра; лимит заявок — прогоном плана; каждый месяц SMA
вне сайдкара — под TRADES; окно перепроверяется после подготовки; сторож замка; журнал
§7 — verify до заявок + итоговая строка; цель О-3-Е — от капитала после расходов.
Мутаций 103. ПРАВИЛО: гонять до НУЛЯ. Вызов: `.venv/bin/python tools/review_new_code.py` (перед этим обновить
BRIEF под текущий круг!). С 13.08.2026 умолчание — МАКСИМУМ (mode=pro, effort=max, зашито
в openai_review.py; решение заказчика после 15-го круга; 16-й — последний в medium).
Ключ — `~/.openai-key`; модель gpt-5.6-sol (чистой 5.6 нет; luna/sol/terra). Перед сменой
параметров — дешёвый пробный вызов. max_retries=0 и реестр джобов защищают от двойной оплаты.

## 6а. 20-й и 21-й круги РАЗОБРАНЫ ЦЕЛИКОМ 14.08 (docs/kritika-krug20.md, krug21.md)

Ряд 16-21-22-22-20. ДВАДЦАТЫЙ: 22 замечания, 21 исправлено, №15 ОТКЛОНЕНО —
ib_insync.IB.reqAccountSummary существует в 0.9.86 (сигнатура (self), блокирующий,
ждёт accountSummaryEnd); класс дефекта («стенды доказывают стаб, а не живой класс»)
признан и закрыт сверкой интерфейса с ОБОИХ концов. ДВАДЦАТЬ ПЕРВЫЙ отправлен ДОСРОЧНО,
до выпуска — и это себя оправдало: большинство его замечаний про неполноту правок
двадцатого, они попали в тот же выпуск. 20 замечаний, 19 исправлено, №4 отклонено с
измерением (списание оборота перекладки ломает эквивалентность: 1,17e-05 при допуске
1e-12; стоимость измеряется по факту в журнале §7).

САМОЕ ЦЕННОЕ ИЗ 21-го: contour_test.py ПАДАЛ с KeyError, а его гоняет выпускной барьер —
выпуск в этот день был бы невозможен. Причина: правка 20-го круга (дата без ИТОГ
исключается) сделала фикстуру теста нереалистичной. Урок: invariants.py — НЕ весь барьер;
selfcheck гоняет ещё mutation, replay_check и contour_test, и «зелёная батарея» их не
покрывает. После каждого класса правок гнать батарею + contour_test + replay_check
(полный selfcheck работает только внутри выпуска — он сверяет манифест).

СЕМЬ ЗАЩИТ ВЫНЕСЕНЫ В ФУНКЦИИ (pair_tol, comp_fill_ok, resume_same_session, carry_pending,
check_one_series, reject_archive, keep_pack_on_refusal, repack_cost): пока защита сидит
условием внутри процедуры, у неё не может быть парной мутации.

ЖИВЫЕ ИНЦИДЕНТЫ ДНЯ. (а) Шлюз не поднял API после суточного перезапуска; автопилот
остановился тревогой штатно, но путь восстановления был БЕССИЛЕН — start.sh всегда
поднимает свой Xvfb на :97, а тот работал с 12.08. Разобрано вручную, книга сверена
(ESU6 2 + MESU6 6 = 26/0, NLV 1 007 659,70). (б) Пограничная проверка сигнала блокировала
бы контур ВЕСЬ месяц: она стояла на последнем месяце решения безусловно, в том числе на
уже записанном и замороженном. Обе дали правки кода.

## 6б. 22-й круг РАЗОБРАН 15.08 (24/24; полный разбор — §12)

Ключевое: asof перестал быть аргументом доверия (сверка с биржевым сегодня); исполнитель
не торгует поверх MIXED; сделка в отмене аварийного пути = MIXED, не ABORT; сверка
капитала fail-closed; пин замера маржи обязателен; вахта О-3-Е каждым тиком (маршрут Е);
тревога не запирает замыкание отторгованного дня; исполненное сокращение = состоявшаяся
сессия; d_fix=0 при живом ZN — отказ передачи; любой MIXED ставит ALARM; start.sh
переиспользует живой Xvfb (починен инцидент 14.08); registry_horizon за 10 дней до ролла
(ноябрьский H27 ловится с 16.11); MarketE несёт европейский календарь (Пасха проверена);
2028 работоспособен; OPEN после ворот окна; потеря журнала §7 не рождает GENESIS (переход
ЯВНО начинает журнал нового маршрута якорной строкой); ориентир и журнал обязательны при
живых заявках; unpaired_usd — сумма модулей; px_order — котировка МОМЕНТА заявки
(prev-close только запасной); мутационный распаковщик принимает оба вида кортежа; даты
стендов разведены по смыслам (тариф 08.08 / переход 10.08 / разбор 11.08); сигнатуры
подмен совместимы; primary_exchange во всех фикстурах, чужая листинговая линия ловится.
ОТКЛОНЕНО №20б: начисление дневного дохода — конвенция замороженного движка (правка
сломала бы 1e-12); живое расхождение меряет §7 котировочным ориентиром.

РЕШЕНИЕ ЗАКАЗЧИКА 15.08: гнать ДО НУЛЯ, сколько бы кругов ни потребовалось; рост кода
этому не помеха («хоть сто кругов, пока не вылежим программу до идеала»). Предложение
исполнителя не наращивать код и ограничиться минимальными правками ОТКЛОНЕНО. Ряд pro+max
держит плато ~20 из-за самопроизводства (22-й круг: +720 строк, −141), но это цена работы,
а не основание её сокращать.

ИНЦИДЕНТ .venv (15.08, мой): симлинк для репетиций лёг при слиянии поверх настоящего
каталога и убил окружение; восстановлено с сети (версии сверены), .venv в .gitignore.
Cron сдвинут на 5,15,...,55 (решение заказчика): тик попадает точно в 08:45/16:05/10:35.

## (история) 18-й круг РАЗОБРАН ЦЕЛИКОМ 14.08 (см. §12), выпуск b4121f4c, коммит d22a430.

## 6б. (история) исходный план разбора 18-го круга

Ряд: …7, 6, 16, 21 (второй круг pro+max — вскрывает автопилот-шелл, метрику §7, WORM).
СДЕЛАНО НЕМЕДЛЕННО (ночь 14.08, в рабочем каталоге, ещё НЕ выпущено): №9 — Gateway
больше не наследует fd 9 замка (exec 9>&- при запуске start.sh; иначе первый же рестарт
шлюза снова делал cron декоративным); №10 — неведомый код календаря = ALARM-calendar-
broken, а не молчаливый «выходной». ОСТАЛЬНЫЕ 19 — разбирать по списку, каждое по коду:
P0: №1 пост-трейд повтор margin_cushion на Е + None при пустой книге ≠ прокси; №2 отмена
по (clientId, orderId)/permId, не по голому orderId; №3 барьер open_orders перед первым
ордером свежего перехода; №4 preview ПОСЛЕ отмен resume; №5 recover-путь и финальные
сверки — под fail()-оболочку; №6 restore_to: после первой неизвестной компенсации СТОП
цикла; №7 first_connect: сверка tag_from_expiry(фактического expiry)==tag и symbol==root
(круговое заверение); №8 ролл: пары close+open по ногам (диры дыры), хотя бы
чередованием ног. P1: №11 признать/поправить формулировку «переносится» (intent+ALARM
остаются — честно «до ручного снятия»); №12 runtime-счётчик 390 в _run_lots + запас в
preflight; №13 маржа: init prior maint, конечность/положительность, davность/серии/счёт
_meta строго, whatIf-нелинейность признать пределом; №14 закрыт частично (замер Z26 —
перед переходом, ворота уже держат); №15 reconcile §7 обязан ЧИТАТЬ пометки исключения
сессий (и строки ИТОГ фильтровать по назначению); №16 ролловая стоимость: делить на
ОДНОСТОРОННИЙ номинал переноса; №17 px_order: писать ориентир ЧЕСТНО как prev-close
(переименовать/докум.) либо снимать котировку, FeedError не глотать molча; №18 WORM:
state.load вместо сырого digest, пути из ADDFUT_*, git-результат проверять, атомарная
запись+fsync; №19 backup_state: tar ПОД hold_book_lock (согласованный снимок); №20
replay_check: сравнивать серии/es_held/roll_pending/E-позиции; №21 манифест: пин к
git-вершине (записывать commit в манифест и сверять при выпуске), признать предел
«автопилот-шелл не исполняется стендами» или сделать bats-прогон. После разбора: полная
батарея → release → §12 → commit/push → Drive → 19-й круг (pro+max) — до НУЛЯ.

## 7. Инциденты и уроки (повторять нельзя)

### 16.08.2026 — ЖИВОЙ РЯД СИГНАЛОВ БЕЗ DIGEST (31-й круг, №7)

Файла `~/.addfut/signals_live.csv.sha256` не существовало, а отметка `sigup-2026-08` стоит
с 14.08: весь август живой контур читал сигнальный ряд БЕЗ проверки целостности. Сверка
digest в `feed.signal_state` включалась только при существующем непустом сайдкаре — то есть
удаление файла её выключало целиком (fail-open того же класса, что молчаливое умолчание
route.txt). Один бит ряда открывает или закрывает целую ногу порядка NLV.

Правка 30-го круга (№9) заверяет ряд на ОБЕИХ ветках signal_update, но следующий его
запуск — только в сентябре, перед первой торговлей месяца. Разбор (О-5, вручную): ряд
побайтово совпал со всеми тремя датированными бэкапами состояния (13.08 11:10, 13.08 21:10,
14.08 21:10) — независимое подтверждение, что его не правили; после этого ряд заверен
`signal_update._ensure_digest`. Читатель теперь ТРЕБУЕТ digest у живого ряда: отсутствие —
отказ торговли, а не разрешение.

УРОК ТОТ ЖЕ, ЧТО У route.txt, И ЭТО ВТОРОЙ СЛУЧАЙ ЗА 31 КРУГ: защита, включающаяся только
при наличии доказательства, отключается его удалением. «Нет доказательства» обязано
означать отказ.

### 16.08.2026 — route.txt ОТСУТСТВОВАЛ, контур работал на умолчании

Файла `~/.addfut/route.txt` не существовало. Прежний `route()` при его отсутствии МОЛЧА
возвращал F, и бумажный пилот всё время работал на умолчании, а не на объявленном
маршруте. Обнаружено собственной правкой (27-й круг, №1), сделавшей это громким; тревогу
поймал lstat-снимок машинного состояния в выпуске.

ПОЧЕМУ ОПАСНО: route.txt пишет hand_over_book. Пока переходов не было, умолчание случайно
совпадало с истиной; после первого перехода в Е потеря файла вернула бы контур к
ФЬЮЧЕРСАМ ПОВЕРХ ФОНДОВОЙ ПОЗИЦИИ. Умолчание маскировало отсутствие файла ровно до
момента, когда оно стало бы разрушительным.

РАЗБОР (О-5, вручную): истина установлена по книге — book-F.json, сессия 3, n_e=26, нога Б
выключена, book-E.json отсутствует; маршрут Ф однозначен. Создан route.txt = F. Тревоги не
удалены, а переименованы в `.resolved-16.08` (доказательство сохранено, активных
ALARM-*.txt нет).

УРОК: система принимала ОТСУТСТВИЕ ДАННЫХ за нормальное состояние. Молчаливое умолчание
опаснее отказа: оно верно ровно до того дня, когда перестаёт быть верным. Первый за 29
кругов случай, когда защита нашла дефект ЖИВОГО состояния, а не путь в коде.



1) Selfcheck затёр живую книгу (тестовый переход писал в ~/.addfut) → изоляция окружением +
   снимок машинного состояния; проверено намеренным снятием изоляции. 2) Журнал §7 «рвался»:
   None хэшировался как 'None', в файл шёл '' → хэшируется ровно то, что читается; цепочка
   пересобрана, якорь в docs/zhurnal-yakor.txt. 3) Конвенция сигнального ряда: строка =
   месяц ДЕЙСТВИЯ (доказано на 2022: 12/12) — моя читалка держала лишнюю ногу день.
   4) Правка без проверки грепом НЕ правка: «суженный» список отказов автопилота не
   применился, увидела это внешняя рецензия. 5) Статус заявки IBKR ≠ факт исполнения
   (Cancelled при состоявшейся сделке) — исполнение только по permId-отчётам; запасных
   путей нет. 6) reqAccountUpdates на paper-шлюзе виснет навсегда → accountSummary.
   7) IBKR: сроки >12 мес — только в годах ('16 M' → пустая история БЕЗ ошибки).
   8) pkill -f убивает собственную сессию — только pgrep/kill по PID. 9) У биржи фонды —
   secType 'STK', не 'ETF'. 10) Мутация, никого не убившая, чаще всего пуста сама.
   11) Прямой запуск session.py --live — ЖИВАЯ СЕССИЯ, а не проверка: замер кодов возврата
   на живом счёте исполнил продажу вне окна (13.08, исход верный, но дисциплина нарушена);
   коды возврата меряются на стабах или --dry.

## 8. Открытые вопросы и признанные пределы

- WORM-якорь журнала §7 (вне машины), HMAC/подпись состояния, namespace-изоляция selfcheck —
  признаны, не реализованы.
- Совпадение битов перекрытия сигналов не доказывает пропорциональность рядов; «два снимка
  позиций» — не барьер IBKR (гарантия — входная сверка следующей сессии).
- Непокрытые сочетания: intent поверх intent; частичное исполнение компенсационной заявки;
  ролл и переход маршрута в один день; NLV не в USD; вторая заявка частична после полной
  первой (стенд partial ломает все заявки разом).
- CTD_RATIO 0,88 нестационарен (§3); модель издержек не учитывает разлом целого ES;
  праздничная таблица 2026–2028, продление вручную по календарю CME/CBOT.
- Переходный исполнитель работает с корнями инструментов, не сериями (долг 28-го круга №4).
- Сверка маршрута остаётся FAIL-OPEN: mr_engine.configure пинует путь журнала и отвергает
  чужой — конфликт с конфигурацией переходного исполнителя (долг 28-го круга №1).
- Отчёты дня нечем сопоставить с намерением: intent хранит пары «инструмент-количество»,
  todays_executions отдаёт только permId (долг 28-го круга №3). Заглушка НЕ поставлена.
- КОНФИГУРАЦИЯ ЖУРНАЛА МР НА МАШИНЕ УКАЗЫВАЕТ НА /tmp (найдено 16.08 при разборе №13):
  `~/.add-fut/deploy.*` пинует `/tmp/tmpkcnli17g/j.csv`, которого нет, и не несёт sha256.
  Сейчас безвредно (ежедневный контур журнал МР не читает — долг 28-го №1; configure()
  отказывает громко), но ПЕРЕД переходом Ф→Е обязательно
  `python3 mr_engine.py --provision-journal <абсолютный путь>`. Побочно: lstat-снимок
  самопроверки сторожит ~/.addfut и НЕ сторожит ~/.add-fut — расширить.
- feed.SHORT_SESSIONS пуста: часы сокращённых сессий даёт биржа, выдумывать нельзя.
- Переход Ф<->Е ни разу не исполнялся вживую — проверен ТОЛЬКО стендами. С 36-го круга у
  IBBroker есть переходный интерфейс (preview/sell_units/buy_units/minutes_since/gross), и
  SAME_API следит, чтобы он не разошёлся со стабом; ЖИВОГО ПОДТВЕРЖДЕНИЯ у этих методов нет.
- Полоса unit_ref ноги Б остаётся широкой (границы по крайним дюрациям норматива): модельная
  единица несёт отношение d_fix/dref, исполнителю неизвестное. Для фонда (3%) и ноги А (5%)
  полосы сужены — там единица наблюдаема прямо (31-й круг, №5).
- Целостность журнала МР держится на digest в конфигурации развёртывания и на WORM-якоре
  (31-й круг, №13). Криптографическая подпись состояния по-прежнему признана и не сделана:
  тот, кто может править журнал, может править и конфигурацию — защита превращает
  одношаговую подделку в двухшаговую и делает её видимой снаружи, но не невозможной.
- Оболочка автопилота стендами не исполняется (признанный предел с 18-го круга): правки
  №1, №10, №14, №15 тридцать первого круга проверены грепом и `bash -n`, а их поведение —
  на уровне вызываемого ими Python (`session.py --o3e` имеет стенд «вахта сокращает уже
  отторгованную сессию»).
- Сверка ролловых издержек §7 ждёт ~20 живых ролловых сессий.
- Cron-задания сессии Claude умирают с ней; системный cron жив, но требует включённой машины.

## 9. Ближайшие шаги

### ПОДПИСКА НА ДАННЫЕ РЕАЛЬНОГО ВРЕМЕНИ — К ЖИВОМУ ЭТАПУ (решение заказчика 16.08)

ВКЛЮЧАЕТСЯ ПРИ ПЕРЕХОДЕ НА ЖИВЫЕ ДЕНЬГИ МЕЛКИМИ ЛОТАМИ, не раньше. Код готов:
ADDFUT_REALTIME_MD=1 меняет reqMarketDataType на реальное время и доводит признак до
адаптера, записи брокера и выборки §7.

ПОЧЕМУ НЕ СЕЙЧАС. На бумажном счёте исполнение СИМУЛИРОВАНО: IBKR не сводит заявки с
реальной книгой, а рисует филы по своей модели. Значит выборка §7 даже на идеальных данных
измеряла бы качество СИМУЛЯТОРА, а не наше проскальзывание. Проверка §7 по издержкам —
измерение живого этапа по своей природе, а не текущий пробел; подписка этого не меняет.
Задержанные данные (тип 3) бумажному пилоту не мешают: сигнал считается по месячным
закрытиям, базис — по закрытиям ES и SPY, запас О-3-Е приходит из сводки счёта.



### ЕДИНЫЙ КОНСТРУКТОР ЖИВОГО СОСТОЯНИЯ (решение заказчика 16.08; после нуля по рецензиям)

Фикстуры писались под КОНКРЕТНУЮ проверяемую защиту, а не как снимок живого состояния.
Пока защит было мало, это сходило с рук; теперь защиты читают состояние ЦЕЛИКОМ, и каждая
новая упирается в то, что фикстура беднее реальности по другому измерению. За одну смену
16.08 это дало шесть остановок выпуска подряд: книга с номером сессии без журнала §7; замер
маржи, штампованный МАШИННОЙ датой вместо биржевой (возраст −1 день на смене суток); каталог
без route.txt; реестр без поставочных серий; «сделка прошла» без отчётов исполнения; голые
имена инструментов вместо серий.

ЧТО СДЕЛАТЬ: один конструктор, собирающий СОГЛАСОВАННОЕ живое состояние — книга, журнал §7,
route.txt, реестр, замер маржи, отчёты исполнения, календарь — и возвращающий его целиком.
Стенды отклоняются от него ТОЧЕЧНО (одно поле, один файл), а не собирают состояние с нуля.
Тогда новая защита, читающая ещё одно измерение состояния, не роняет весь набор.

ПОЧЕМУ ПОСЛЕ НУЛЯ: это переработка проверочного слоя, а не денежного пути; делать её
одновременно с разбором кругов значит менять опору под ногами во время движения.



1) ПОРЯДОК НА 19.08: контур восстановлен (§4а), сегодня торгует штатно; 44-й круг разобран
   ЦЕЛИКОМ. Дальше — пять углов саморецензии по правкам круга, выпуск → коммит → пуш →
   45-й круг. Пуш — после КАЖДОГО коммита (урок 13.08: GitHub полдня
   отставал на пять кругов).
3) 26.08 — первый живой ролл U26→Z26 (праздничный календарь и навёрстывание готовы).
4) После ролла — переход Ф→Е исполнителем (права на EBS подтверждены пробной сделкой),
   затем торговля на Е; сквозной сценарий уже в стендах. ПЕРЕД переходом — полный замер
   маржи ОБЕИХ серий (17-й круг №8: сейчас в margins_live.json только U26; paper-whatIf
   капризен — утром отдаёт, днём пусто; ворота №8 без полного замера переход не пустят).
5) 20.08 — если рецензии ещё идут, помнить: session-cron'ы Claude самоистекают,
   системный автопилот — нет.
6) ПОСЛЕ 18-го круга — три работы по решению заказчика 13.08.2026:
   (а) файрвол: порт 4002 закрыт извне (слушает на всех интерфейсах; TrustedIPs=127.0.0.1
       держит, но поверхность лишняя), SSH сохранить обязательно;
   (б) ежедневный WORM-якорь вне машины: корень цепочки журнала §7 + digest книги —
       на Drive/GitHub при каждом замыкании (подмена root-ом видна пост-фактум);
   (в) диагност ТРЕВОГ: при постановке ALARM автоматика дописывает в файл тревоги
       «вероятные причины» по таблице сигнатур отказов (нет сегодняшнего бара + не
       праздник = вероятно внезапный выходной; расхождение множителя = смена спецификации;
       посторонняя позиция = чужая торговля на счёте...) плюс факты (время, шлюз, книга).
       ДИАГНОЗ БЕЗ САМОЛЕЧЕНИЯ: О-5 незыблем, автоматика по-прежнему ничего не чинит.

## 10. Как работать (краткая шпаргалка)

```
статус живого контура      bash tools/autopilot.sh status
предпросмотр решения       cd r33build/live && ../../.venv/bin/python session.py --dry
выпуск пакета              .venv/bin/python tools/release.py   (только так!)
все переборы               cd r33build/live && ../../.venv/bin/python invariants.py
мутации                    …/mutation.py    (базлайны обязаны быть чистыми)
рецензия                   обновить BRIEF в tools/review_new_code.py → запустить
```
Перед любой правкой замороженных движков — стоп: регрессия 1e-12 нормативна. Правки
подтверждать грепом/прогоном. Секреты — только в ~/.addfut. Стенды — только с
ADDFUT_LOCK_DIR/ADDFUT_SIGNALS/ADDFUT_BOOK_PATH на временные пути.
