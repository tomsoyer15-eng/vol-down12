# Наблюдение paper pilot — 16.09.2026 (среда)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись. Первый день
пилота на маршруте **Е**.

## Задача на сегодня

По графику (PROJECT-STATE §0а) на 16.09 особой задачи нет: ближайшие даты — **21.09**
напоминание и **до 22.09** перегенерация `first_connect`, затем **20.11** расчёт даты
ноябрьского ролла. Значит сегодня — быстрый статус плюс обязательная строка про пин
журнала МР. Scope не расширялся.

---

## 1. Пин журнала МР — БОЕВОЙ

```
cd r33build && ../.venv/bin/python -c "import mr_engine as M; print(M._read_deploy()['journal'])"
-> /home/alex/claude-projects/vol-down12/r33build/mr_journal.csv
```
Путь боевой, не временный. Расхождения нет. Хвост журнала несёт обе строки перехода:
`2026-09-15 TRANSITION_OPEN E|2026-09-04#1|26deb37d5243f30c` и
`2026-09-15 TRANSITION_COMPLETE …` с тем же идентификатором.

---

## 2. Как закрылся день перевода (15.09)

* Тик 16:45 дал **штатный отказ**: «сессия 2026-09-15 не новее последней завершённой:
  книга этого дня уже сохранена и ждёт замыкания». Это правильно — сессию №24 закрыл сам
  переход, повторная торговля в тот же день запрещена.
* Замыкание 18:35: **NLV закрытия 974 811,39**, вход `{'CSPX': (817.97, '2026-09-15'),
  'CBU0': (150.086, '2026-09-15')}`, **плечо закрытия 1,0229** при потолке 2,00.
* Снимок+WORM `addfut-2026-09-15-103530.tgz`, якорь `anchors/worm-2026-09-15.txt`
  (коммит `07c0459`), `зеркало ok / github ok / drive ok`, «замыкание 2026-09-15: ок».
* Отметки `traded-2026-09-15` (16:45) и `closed-2026-09-15` (18:36) на месте.

**Счёт:** 974 811,39 против 983 955,46 накануне — **−9 144,07 (−0,93%)** за день перевода,
из них комиссии перехода **509,14** (3,2 б.п. от оборота 1 615 530, в расчётах заложено
5 б.п.); остальное — движение рынка и переход экспозиции с фьючерсов на доли. С начала
пилота (1 000 000) — **−2,52%**.

---

## 3. Быстрый статус контура

**Маршрут:** `~/.addfut/route.txt` = **E**. `book-F.json` остался как история; работает
`book-E.json`.

**Книга** прочитана ШТАТНЫМ загрузчиком `state.load(book-E.json, daily.BookE)` — исключения
нет, значит хэш состояния сошёлся: маршрут **E**, сессия **№24**,
`n_eq` = **1219,0** долей CSPX, `n_bd` = **0**, `roll_pending` = false,
`prev_close_lev` = **1,0228701061853618**, `prev_st_eq` = true, `prev_st_bd` = false,
`last_session` = **2026-09-15**, `close_provisional` = false. Последняя известная позиция
брокера (файл прогресса перехода, `complete`) — **CSPX 1219 долей, фьючерсов нет**:
сходится с книгой. Нога Б выключена сигналом и отсутствует и там, и там. Следующая сверка
с брокером — в сегодняшней сессии днём.

**Календарь сходится:** `feed.prev_session(2026-09-16)` = **2026-09-15** = `last_session`;
общее окно на сегодня `feed.common_window(2026-09-16)` = **02:00–10:30 Чикаго**
(10:00–18:30 по машине), пуск торговой части в 16:45 по машине попадает внутрь.

**Тревоги:** активных **0** (все шесть `ALARM*` с суффиксом `.resolved-*`, свежайший
`ALARM-signal-2026-09.resolved-01.09`). `WARN-margins-*` — предупреждение, не тревога.

**Тик жив:** `tick-heartbeat` 16.09 08:55:01, возраст **147 с** при пороге почтового
сторожа 65 мин.

**Журнал §7.** `journal-E.csv` начат переходом: заголовок и одна строка
`2026-09-15 … ИТОГ … итог сессии 24: строк 0 (журнал начат переходом F->E)`, цепочка
начинается с нулевого `prev_hash` — так и задумано при передаче книги. `journal-F.csv`
закрыт строкой сессии 23 (`2026-09-14 … 980347.96`), цепочка хэшей непрерывна. Издержки
перехода (15 заявок, 509,14, оборот 1 615 530) лежат в `perehod-2026-09-15.json` и в
разборе `docs/nablyudenie/2026-09-15-perevod.md`, строками §7 они не оформлялись — журнал
маршрута Е начат уже после передачи книги.

**Сигнал:** строка месяца действия `2026-09-30` = **(1, 0)** — нога А включена, нога Б
выключена, позиция ей соответствует. Сайдкар уровней заканчивается августом
(92,74 / 767,05), поколение одно. `sigup-2026-09` на месте. Ближайший месячный переход —
вторая сессия октября.

**Маржа (маршрут Е, цифры предполёта перехода):** начальная **287 316 ≈ 29% счёта**,
поддерживающая 249 840, свободные средства 688 669, запас предполёта **3,90×**. На Ф было
9% — на Е экспозиция держится займом, это ожидаемо и записано в §0а. Запас к
поддерживающей большой.

**Замер маржи стареет штатно:** `WARN-margins-2026-09-16.txt` — «ему **29 дней**, до
предела 35 осталось **6**». Край подтверждён вчерашним исполнением `_meta_age_ok`:
**22.09 — последний годный день, 23.09 — первый день отказа**. Что встанет: **выпуск
пакета** и **переход Ф↔Е** (сегодня он уже не нужен, но в середине ноября возможен возврат
на Ф). Ежедневная торговля, замыкание и WORM-якорь от возраста замера не зависят.

**Копии и диск:** `~/.addfut-backups` и `~/state-mirror` — обе с
`addfut-2026-09-15-103530.tgz`. `wip-push` каждые полчаса без пропусков, последний срез
**16.09 08:37:05**. Диск: свободно **10 ГБ (занято 83%)**, как вчера.

**Git:** незапушенных коммитов **0**, HEAD `b18eb0b`. Незакоммиченными висят только
`docs/ADD-FUT-STATUS.pdf/.txt` — рабочий вывод статус-страницы, не состояние.

**Выпуск.** Действующий пакет — **45de3d90** от 15.09 (`releases/INDEX.txt`,
`paket-r33-20260915-173252-45de3d90.zip`, 5,35 МБ). Он вобрал правку по инциденту 15.09:
`transition.параметры_tid(capital, legs)` (r33build/transition.py:586, две точки вызова) —
капитал и ноги сохраняются в файле прогресса, идентификатор перехода восстановим без
перебора.

**H19:** новых завершённых месяцев нет (записан 1, за 2026-08) — журнал ведётся, задачи нет.

---

## 4. Расхождения документа, НЕ контура (код не трогаю)

В PROJECT-STATE §0а два места отстали от факта:

1. «ОТКРЫТАЯ КАРТОЧКА (класс F, правка НЕ сделана, ждёт решения заказчика)» про
   невосстановимый `transition_id` — правка **сделана** интерактивной сессией 15.09
   (коммиты `c660cd3`, `b18eb0b`) и вошла в выпуск 45de3d90; проверено грепом по коду.
2. «ДЕЙСТВУЮЩИЙ БАЗЛАЙН ПИЛОТА — ВЫПУСК 2c6e76ca от 02.09» — фактический базлайн
   **45de3d90** от 15.09 (промежуточные 568930d7, 2ece058c).

Это устаревший текст документа, а не дефект контура; правка документа — дело интерактивной
сессии, наблюдатель пишет только в `docs/nablyudenie/`.

**Открытым остаётся со вчера:** напоминания на 21.09 нет ни одним из двух каналов —
ждущих процессов `napominanie_v_srok.py` **0** (проверено разбором `ps` из Python),
файла `~/.addfut/napominanie-2026-09-21.txt` нет (в каталоге только `-09-11` и `-09-14`).
Завести не могу: это запись в `~/.addfut`.

---

## Письма

* **Событие 15.09 (перевод Ф→Е) уже отписано** — письмо «ПЕРЕВОД НА Е ВЫПОЛНЕН: продано
  2 ES + 6 MES, куплено 1219 долей CSPX, плечо 1,0245» ушло 15.09 в 15:34 по машине
  (проверено в почте). Второе письмо о том же событии не отправляю.
* Новых событий из закрытого списка п.3б со времени того письма нет.
* Сегодня среда — еженедельной сводки нет.
* Вердикт штатный, поэтому письма об отклонении нет. Не заведённое напоминание на 21.09 —
  то же самое отклонение, о котором письмо ушло вчера; повторять не стал.

## Вердикт

**Изменений не требуется. Paper pilot продолжается.**

День перевода закрылся штатно: сессия №24 замкнута, NLV 974 811,39, плечо закрытия 1,0229
при потолке 2,00, снимок и WORM-якорь сняты, копии разошлись по трём адресам. Книга
маршрута Е читается штатным загрузчиком и сходится с последней известной позицией брокера
(CSPX 1219, фьючерсов нет), календарь и сигнал сходятся, тревог 0, тик 147 с, журналы целы,
пин журнала МР боевой, диск и копии в норме. Дефектов классов A–F не обнаружено.

**На заказчика (не на меня):**
1. **Перегенерировать `first_connect` утром до 22.09 включительно** — иначе с 23.09 встанут
   выпуск пакета и возможный возврат Е→Ф (ежедневная торговля не встанет). Осталось 6 дней.
2. **Напоминание на 21.09 по-прежнему не заведено** ни ждущим процессом, ни файлом.
3. Обновить в PROJECT-STATE §0а базлайн (45de3d90) и статус карточки класса F (закрыта).
4. Начинается наблюдение за фактической стоимостью заёмных средств на Е — первая
   осмысленная оценка появится по выписке за несколько дней займа.
