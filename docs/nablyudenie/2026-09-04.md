# Наблюдение paper pilot — 04.09.2026 (пятница)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись.

## Задача на сегодня

По графику наблюдения (PROJECT-STATE §0а) на 04.09 назначен **перевод бумажного пилота с
маршрута Ф на маршрут Е** (акт заказчика 29.08.2026). Утренняя часть наблюдателя — ровно
одна: **отправить письмо-напоминание «сегодня перевод»** и подтвердить, что состояние
контура к переводу пригодно. Само исполнение — днём, безголовым исполнителем из crontab
(16:51), и в задачу этой сессии НЕ входит. Плюс обычный быстрый статус. Scope не расширялся.

## Факты

### 1. Письмо-напоминание отправлено
Тема: «НАПОМИНАНИЕ: сегодня 04.09 перевод пилота Ф -> Е, исполнитель в 16:51, счёт 998 477,
книга 26/0». Отправка `tools/trevoga_mail.py` вернула код 0, письмо положено в папку
«ADD-FUT». В теле — порядок дня, пять предусловий исполнителя, что письмо об исходе придёт
при любом результате, состояние счёта на утро и ближайшие шаги (отчёт 05.09, замер стоимости
займа, §12 и перевыпуск в интерактивной сессии, снятие разовой строки крона).

### 2. Готовность к переводу подтверждена по всем видимым признакам
- `~/.addfut/route.txt` = **F** — перевод ещё не выполнялся, повторного срабатывания не будет.
- Книга `book-F.json` живая: `session_no` = **17**, `last_session` = **2026-09-03**,
  `close_provisional` = **false**, `roll_pending` = false, `n_e` = **26**, `n_b` = **0**,
  `unit_is_mes` = true, `es_held` = **2**, `ser_a` = `ser_b` = **Z26**, `route` = F,
  `prev_close_lev` = **1,0066539124**.
- Фактическая позиция брокера по журналу сессии №17: `{MESZ26: 6, ESZ26: 2}` = 2×10 + 6 =
  **26 MES** = `n_e`; ноги Б нет ни в книге, ни у брокера. **Расхождений нет.**
- Активных тревог нет (см. §5), тик жив, незакрытых намерений (intent) в `~/.addfut/` нет.
- Скрипт исполнителя `tools/perevod_e_20260904.sh` и его задание
  `tools/perevod_e_20260904-prompt.md` на месте (оба от 29.08); сторожевая дата в скрипте —
  `[ "$(date +%F)" = "2026-09-04" ] || exit 0`, то есть сегодня он сработает, в другой день
  выйдет молча.

**Ограничение проверки, не дефект:** саму строку crontab «51 16 4 9 *» в этой сессии
прочитать не удалось — `crontab -l` и `/var/spool/cron/crontabs/alex` заблокированы правами
среды. Подтверждено только наличие скрипта и задания. Признаком несработавшей строки станет
отсутствие письма о переводе к вечеру; почтовый сторож ловит и ненулевой код исполнителя.

### 3. Вчерашняя сессия №17 (03.09) — обычный день без сделок
`autopilot.log` 03.09 16:45: шлюз не отвечал — автопилот его поднял («шлюз поднят, API
отвечает» через 21 с), прогрет с первой пробы. Далее: `счёт DUR833748: NAV 994 013,57;
позиции {'MESZ26': 6.0, 'ESZ26': 2.0}; сессия №17`; вход `es_close=(7743.5, '2026-09-02')`,
`spy_close=765.16`, `basis=+1.201%`, `yield_10y_pct=4.796`, `signal_date=2026-09-30`,
`signal_age_d=3`, серия Z26. Решение: «плечо 1.0007, книга **26/0**», **«заявки: нет»**,
режим ЖИВОЙ; «торговля 2026-09-03: ок».
Журнал §7: `2026-09-03 … ИТОГ … NAV 994 013,57 … итог сессии 17: строк 0` — исполнений
ноль, издержек ноль; цепочка хэшей непрерывна (`83eae6c7…` → `492bca35…`).

### 4. Замыкание 03.09, копии и WORM
«замкнуто: NLV закрытия **998 477,22**», вход {spy_close 773,17 (03.09), es_close 7 816,25,
yield_10y 4,762, dref 7,882919884314144, серия Z26}; «плечо закрытия **1.0067** — триггер
капа следующей сессии» при потолке **2,00** (запас почти двукратный). Затем «зеркало ok»,
«github ok», «drive ok», «замыкание 2026-09-03: ок (копия состояния снята)». Отметка
`closed-2026-09-03` на месте, архив `addfut-2026-09-03-160528.tgz` снят (всего 17 при
ротации до 60), якорь `anchors/worm-2026-09-03.txt` в HEAD (коммит `f03562a`).

### 5. Общий фон (быстрый статус)
- **Тревог нет**: шесть файлов `ALARM*`, все с суффиксом `.resolved-*`; последний —
  `ALARM-signal-2026-09.resolved-01.09`. Почтовый сторож новых писем не слал с 01.09 16:53.
- **Тики живы**: `tick-heartbeat` = 1788501301 → 04.09 08:55:01, возраст на момент проверки
  **2,9 минуты** при пороге сторожа 65 минут.
- **wip-push работает**: последняя запись 04.09 08:37:04 (выгрузка 2 файлов на Drive);
  `origin/master..master` и `mirror/master..master` — **0 коммитов**, незапушенного нет.
- **Диск**: свободно **9,4 ГБ (занято 84%)** против 11 ГБ (82%) сутки назад. Порога,
  останавливающего контур, нет; величину держу под наблюдением как фон, отдельной задачей
  не делаю.
- **Маржа по книге** (замер `r33build/live/margins_live.json` от 18.08): начальная
  2×35 050,37 + 6×3 506,29 = **91 138,48** = **9,1%** счёта; поддерживающая **65 302,06** =
  **6,5%**. Запас большой. (Напоминание графика: замер протухает 23.09.)
- Строка `Error 322 … Maximum number of account summary requests exceeded` в сессии №17
  снова присутствует — тот же известный безвредный шум шлюза, NAV и позиции получены,
  сессия отработала. В scope не входит, правка не предлагается.

## Письма

Событий из закрытого списка (сделки, ролл, месячный переход, замыкание месяца, смена
упаковки, восстановление, обслуживание) со времени отчёта 03.09 не было: 03.09 — обычный
день, заявок ноль. Письмо о событии не требуется. Отправлено только назначенное графиком
письмо-напоминание о сегодняшнем переводе (§1).

## Вердикт

Контур в норме: тревог нет, тик свежий, сессия №17 отработала штатно без сделок, книга 26/0
сходится с фактической позицией брокера {MESZ26: 6, ESZ26: 2}, плечо закрытия 1,0067 при
потолке 2,00, замыкание, копии и WORM за 03.09 сняты, незапушенного нет. К сегодняшнему
переводу Ф→Е контур пригоден по всем видимым признакам; письмо-напоминание отправлено.
Отклонений нет, карточка дефекта не оформляется.

**Изменений не требуется. Paper pilot продолжается.**

Ближайшие даты графика: **сегодня 16:51 — перевод на маршрут Е** (письмо об исходе придёт
при любом результате); 05.09 — отчёт о переводе и первый замер фактической стоимости заёмных
средств на Е; 08.09 — проверка, что Labor Day 07.09 пропущен корректно; 15.09 —
предупреждение о старении замера маржи (протухает 23.09); до 23.09 — перегенерация
first_connect.
