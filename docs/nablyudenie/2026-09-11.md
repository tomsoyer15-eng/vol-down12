# Наблюдение paper pilot — 11.09.2026 (пятница)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись.

## Задача на сегодня

По графику (PROJECT-STATE §0а) на 11.09 назначен **ПЕРЕВОД ПИЛОТА НА МАРШРУТ Е — третья
попытка**; утренняя роль наблюдателя — напоминание и проверка предпосылок. Плюс быстрый
статус, плюс обязательная строка про пин журнала МР. Scope не расширялся.

## 1. ЗАКРЫТО: Drive восстановлен, автопилот НЕ остановился

Вчерашнее срочное отклонение снято заказчиком **вечером 10.09**. Факты:

* последний отказ выгрузки — **10.09 19:07:01**, первый успех — **10.09 19:37:14**
  (`~/.addfut/wip-push.log`); с тех пор все получасовые срезы проходят, последний
  **11.09 08:37:05**;
* в журнале автопилота 10.09 19:23 появились `зеркало ok / github ok / **drive ok**`;
* ночное замыкание **11.09 00:05–00:06**: снимок+WORM `addfut-2026-09-10-160529.tgz`,
  якорь `worm-2026-09-10.txt`, `зеркало ok`, `github ok`, **`drive ok`** — строки
  «внешняя выгрузка не удалась» НЕТ;
* счётчик `~/.addfut/push-fails` **отсутствует** = 0 (сбрасывается удалением файла при
  успехе — `r33build/live/autopilot.sh:1393`). До порога `>=3` (ALARM-push) не дошло:
  было 2, стало 0;
* живая проба этой сессии: `~/bin/rclone lsd vol-down12-drive:` — **код 0**, каталоги
  отдаются (в т.ч. `portfolio-research-backups`).

Предсказанной вчера остановки автопилота ночью **не произошло**. Копии вне машины снова
свежие (10.09). Отклонение закрыто.

## 2. СЕГОДНЯ — ПЕРЕВОД НА МАРШРУТ Е: напоминание ушло, предпосылки на месте

**Напоминание заказчику отправлено автоматически в 08:00** (`napominanie-v-srok.log`):
«НАПОМИНАНИЕ: сегодня перевод на маршрут Е — откройте сессию», код 0, письмо положено в
папку «ADD-FUT». Текст (`~/.addfut/napominanie-2026-09-11.txt`) просит открыть сессию
Claude Code и написать «переводим» в окне 10:00–18:30 по машине. **Своего письма не шлю —
это был бы повтор того же самого через час.**

Предпосылки перечитаны сегодня:

| что | значение |
|---|---|
| `~/.addfut/route.txt` | **F** (перехода ещё не было) |
| акты заказчика `r33build/mr_journal.csv` | 4 строки от 04.09: `SWITCH_SIGNAL E|2026-09-04#1`, `OWNER_APPROVE`, `GRANULARITY_EXCEPTION 387 026` и `450 000` — не менялись |
| общее окно, спрошено ТОЛЬКО у `feed.common_window(2026-09-11)` | **02:00–10:30 America/Chicago** (10:00–18:30 по машине) |
| заготовка исполнителя | `tools/perevod_e_20260911.sh` (4872 б) и prompt на месте, от 10.09 19:27 |
| промежуточное состояние перехода | нет: `intent`-файлов в `~/.addfut` нет вовсе |

**Чего проверить не могу:** установлена ли строка крона «50 16 11 9 *». `crontab -l`
отклонён классификатором прав (попытка сделана в этой сессии, а не взята из вчерашней
записи). Это не критично: по тексту напоминания основной путь сегодня — **живая сессия**,
а не безголовый исполнитель. Двойное исполнение исключено по построению: пришедший вторым
видит `route=E` и только докладывает.

## 3. Пин журнала МР — БОЕВОЙ

```
cd r33build && ../.venv/bin/python -c "import mr_engine as M; print(M._read_deploy()['journal'])"
-> /home/alex/claude-projects/vol-down12/r33build/mr_journal.csv
```
Путь боевой, не временный. Расхождения нет.

## 4. Контур: вчерашняя сессия и текущее состояние

**Сессия 10.09 прошла штатно.** Счёт DUR833748, NAV **979 451,05**, позиции
`{ESZ26: 2, MESZ26: 6}`, сессия **№21**; вход `es_close=(7709.5, '2026-09-09')`,
`spy_close=762.4`, базис +1,121%, `yield_10y_pct=4.837`, `dref=7.8549`,
`signal_date=2026-09-30` (возраст 10 дней), серия Z26. Решение — плечо **1,0119**, книга
**26/0**, **заявок нет**; причина: «БУМАЖНЫЙ ЭТАП: порог §8 (3 000 000) не применён при
NLV 979 451; механический пол соблюдён». В 16:45 шлюз не отвечал — автопилот поднял его
сам («шлюз поднят, API отвечает» за 21 с), торговля прошла в 16:46. Замыкание:
NLV закрытия **980 143,55**, плечо закрытия **1,0051** при потолке 2,00.

**Книга** `book-F.json` (11.09 00:05): `session_no` = **21**, `last_session` =
**2026-09-10**, `close_provisional` = false, `roll_pending` = false, `n_e` = **26**,
`n_b` = **0**, `unit_is_mes` = true, `es_held` = **2**, `ser_a` = `ser_b` = **Z26**,
`prev_close_lev` = **1,0051374617524136**, `prev_st_eq` = true, `prev_st_bd` = false,
digest `d9acd498…`. Сверка с брокером по последним живым данным (10.09): 2×10 + 6 =
**26 MES** = `n_e`; ноги Б нет ни в книге, ни у брокера. **Расхождений нет.**

**Стык календаря сходится:** `feed.prev_session(2026-09-11)` = **2026-09-10** =
`last_session` в книге.

**Тревоги:** активных **0** (файлов `ALARM*.txt` — ноль; все шесть несут суффикс
`.resolved-*`, свежайший `ALARM-signal-2026-09.resolved-01.09`).

**Тик жив:** `tick-heartbeat` 11.09 **08:55**, возраст **~2 минуты** при пороге почтового
сторожа 65 минут. Отметки дня на месте: `traded-2026-09-10`, `closed-2026-09-10`.

**Журнал §7** `journal-F.csv`: **34** строки, последняя —
`2026-09-10 … ИТОГ … 979451.05 … итог сессии 21: строк 0`, цепочка хэшей непрерывна
(`275be8d5…` → `226560cf…`).

**Сигнал:** строка месяца действия `2026-09-30` = **(1, 0)** — нога А включена, нога Б
выключена. Сайдкар уровней заканчивается августом (92,74 / 767,05), поколение одно.
Отметка `sigup-2026-09` на месте.

**Маржа по книге** (замер `margins_live.json` от 18.08 08:54 UTC): начальная
2×35 050,37 + 6×3 506,29 = **91 138,48** = **9,30%** счёта; поддерживающая
2×25 116,17 + 6×2 511,62 = **65 302,06** = **6,66%**. Запас очень большой. Замеру
**24 дня из 35**, протухает **23.09**; предупреждение по графику — 15.09, файлов
предупреждения пока нет.

**Почта:** пульс за 10.09 ушёл («ПУЛЬС 10.09.2026: жив — NLV 985 482,46, тревог нет»),
сегодняшний в 09:07 впереди; напоминание о переводе ушло в 08:00; в `pochta-storozh.log`
новых записей с 04.09 17:03 нет.

**Диск:** свободно **9,5 ГБ (занято 84%)** — та же полка.
**Точки восстановления:** `~/.addfut-backups` и `~/state-mirror` — оба с
`addfut-2026-09-10-160529.tgz`; якорь `anchors/worm-2026-09-10.txt`; **0** незапушенных
коммитов, HEAD `f7d788d`.
**Пакет:** действующий — **568930d7** (`paket-r33-20260904-201716-568930d7.zip`).
**Git:** незакоммиченными висят только `docs/ADD-FUT-STATUS.pdf/.txt` — рабочий вывод
`status_stranitsa.py`, не состояние.

## Письма

Событий из закрытого списка п.3б (сделки, ролл, месячный переход, замыкание месяца, смена
упаковки, восстановление после сбоя контура, обслуживание) со времени отчёта 10.09 не
было: 10.09 — обычная сессия без заявок; подъём шлюза автопилотом — штатная работа, а не
восстановление после сбоя. Восстановление Drive сделал сам заказчик своей командой, так
что доклад ему об этом — не новость. Сегодня пятница — еженедельная сводка не положена
(понедельник). Напоминание о переводе уже ушло отдельным письмом в 08:00.
**Письмо от наблюдателя не отправляется.**

## Вердикт

**Изменений не требуется. Paper pilot продолжается.**

Вчерашнее срочное отклонение (токен Drive, счётчик 2 из 3) **закрыто**: заказчик
переавторизовал Drive 10.09 около 19:20, ночное замыкание прошло с `drive ok`, счётчик
сброшен в 0, `ALARM-push` не встал, пятничная сессия состоится.

Контур в норме: активных тревог 0, сессия 10.09 штатная (№21, заявок нет), книга 26/0
сходится с позицией брокера, плечо закрытия 1,0051 при потолке 2,00, журнал §7 цел,
тик свежий, копии вне машины снова свежие, незапушенного нет, маржа 9,30% счёта,
диск 9,5 ГБ, пин журнала МР боевой.

**Открытый пункт — не дефект, а запланированное действие заказчика:** сегодня третья
попытка перевода на маршрут Е; напоминание отправлено в 08:00, предпосылки (акты,
окно 10:00–18:30 по машине, заготовка исполнителя, `route=F`, отсутствие промежуточного
состояния) на месте. Классов A–F нет ни в одном пункте.
