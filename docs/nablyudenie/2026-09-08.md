# Наблюдение paper pilot — 08.09.2026 (вторник)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись.

## Задача на сегодня

По графику (PROJECT-STATE §0а) на 08.09 назначено: **«Labor Day 07.09 пропущен корректно»**.
Второе событие дня — **перевод на маршрут Е**, но он идёт днём (пуск исполнителя 16:50 по
машине), в утреннем наблюдении не участвует; ниже — только проверяемый утром факт наличия
предпосылок. Scope не расширялся.

## 1. Главное: Labor Day 07.09 пропущен корректно — PASS

Проверено четырьмя независимыми фактами.

**Календарь говорит «праздник».** Прогон кодом контура (`daily.holidays_for(2026)`, тот же
источник, что читает `is_trade_day` в `autopilot.sh:578`):

| дата | день | сб/вс | праздник CME |
|---|---|---|---|
| 04.09 | Friday | нет | нет |
| 05.09 | Saturday | **да** | нет |
| 06.09 | Sunday | **да** | нет |
| 07.09 | Monday | нет | **да** |
| 08.09 | Tuesday | нет | нет |

Праздники CME с сентября: **07.09**, 26.11, 25.12. То есть 07.09 отсекается веткой
«праздник», а 05–06.09 — более ранней проверкой `dow>=6` по Чикаго (`autopilot.sh:553`).

**Автопилот тикал и промолчал ровно там, где должен.** Сердцебиение пишется ДО календарных
ворот (`hb_write` в строке 490, ворота — в 1108), поэтому свежий `tick-heartbeat` доказывает,
что тики шли: **08.09 08:55:01**, возраст **2,7 минуты** при пороге почтового сторожа 65
минут. В `~/.addfut/autopilot.log` за 05, 06 и 07.09 нет ни одной строки — так и задумано:
ветка выходного/праздника делает `return 0` без записи в журнал; вахта О-3-Е в этой ветке
работает только на маршруте Е, а мы на Ф.

**Отметок дня не появилось.** Последняя `traded-*` — `traded-2026-09-04`, последняя
`closed-*` — `closed-2026-09-04` (создана 05.09 в 00:06). За 05, 06 и 07.09 отметок нет:
ни торговли, ни замыкания в нерабочие дни не было.

**Тревог календаря не возникло.** Файлов `ALARM-calendar*`, `ALARM-calendar-broken*` нет;
в `~/.addfut` шесть файлов `ALARM*`, **все** с суффиксом `.resolved-*`, свежайший —
`ALARM-signal-2026-09.resolved-01.09`. **Активных тревог 0.**

**Стык с сегодняшним днём сходится.** `feed.prev_session(2026-09-08)` = **2026-09-04** —
ровно `last_session` в книге. Значит сегодняшняя сессия возьмёт за предыдущее закрытие
пятницу 04.09, как и положено после пропущенного праздника, а не «вчерашний» день.

## 2. ОТКЛОНЕНИЕ ПРОДОЛЖАЕТСЯ: Drive не восстановлен, до остановки контура двое суток

Находка отчёта 07.09 не закрыта. Проверено живьём в этой сессии:
`~/bin/rclone lsd vol-down12-drive:` в 08:59 отвечает
`couldn't fetch token: invalid_grant: maybe token expired?` — тот же отозванный
refresh-токен OAuth (приложение в статусе Testing, Google убивает токен раз в 7 суток;
BACKLOG 22б и чек-лист живого старта п.4).

Счётчик отказов растёт: в `~/.addfut/wip-push.log` строк
`wip: ОТКАЗ выгрузки на Drive (2 файлов НЕ покрыты)` уже **125** (вчера в это время было 77),
последняя — **08.09 08:37:01**. Промежутков успеха нет ни одного.

Календарь остановки не изменился, потому что за выходные и праздник замыканий не было
(`push-fails` по-прежнему отсутствует — счётчик неудачных замыканий чист):

| замыкание 00:05 | за сессию | счётчик | что происходит |
|---|---|---|---|
| 09.09 | вт 08.09 | 1 | «внешняя выгрузка копий не удалась (1 подряд)» |
| 10.09 | ср 09.09 | 2 | то же |
| 11.09 | чт 10.09 | 3 | **ALARM-push, автопилот остановлен** |

Контур встанет **в ночь на 11.09**; пятничная сессия 11.09 не состоится. Это не дефект:
`autopilot.sh` (1392–1416) ведёт себя ровно как задумано — три замыкания без внешних копий
означают остановку до ручного разбора. Правки production-кода не требуется ни в одном файле,
класса A–F здесь нет; это **сработавшее принятое ограничение версии**, у которого есть срок.

Сам себе помочь не могу и это перепроверено, а не предположено (правило 13): обновление по
refresh-токену отвергнуто сервером Google, безголового пути не осталось,
`rclone config reconnect` требует живого согласия в браузере под аккаунтом владельца.

**Действие заказчика — одна команда до вечера 10.09:**
`~/bin/rclone config reconnect vol-down12-drive:`

Локальные точки восстановления при этом целы: `~/.addfut-backups` и `~/state-mirror` — по
свежему `addfut-2026-09-04-160528.tgz`; git-зеркало и GitHub в актуальном состоянии
(**0** незапушенных коммитов в обе стороны). Вне машины не обновляются только архивы
состояния счёта — их свежесть застыла на **04.09**.

## 3. Перевод на маршрут Е: предпосылки на месте, строку крона проверить не смог

`~/.addfut/route.txt` = **F** — перевод (перенесён с 04.09) ещё не состоялся, что и
ожидается: исполнитель `tools/perevod_e_20260908.sh` (файл на месте, 4871 байт от 04.09
21:13, исполняемый) запускается в 16:50 по машине. Торговое окно, спрошенное только у
`feed.common_window(2026-09-08)`, как требует §0а: **02:00–10:30 по Чикаго**, то есть
10:45–18:45 по машине — пуск в 16:50 попадает внутрь с запасом. Следов работы исполнителя
пока нет (в `/tmp` только письмо от несостоявшегося перевода 04.09).

**Чего проверить не смог:** содержимое системного crontab в этой сессии недоступно —
`crontab -l` отклонён классификатором прав, `/var/spool/cron/crontabs/alex` не читается
(Permission denied). Поэтому подтвердить наличие строки
`50 16 8 9 * … perevod_e_20260908.sh`, которую по §0а кладёт заказчик, я не могу ни
опытом, ни чтением. Записываю как непроверенный пункт, а не как «всё в порядке».
Напоминание ушло в понедельничной сводке; повторено в сегодняшнем письме.

## 4. Книга, журнал, фон

**Книга** `book-F.json` (05.09 00:05, с тех пор не менялась): `session_no` = **18**,
`last_session` = **2026-09-04**, `close_provisional` = false, `roll_pending` = false,
`n_e` = **26**, `n_b` = **0**, `unit_is_mes` = true, `es_held` = **2**, `ser_a` = `ser_b` =
**Z26**, `prev_close_lev` = **1,0071267672072388**, `prev_st_eq` = true, `prev_st_bd` = false,
digest `8d6ce2f8…`. Незакрытых намерений нет: единственный intent-файл несёт суффикс
`-done-` и датирован 26.08. Сверка с брокером — по последним живым данным сессии №18 (04.09):
`{ESZ26: 2, MESZ26: 6}` = 2×10 + 6 = **26 MES** = `n_e`; ноги Б нет ни в книге, ни у брокера.
Расхождений нет; за выходные и праздник обращений к брокеру не было и быть не должно.

**Журнал §7** `journal-F.csv`: 31 строка, последняя —
`2026-09-04 … ИТОГ … 998642.01 … итог сессии 18: строк 0`, цепочка хэшей непрерывна
(`492bca35…` → `245d5c00…`).

**Сигнал**: строка месяца действия `2026-09-30` = **(1, 0)** — нога А включена, нога Б
выключена; сайдкар уровней заканчивается августом (92,74 / 767,05), поколение одно.
Отметка `sigup-2026-09` на месте.

**Маржа по книге** (замер `margins_live.json` от 18.08): начальная 2×35 050,37 +
6×3 506,29 = **91 138,48** = **9,2%** счёта; поддерживающая 2×25 116,17 + 6×2 511,62 =
**65 302,06** = **6,6%**. Запас очень большой. Замер протухает 23.09, предупреждение по
графику — 15.09; файлов предупреждения о старении пока нет.

**Почта и пульс**: пульс за 07.09 ушёл («ПУЛЬС 07.09.2026: жив — NLV 994 161,84, тревог
нет»); `pochta-storozh.log` — новых записей с 04.09 17:03 нет. Сегодняшний пульс в 09:07
ещё впереди.

**Диск**: свободно **9,3 ГБ (занято 84%)** — та же полка, что вчера (9,4 ГБ / 84%).
**Пакет**: действующий — **568930d7** (`paket-r33-20260904-201716-568930d7.zip`).
**Git**: незакоммиченными висят только `docs/ADD-FUT-STATUS.pdf/.txt` — рабочий вывод
`status_stranitsa.py`, не состояние.

## Письма

Событий из закрытого списка (сделки, ролл, месячный переход, замыкание месяца, смена
упаковки, восстановление, обслуживание) со времени отчёта 07.09 не было: 07.09 — праздник,
сессии не было. Письмо отправляется по пункту 5 задания — вердикт нештатный: срок по Drive
сократился до двух суток, и сегодня день перевода на Е.

## Вердикт

Назначенная на сегодня проверка — **PASS**: Labor Day 07.09 пропущен корректно (календарь
контура отмечает 07.09 праздником CME, тики шли — сердцебиение свежее, журнал за 05–07.09
пуст по построению, отметок `traded-`/`closed-` за эти дни нет, тревог календаря нет,
`prev_session(08.09)` = 04.09 сходится с книгой). Контур в норме: активных тревог 0, книга
26/0 не менялась с пятницы и сходится с последней известной позицией брокера, плечо закрытия
1,0071 при потолке 2,00, журнал §7 цел, незапушенного нет.

Вердикт при этом **не штатный** — по не закрытой со вчера причине: токен Drive отозван,
копии состояния вне машины не обновляются с 05.09, и по правилу трёх неудачных замыканий
подряд автопилот **остановится в ночь на 11.09**. Правок кода это не требует; требуется
переавторизация в браузере **до вечера 10.09**, недоступная мне. Отдельно отмечаю
непроверяемый в этой сессии пункт: наличие строки крона на сегодняшний перевод в Е.

**Paper pilot продолжается; до вечера 10.09 нужна переавторизация Drive.**
