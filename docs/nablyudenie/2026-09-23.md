# Наблюдение paper pilot — 23.09.2026 (среда)

Сессия запущена системным cron. Режим — только чтение и доклад; production-код, книга,
`~/.addfut`, cron и норматив не менялись. Маршрут **Е**.

## Задача на сегодня

По графику §0а особой задачи на 23.09 нет (прежний срок старения замера маржи 23.09
закрыт перегенерацией first_connect 20.09) — быстрый статус и строка про пин журнала МР.
Scope не расширялся.

## 1. Пин журнала МР — БОЕВОЙ

```
-> /home/alex/claude-projects/vol-down12/r33build/mr_journal.csv
```

## 2. Быстрый статус

| проверка | факт | итог |
|---|---|---|
| тревоги `ALARM*` | активных нет (только `.resolved-*`) | ок |
| пульс тика | `tick-heartbeat` 23.09 08:55 | ок |
| торговля 22.09 | шлюз не отвечал в 16:45:04 — поднят автопилотом за 21 с (штатный путь, 16-й случай с начала пилота); сессия №29: NAV 999 655,98, позиции CSPX 1219, плечо 1,0166, заявок нет; «торговля 2026-09-22: ок» 16:46 | ок |
| замыкание 22.09 | NLV закрытия 998 973,35, плечо закрытия 1,0221; снимок+WORM `worm-2026-09-22`; зеркало/github/drive ok; `closed-2026-09-22` | ок |
| книга | `book-E.json` читается: сессия 29, `n_eq` 1219, `n_bd` 0, `last_session` 2026-09-22, `close_provisional` false | ок |
| маршрут | `route.txt` = E | ок |
| журнал §7 (E) | итог сессии 29: строк 0 | ок |
| замер маржи | `margins_live.json` от 2026-09-20 09:25 UTC (Z26/H27) | ок |
| push-fails | счётчика нет | ок |
| wip-push | каждые полчаса «на Drive выгружено», последняя 08:37 | ок |
| диск | / 58G, занято 46G (80%), свободно 12G | ок |

Попутно: «Error 322 … Maximum number of account summary requests exceeded» (4 строки) на
прогреве сессии 22.09 — известный шум, на результат не повлиял. Не отклонение.

## 3. События со времени прошлого отчёта (22.09)

Одна обычная сессия без сделок и обычное замыкание дня. Подъём шлюза при старте сессии —
штатный путь запуска, а не восстановление после сбоя (тревоги не было). Событий из
закрытого списка нет — письмо не отправлялось.

## Вердикт

Изменений не требуется. Paper pilot продолжается.
