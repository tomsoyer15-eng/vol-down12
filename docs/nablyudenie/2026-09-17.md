# Наблюдение paper pilot — 17.09.2026 (четверг)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись. Второй день
пилота на маршруте **Е**.

## Задача на сегодня

По графику (PROJECT-STATE §0а) на 17.09 особой задачи нет: ближайшие даты — **21.09**
напоминание и **до 22.09** перегенерация `first_connect`, затем **20.11** расчёт даты
ноябрьского ролла. Значит сегодня — быстрый статус, обязательная строка про пин журнала МР
и назначенная вчерашним отчётом перепроверка счётчиков после перезагрузки. Scope не
расширялся.

---

## 1. Пин журнала МР — БОЕВОЙ

```
cd r33build && ../.venv/bin/python -c "import mr_engine as M; print(M._read_deploy()['journal'])"
-> /home/alex/claude-projects/vol-down12/r33build/mr_journal.csv
```
Путь боевой, не временный. Расхождения нет. Хвост журнала — обе строки перехода
(`TRANSITION_OPEN` / `TRANSITION_COMPLETE`, `E|2026-09-04#1|26deb37d5243f30c`), новых
строк со вчера нет.

---

## 2. ГЛАВНОЕ ЗА СУТКИ: машина перезагружена, контур поднялся сам

Перезагрузка, о необходимости которой писал вчерашний отчёт
(`docs/nablyudenie/2026-09-16-processy.md`), **выполнена**:

```
uptime -s / who -b  -> 2026-09-16 16:41   (вчера было up 36 days)
uname -r            -> 6.8.0-139-generic  (работало 137)
```
Ядро 139 и обновлённая libc6 подхвачены, требование `*** System restart required ***`
снято.

**Восстановление контура — автоматическое и уложилось в 21 секунду.** Журнал:

```
2026-09-16 16:45:05 шлюз не отвечает — запускаю
2026-09-16 16:45:26 шлюз поднят, API отвечает
2026-09-16 16:46:01 торговля 2026-09-16: ок
2026-09-16 18:36:13 замыкание 2026-09-16: ок (копия состояния снята)
```
То есть перезагрузка пришлась на **16:41**, а первый торговый тик — на **16:45**: запас
составил четыре минуты. Сработало ровно так, как обещал вчерашний разбор (`cron.service`
поднимается сам, шлюз поднимает `ensure_gw` за 21 с), но окно оказалось теснее
рекомендованного (советовалось 10:00–16:00 по машине). **Фактом наблюдения считаю
подтверждённое восстановление после перезапуска машины** — один из пунктов §12 ТЗ,
который до сих пор проверялся только стендами.

**Сессия №25 (16.09) прошла штатно, заявок нет:**
```
счёт DUR833748: NAV 977,851.84; позиции {'CSPX': 1219.0}; сессия №25
вход: CSPX=(818.06, '2026-09-15'), CBU0=(150.16, '2026-09-15'), signal_date=2026-09-30
решение: плечо 1.0198, книга -/-
заявки: нет
```
Сверка с брокером сошлась: позиция на счёте `{'CSPX': 1219.0}` = книга (`n_eq` 1219,
`n_bd` 0). Замыкание 18:35: **NLV закрытия 979 083,05**, вход
`{'CSPX': (821.68, '2026-09-16'), 'CBU0': (150.6, '2026-09-16')}`, **плечо закрытия
1,0230** при потолке 2,00. Снимок+WORM `addfut-2026-09-16-103529.tgz`, якорь
`worm-2026-09-16.txt` (коммит `839891f`), `зеркало ok / github ok / drive ok`.
Отметки `traded-2026-09-16` и `closed-2026-09-16` на месте.

**Счёт:** 979 083,05 против 974 811,39 накануне — **+4 271,66 (+0,44%)** за день, движение
рынка, сделок не было. С начала пилота (1 000 000) — **−20 916,95 (−2,09%)**.

---

## 3. Перепроверка счётчиков после перезагрузки (назначено вчера)

Вчерашний прогноз был «зависших тиков 0, JVM 1». Фактически после **16,3 часа** аптайма:

| показатель | до перезагрузки (36 дней) | сейчас (16,3 ч) |
|---|---|---|
| зависших `autopilot.sh tick` | 4 | **1** (pid 3013, 16,2 ч = тик 16.09 16:45) |
| живых JVM шлюза | 6, суммарно 1,62 ГБ | **2**, суммарно 1,15 ГБ |
| память занято / доступно | 3 666 / 4 228 МБ | **2 612 / 5 282 МБ** |
| своп занят | 2 057 МБ (40%) | **0** |

Перезагрузка накопление сняла полностью, как и ожидалось. Но **механизм воспроизвёлся
сразу же**: единственный за сутки запуск шлюза (16.09 16:45) снова оставил осиротевший
тик (`ppid=1`, `wchan=do_wait`) с живой цепочкой `start.sh -> displaybannerandlaunch.sh ->
ibcstart.sh -> JVM`. Вторая JVM (pid 31667, 645 МБ, возраст 9,1 ч, собственное дерево
`xvfb-run`, `ppid=1`) появилась ночью около 23:52 без строки в `autopilot.log` — это
собственный суточный перезапуск IBC, не тик. Разбираться в устройстве связки не стал:
вчерашняя карточка закрыта как «не BLOCKER и не MATERIAL», правка `autopilot.sh` запрещена
режимом пилота §3, а свободной памяти 5,28 ГБ и своп пуст.

**Класс сегодня прежний: ни A, ни B, ни C, ни D, ни E, ни F.** Наблюдение продолжается
по вчерашнему правилу: смотреть тот же счётчик раз в неделю; переоткрытие карточки как
MATERIAL — если счётчик снова дойдёт до четырёх при свободной памяти менее ~2 ГБ.

---

## 4. Быстрый статус контура

**Маршрут:** `~/.addfut/route.txt` = **E**.

**Книга** прочитана штатным загрузчиком `state.load(book-E.json, daily.BookE)` — исключения
нет, значит хэш состояния сошёлся: маршрут **E**, сессия **№25**, `n_eq` = **1219,0** долей
CSPX, `n_bd` = **0**, `roll_pending` = false, `prev_close_lev` = **1,0230265144514552**,
`prev_st_eq` = true, `prev_st_bd` = false, `last_session` = **2026-09-16**,
`close_provisional` = false. Сходится с позицией брокера, прочитанной вчерашней сессией.

**Календарь сходится:** `feed.prev_session(2026-09-17)` = **2026-09-16** = `last_session`;
`feed.common_window(2026-09-17)` = **02:00–10:30 Чикаго** (10:00–18:30 по машине), пуск
торговой части в 16:45 по машине попадает внутрь.

**Тревоги:** активных **0** (все шесть `ALARM*` с суффиксом `.resolved-*`, свежайший
`ALARM-signal-2026-09.resolved-01.09`). `WARN-margins-*` — предупреждение, не тревога.

**Тик жив:** `tick-heartbeat` 17.09 08:55:02, возраст **165 с** при пороге почтового
сторожа 65 мин.

**Журнал §7.** `journal-E.csv`: две строки, добавилась
`2026-09-16 … ИТОГ … nav 977 851,84 … итог сессии 25: строк 0`; цепочка хэшей непрерывна
(`prev_hash` строки 16.09 = `row_hash` строки 15.09, `791cd354…`). Сделок не было — строк
исполнений нет, и это верно.

**Сигнал:** строка месяца действия `2026-09-30` = **(1, 0)** — нога А включена, нога Б
выключена, позиция ей соответствует; возраст сигнала 16 дней. Сайдкар уровней
заканчивается августом (92,74 / 767,05), поколение одно. Ближайший месячный переход —
вторая сессия октября.

**Маржа:** новых замеров со вчера нет; действуют цифры предполёта перехода — начальная
**287 316 ≈ 29% счёта**, поддерживающая 249 840, свободные средства 688 669, запас
**3,90×**. Отдельного подключения к шлюзу ради свежей цифры не делал (наблюдатель только
читает файлы).

**Замер маржи стареет штатно:** `WARN-margins-2026-09-17.txt` — «ему **30 дней**, до
предела 35 осталось **5**». Край прежний: **22.09 — последний годный день, 23.09 — первый
день отказа**. Встанут выпуск пакета и переход Ф↔Е; ежедневная торговля, замыкание и
WORM-якорь от возраста замера не зависят.

**Копии и диск:** `~/.addfut-backups` и `~/state-mirror` — обе с
`addfut-2026-09-16-103529.tgz`. `wip-push` каждые полчаса без пропусков, последний срез
**17.09 08:37:05**. Диск: свободно **13 ГБ (занято 79%)** — на 3 ГБ больше вчерашнего,
перезагрузка освободила временные файлы.

**Git:** незапушенных коммитов **0**, HEAD `839891f`. Незакоммиченными висят только
`docs/ADD-FUT-STATUS.pdf/.txt` — рабочий вывод статус-страницы, не состояние.

**Выпуск:** действующий пакет — **45de3d90** от 15.09
(`paket-r33-20260915-173252-45de3d90.zip`), изменений нет.

**H19:** новых завершённых месяцев нет (записан 1, за 2026-08).

**Известный шум IBKR, не дефект:** при подъёме шлюза журнал печатает
`Error 322 … Maximum number of account summary requests exceeded`. Строка не новая — она
появляется при каждом запуске шлюза с **19.08** (22 вхождения за месяц), и каждый раз NAV
и позиции после неё прочитаны успешно, включая вчерашний тик. Нового ничего, разбирать не
стал.

---

## 5. Открытое со вчера (на заказчика, не на меня)

1. **Перегенерировать `first_connect` утром до 22.09 включительно** — осталось **5 дней**;
   иначе с 23.09 встанут выпуск пакета и возможный возврат Е→Ф.
2. **Напоминание на 21.09 по-прежнему не заведено**: ждущих процессов
   `napominanie_v_srok.py` — **0** (проверено разбором `ps` из Python), файла
   `~/.addfut/napominanie-2026-09-21.txt` нет (в каталоге только `-09-11` и `-09-14`).
   Завести не могу — это запись в `~/.addfut`.
3. Обновить в PROJECT-STATE §0а базлайн (фактический — **45de3d90** от 15.09, в тексте
   стоит 2c6e76ca) и статус карточки класса F про `transition_id` (закрыта правкой 15.09).
4. Наблюдение за фактической стоимостью заёмных средств на Е продолжается — первая
   осмысленная оценка появится по выписке за несколько дней займа.

## Письма

* **Отправлено письмо о событии** «перезагрузка машины и автоматическое восстановление
  контура» (правило п.3б, класс «обслуживание / восстановление»). Проверено по почте: за
  15–17.09 писем об этом событии не уходило, так что письмо не дубль.
* Других событий из закрытого списка п.3б со вчерашнего отчёта нет: сделок нет, ролла нет,
  месячного перехода нет, замыкания месяца нет, смены упаковки нет.
* Сегодня четверг — еженедельной сводки нет.
* Пункты 1 и 2 раздела 5 — те же самые отклонения, о которых письмо ушло 15.09 и 16.09;
  повторять не стал.

## Вердикт

**Изменений не требуется. Paper pilot продолжается.**

Сутки прошли с одним заметным событием — перезагрузкой машины в 16:41, за четыре минуты до
торгового тика. Контур поднялся сам: шлюз за 21 секунду, торговля 16:46 штатно, замыкание
18:36, NLV 979 083,05 (+0,44% за день), плечо закрытия 1,0230 при потолке 2,00, заявок
нет, сверка с брокером сошлась (CSPX 1219, фьючерсов нет). Накопление зависших процессов
перезагрузкой снято (своп 0, доступно 5,28 ГБ) и воспроизводится прежним темпом — запись
без правки, как и вчера. Тревог 0, тик 165 с, книга и календарь сходятся, журнал §7 цел,
пин журнала МР боевой, копии разошлись по трём адресам. Дефектов классов A–F не
обнаружено.
