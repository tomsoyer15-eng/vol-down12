# Наблюдение paper pilot — 31.08.2026 (понедельник)

Сессия запущена системным cron (`~/.addfut/nablyudatel.log`: «2026-08-31 08:57:01 наблюдатель:
старт»). Режим — только чтение и доклад; production-код, книга, `~/.addfut`, cron и норматив
не менялись.

## Задача на сегодня

По графику наблюдения (PROJECT-STATE §0а) на 31.08 назначено: **состояние перед замыканием
месяца** — тревоги, тики, книга. Сегодня понедельник и одновременно ПОСЛЕДНЯЯ сессия августа:
замыкание месяца пройдёт ночью (после 16:00 Чикаго, по машине ~00:05 01.09), и его результат
проверяется по графику завтра утром. Дополнительно по регламенту понедельника отправлена
еженедельная сводка письмом.

## Факты

### 1. Тревоги — активных нет
В `~/.addfut/` четыре файла `ALARM*`, все с суффиксом `.resolved-*`:
`ALARM-route-2026-08-15.resolved-16.08`, `ALARM-route-2026-08-16.resolved-16.08`,
`ALARM-backup-2026-08-18.resolved-19.08`, `ALARM-env-2026-08-26.resolved-27.08`.
Файла активной тревоги нет — автопилот не заперт, замыкание месяца выполнится штатно.

### 2. Тики живы
`tick-heartbeat` = 1788155701 → 2026-08-31 08:55:01, возраст на момент проверки **2,4 минуты**
(порог почтового сторожа — 65 минут). Строка тика в системном crontab на месте
(`5,15,25,35,45,55 * * * *`), как и замыкание/сторожа/пульс/наблюдатель.

### 3. Книга читается и согласована; незавершённых операций нет
`~/.addfut/book-F.json`, digest `912bcbef…01d2e`:
`session_no` = 13, `n_e` = **26**, `n_b` = **0**, `unit_is_mes` = true, `es_held` = 2,
`ser_a` = `ser_b` = **Z26**, `roll_pending` = **false**, `close_provisional` = **false**,
`prev_close_lev` = 1,00529, `prev_st_eq` = true, `prev_st_bd` = false,
`last_session` = **2026-08-28**, `d_fix` = 7,92914.
Отметки последнего торгового дня на месте обе: `traded-2026-08-28`, `closed-2026-08-28`.
`last_session` = последнему замкнутому дню, `close_provisional` = false — то есть контур
входит в сегодняшнюю сессию из полностью замкнутого состояния, промежуточных операций и
незакрытых намерений нет (последний intent — `intent-done` от 26.08, ролл).

### 4. Журнал за выходные — тишина, как и должно быть
Последняя запись `autopilot.log` — 2026-08-29 00:06:29 «замыкание 2026-08-28: ок (копия
состояния снята)». Суббота и воскресенье торговых дней не имеют, тики отработали молча.
Ошибок в журнале с прошлого отчёта нет. Событий контура сверх обычного с 29.08 не было,
поэтому письмо «о событии» не отправлялось (отправлена только понедельничная сводка).

### 5. Сигнальный ряд — августовская строка на месте, сентябрьская ещё не ожидается
`signals_live.csv`, последняя строка `2026-08-31,1,0` — месяц действия август: нога А ВКЛ,
нога Б ВЫКЛ. Ровно это и стоит в книге (26 MES по ноге А, 0 по ноге Б). Отметка месяца
`sigup-2026-08` присутствует. Строка сентября (`2026-09-30`) появляется ПОСЛЕ замыкания
августа — её проверка назначена на 01.09, месячный переход исполняется второй сессией
сентября (02.09), проверка — 03.09.

### 6. Резервирование и диск
`wip-push.log`: последняя запись 2026-08-31 08:37:04, выгрузка на Drive прошла.
Незапушенных коммитов нет ни в `origin`, ни в `mirror`. В рабочем дереве изменены только
`docs/ADD-FUT-STATUS.pdf`/`.txt` — это страница статуса, которую каждые 10 минут
перегенерирует `status_stranitsa.py`; она и попадает срезом на Drive («2 файл(ов)»).
Диск `/dev/vda1`: 58G всего, 43G занято, **15G свободно, 75%** (на 29.08 было 16G/74%).

### 7. Еженедельная сводка (понедельник) — отправлена
Содержание: NLV пятницы 28.08 **994 888,13** против пятницы 21.08 **989 847,19** — за неделю
**+5 040,94 (+0,51%)**; с начала пилота от 1 000 000 — **−5 111,87 (−0,51%)**.
Позиции: нога А включена, набрана 2×ESZ26 + 6×MESZ26 = 26 MES-единиц; нога Б выключена
сигналом (0). Плечо закрытия **1,0053** при потолке 2,00.
Маржа по книге (замер `margins_live.json` от 18.08): начальная **91 138,48** (9,2% счёта),
поддерживающая **65 302,06** (6,6%) — запас более чем десятикратный.
События недели: **ролл U26→Z26 в среду 26.08** — четыре заявки (−2 ESU26, −6 MESU26,
+2 ESZ26, +6 MESZ26), суммарная комиссия **16,28**; сессия помечена как исключённая из
выборки издержек §7 (ориентир задержанный). Других сделок за неделю нет. Тревог за неделю
нет. 27.08 в 11:42–11:43 две выгрузки копий дали ошибку и тут же были повторены успешно
(«drive ok» в 11:43:57) — тревога не поднималась, ни одна копия не пропущена.

## Вердикт

Отклонений от норматива не обнаружено. Карточка дефекта не оформляется.

**Изменений не требуется. Paper pilot продолжается.**

Ближайшие даты графика: 01.09 — проверка замыкания августа (closed-отметка, WORM, итог §7,
плечо, строка сентября); 03.09 — месячный переход; 04.09 — перевод пилота на маршрут Е
(безголовый исполнитель в crontab, 16:51); 08.09 — Labor Day 07.09; 15.09 — старение замера
маржи (протухает 23.09).
