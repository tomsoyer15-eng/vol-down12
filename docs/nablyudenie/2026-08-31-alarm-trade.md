# ИНЦИДЕНТ: ALARM-trade-2026-08-31 — тревога первого тика, гонка прогрева шлюза

## Хроника (по журналу, время машины)
16:45:04 первый торговый тик недели: шлюз не отвечает (после выходных) -> автопилот запускает
16:45:16 «шлюз поднят, API отвечает» (порт открылся)
16:45:16+ session.py подключается; внутри connectAsync шаг reqExecutions виснет
16:45:49 TimeoutError -> код 1 -> ТРЕВОГА торговли, автопилот остановлен (fail-closed)
16:53:06 почтовый сторож отправил письмо заказчику (штатно, в пределах 10 минут)
16:5x    разбор: полное подключение проверкой проходит — шлюз здоров, гонка прогрева
17:0x    тревога снята (.resolved-31.08); следующий тик навёрстывает сессию

## Что НЕ пострадало
Ни одной заявки не подано (падение на этапе подключения, до торговли); книга не тронута;
позиции целы; сторож и почта отработали как задуманы. Пятница 28.08 прошла тот же путь
успешно (шлюз поднят 16:45:25 -> торговля ок 16:46:02) — исход решают секунды прогрева.

## КАРТОЧКА ДЕФЕКТА (по формату режима пилота)
ПРОБЛЕМА: сессия подключается к ТОЛЬКО ЧТО поднятому шлюзу без проверки его готовности;
  порт API открывается раньше, чем шлюз способен отдать executions, и таймаут разогрева
  превращается в остановку торговли на весь день до ручного разбора.
ПУТЬ ДОСТИЖЕНИЯ: первый тик после простоя шлюза (понедельник/после рестарта IB) ->
  автопилот поднимает шлюз -> в том же тике запускает сессию -> прогрев дольше ~30 с ->
  TimeoutError на connect -> ТРЕВОГА.
ВОСПРОИЗВЕДЕНИЕ: наблюдено живьём 31.08.2026 (полный traceback в ALARM-файле); пятница
  28.08 — тот же путь с успехом на более быстром прогреве: гонка подтверждена парой
  наблюдений, а не теорией.
ПОСЛЕДСТВИЕ: потеря торгового дня (до ручного снятия) на ровном месте; в день месячного
  перехода или ролла — потеря ключевой сессии. Деньгам напрямую не грозит (fail-closed).
КЛАСС: MATERIAL (класс D — необоснованная остановка после штатно предусмотренного сбоя).
МИНИМАЛЬНАЯ ПРАВКА (предложение, ждёт решения заказчика): в session._connect один повтор
  подключения через ~30 с при TimeoutError. Повтор ПОДКЛЮЧЕНИЯ, не заявки: заявок в этот
  момент ещё не существует, правило 7 не задевается. ~4 строки. Альтернатива — ждать
  готовности шлюза в автопилоте, но это новая развилка в оболочке (против правила 8д).
РИСК ПРАВКИ: +30 с к худшему времени старта сессии; повтор строго до первой заявки.
СОПУТСТВУЮЩЕЕ (в BACKLOG): диагност не знает сигнатуру TimeoutError-на-connect —
  «причина не классифицирована»; при правке добавить код ШЛЮЗ-НЕ-ПРОГРЕЛСЯ.

## Решение по инциденту
Тревога снята после проверки здоровья шлюза полным подключением. Правка кода — ТОЛЬКО
после «да» заказчика по карточке (режим пилота, п.5).
