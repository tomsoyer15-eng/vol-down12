# Наблюдение paper pilot — 03.09.2026 (четверг)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись.

## Задача на сегодня

По графику наблюдения (PROJECT-STATE §0а) на 03.09 назначено **ключевое событие пилота —
проверка МЕСЯЧНОГО ПЕРЕХОДА**: решение второй сессии месяца (02.09), исполнение либо штатное
отсутствие сделок, сверка книги с фактической позицией IBKR (reconcile), кап плеча.
Проверено строго это; scope не расширялся.

## Факты

### 1. Вторая сессия сентября состоялась и это сессия №16
Отметки в `~/.addfut/`: `traded-2026-09-01` и `traded-2026-09-02` — за сентябрь ровно две,
значит **02.09 и есть вторая сессия месяца**, на которой по конвенции strict_states
исполняется месячное переключение. Из `autopilot.log` (02.09 16:45):
`счёт DUR833748: NAV 985,387.49; позиции {'MESZ26': 6.0, 'ESZ26': 2.0}; сессия №16`.
Строка «торговля 2026-09-02: ок» стоит, режим — ЖИВОЙ.

### 2. Решение перехода: строка сентября применена, менять было нечего
Вход сессии: `signal_date=2026-09-30`, `signal_age_d=2`, `es_close=(7709.5, '2026-09-01')`,
`spy_close=761.78`, `basis=+1.204%`, `yield_10y_pct=4.796`, `dref=7.870211339047597`,
серия `Z26`.
Сигнальный ряд `signals_live.csv`: `2026-08-31,1,0` → `2026-09-30,**1,0**`. То есть месячный
сигнал сентября совпал с августовским: **нога А (акции) ВКЛЮЧЕНА, нога Б (трежерис)
ВЫКЛЮЧЕНА**. Переключать нечего — целевая книга сентября равна августовской.
Фактический sha256 файла `cf9c84ce…f8d5e` совпадает с записанным в `signals_live.csv.sha256`;
отметка `sigup-2026-09` на месте.

### 3. Исполнение: заявок нет — штатно
Решение сессии №16 дословно: «плечо 1.0050, книга **26/0**», причина «БУМАЖНЫЙ ЭТАП: порог §8
(3,000,000) не применён при NLV 985,387; механический пол соблюдён», **«заявки: нет»**.
Отсутствие сделок здесь — правильный исход, а не пропуск: сигнал ног не менял, целевая книга
совпала с фактической, полоса 10% не пробита.
В журнале §7 (`journal-F.csv`) последняя строка: `2026-09-02 … ИТОГ … NAV 985 387,49 …
итог сессии 16: строк 0` — исполнений в этот день ноль, издержек ноль. Цепочка хэшей строк
непрерывна (`de5b2152…` → `83eae6c7…`).

### 4. Reconcile: книга сходится с фактической позицией IBKR
Книга `~/.addfut/book-F.json` (файл digest `f306cffb…`, внутренний `53b3c8f8…`):
`session_no` = **16**, `last_session` = **2026-09-02**, `close_provisional` = **false**,
`roll_pending` = false, `n_e` = **26**, `n_b` = **0**, `unit_is_mes` = true, `es_held` = **2**,
`ser_a` = `ser_b` = **Z26**, `route` = **F**.
Фактическая позиция брокера: `{MESZ26: 6, ESZ26: 2}`. Пересчёт в модельную единицу:
2 × 10 + 6 = **26 MES** = `n_e`; `es_held` = 2 совпадает с числом больших контрактов; ноги Б
нет ни в книге (`n_b`=0), ни у брокера. **Расхождений нет.** Незакрытых намерений (intent) в
`~/.addfut/` нет.

### 5. Кап плеча — запас более чем двукратный
Плечо решения сессии №16 — **1,0050**; плечо ФАКТИЧЕСКОГО ЗАКРЫТИЯ 02.09 —
**1,0065424327181074** (`prev_close_lev` в книге, та же цифра в журнале замыкания:
«плечо закрытия 1.0065 — триггер капа следующей сессии»). Потолок — **2,00**. Кап не
приближен; ветка среза не активировалась и активироваться была не должна.

### 6. Замыкание 02.09 прошло, копии и WORM сняты
`autopilot.log` 03.09 00:05–00:06: «замкнуто: NLV закрытия **988 242,49**», вход
{spy_close 765,16 (02.09), es_close 7 737,75 (02.09), yield_10y 4,796, dref 7,870211339047597,
серия Z26}; «зеркало ok», «github ok», «drive ok»; «замыкание 2026-09-02: ок (копия состояния
снята)». Отметка `closed-2026-09-02` на месте. WORM-якорь `anchors/worm-2026-09-02.txt`
записан и лежит в HEAD (коммит `53fae32`). Архив состояния
`~/.addfut-backups/addfut-2026-09-02-160529.tgz` снят, всего архивов 16 (ротация до 60).

### 7. Общий фон (обязательная часть быстрого статуса)
- **Тревог нет**: в `~/.addfut/` шесть файлов `ALARM*`, все с суффиксом `.resolved-*`
  (последний — `ALARM-signal-2026-09.resolved-01.09`). Почтовый сторож новых писем не слал
  с 01.09 16:53.
- **Тики живы**: `tick-heartbeat` = 1788414901 → 2026-09-03 08:55:01, возраст на момент
  проверки **2,3 минуты** при пороге сторожа 65 минут.
- **wip-push работает**: последняя запись 03.09 08:37:05; `origin/master..master` и
  `mirror/master..master` пусты — незапушенного нет.
- **Диск**: свободно **11 ГБ (82%)**, как и сутки назад. Порога, останавливающего контур, нет.
- **Маржа по книге** (по замеру `r33build/live/margins_live.json` от 18.08): начальная
  2×35 050,37 + 6×3 506,29 = **91 138,48** = 9,2% счёта; поддерживающая **65 302,06** = 6,6%.
  Запас большой.
- В журнале сессии №16 снова присутствует `Error 322 … Maximum number of account summary
  requests exceeded` — это одиннадцатое появление той же строки за пилот, NAV и позиции при
  этом получены (сессия отработала). Известный безвредный шум шлюза, новых признаков нет;
  в текущий scope разбор не входит и правка не предлагается.

## Письмо о событии

Месячный переход — событие из закрытого списка, письмо обязательно даже при отсутствии
сделок. Отправлено письмо с темой-выжимкой «СОБЫТИЕ: месячный переход прошёл штатно —
сигнал сентября (1,0) тот же, сделок нет, книга 26/0, плечо 1,0065».

## Вердикт

Месячный переход исполнен второй сессией сентября (02.09, сессия №16) в соответствии с
конвенцией: строка `2026-09-30 = (1,0)` применена, ноги менять было не нужно, заявок ноль,
книга 26/0 сошлась с фактической позицией брокера {MESZ26: 6, ESZ26: 2}, плечо закрытия
1,0065 при потолке 2,00, замыкание, копии и WORM за 02.09 сняты. Отклонений нет, карточка
дефекта не оформляется.

**Изменений не требуется. Paper pilot продолжается.**

Ближайшие даты графика: **04.09 — перевод бумажного пилота на маршрут Е** (утром
письмо-напоминание, безголовый исполнитель в crontab в 16:51, после события снять строку
«51 16 4 9 *»); 05.09 — отчёт о переводе и замер фактической стоимости заёмных средств на Е;
08.09 — проверка, что Labor Day 07.09 пропущен корректно; 15.09 — старение замера маржи
(протухает 23.09).
