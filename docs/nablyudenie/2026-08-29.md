# Наблюдение paper pilot — 29.08.2026 (суббота)

Сессия запущена автоматически (`~/.addfut/nablyudatel.log`: «2026-08-29 10:37:36 наблюдатель:
старт»). Режим — только чтение и доклад; production-код, книга, `~/.addfut`, cron и норматив
не менялись.

## Задача на сегодня

По графику наблюдения (PROJECT-STATE §0а) на 29.08 особой задачи НЕТ — ближайшая назначена на
31.08 (состояние перед замыканием месяца). Выполнен быстрый статус по регламенту: тревоги,
тики автопилота за последние сутки, книга и `last_session`, wip-push, место на диске.
Суббота — торгового дня нет, сделок и не ожидается.

## Факты

### 1. Тревоги — нет ни одной активной
В `~/.addfut/` четыре файла ALARM*, ВСЕ с суффиксом `.resolved-*`:
`ALARM-backup-2026-08-18.resolved-19.08`, `ALARM-env-2026-08-26.resolved-27.08`,
`ALARM-route-2026-08-15.resolved-16.08`, `ALARM-route-2026-08-16.resolved-16.08`.
Файла активной тревоги (без `.resolved`) нет — автопилот не заперт.

### 2. Тики автопилота живы
`tick-heartbeat` = 1787988901 → 2026-08-29 10:35:01, возраст на момент проверки **3,0 минуты**.
Cron-тик работает.

### 3. Последние сутки журнала — штатные (13 значимых строк за 28–29.08, без rclone-NOTICE)
- 28.08 16:45:05 «шлюз не отвечает — запускаю» → 16:45:25 «шлюз поднят, API отвечает» — штатный
  автозапуск шлюза;
- 28.08 торговый прогон: счёт DUR833748, NAV 997 987,14; позиции `{'MESZ26': 6.0, 'ESZ26': 2.0}`;
  сессия №13; решение — плечо 1,0045, книга 26/0, **заявок нет**; режим ЖИВОЙ;
- 28.08 16:46:02 «торговля 2026-08-28: ок»;
- замыкание: NLV закрытия 994 888,13; плечо закрытия **1,0053** — помечено как триггер капа
  следующей сессии;
- 29.08 00:05:28 снимок+WORM: `addfut-2026-08-28-160528.tgz`, якорь `worm-2026-08-28.txt` (в HEAD);
  зеркало ok, github ok, drive ok; 00:06:29 «замыкание 2026-08-28: ок (копия состояния снята)».
Ошибок в журнале за сутки нет. Строка `Error 322 ... account summary requests exceeded` —
известное поведение шлюза при переподписке, прогон завершился успешно (сводка счёта получена).

### 4. Книга и отметки дня читаются, состояние согласовано
`book-F.json` читается, digest присутствует:
`session_no` = 13, `n_e` = 26, `n_b` = 0, `unit_is_mes` = true, `es_held` = 2,
`ser_a` = `ser_b` = `Z26`, `roll_pending` = false, `close_provisional` = **false**,
`prev_close_lev` = 1,0052939318916188, `last_session` = **2026-08-28**, `d_fix` = 7,9291443970797975,
digest `912bcbef…01d2e`.
Отметки за последний торговый день на месте обе: `traded-2026-08-28` и `closed-2026-08-28`.
`last_session` совпадает с последним замкнутым днём — незавершённых операций нет.

### 5. Резервирование работает
`wip-push.log`, последняя запись 2026-08-29 10:37:01 «mirror догнал master (+2 коммитов)».
Незапушенных коммитов нет ни в origin, ни в mirror (`git log origin/master..master` и
`mirror/master..master` пусты). Рабочее дерево чистое (`git status --porcelain` пуст).
Якорь `anchors/worm-2026-08-28.txt` в репозитории.

### 6. Диск
`/dev/vda1`: 58G всего, 42G занято, **16G свободно, 74%**. Журнал автопилота 111,0 КБ
(793 строки) — роста, требующего внимания, нет.

## Вердикт

Отклонений от норматива не обнаружено. Карточка дефекта не оформляется.

**Изменений не требуется. Paper pilot продолжается.**

Следующая плановая проверка по графику — 31.08 утром: состояние перед замыканием месяца.
