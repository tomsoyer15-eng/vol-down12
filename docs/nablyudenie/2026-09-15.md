# Наблюдение paper pilot — 15.09.2026 (вторник)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись.

## Задача на сегодня

По графику (PROJECT-STATE §0а): **«15.09 утро — предупреждение о старении замера маржи
(протухает 23.09)»**. Плюс быстрый статус и обязательная строка про пин журнала МР. Scope
не расширялся.

---

## 1. НАЗНАЧЕННАЯ ПРОВЕРКА: старение замера маржи

### Что показывает контур

Предупреждение выписывается раз в чикагские сутки и идёт без пропусков:

```
2026-09-13 08:05:02 | ВНИМАНИЕ: … ему 26 дней, до предела 35 осталось 9 …
2026-09-14 08:05:02 | ВНИМАНИЕ: … ему 27 дней, до предела 35 осталось 8 …
2026-09-15 08:05:03 | ВНИМАНИЕ: ADDFUT-VERDICT NO замер маржи от 2026-08-18: ему 28 дней,
                       до предела 35 осталось 7 — после этого ВЫПУСК ПАКЕТА НЕВОЗМОЖЕН.
                       Освежить first_connect утром, пока whatIf отдаёт маржу.
```

Файлы `WARN-margins-2026-09-11 … -09-15.txt` на месте, счётчик идёт правильно (24→28).
Замер: `r33build/live/margins_live.json`, `_meta.date` = **2026-08-18 08:54 UTC**, счёт
DUR833748, шесть серий ES/MES/ZN с con_id. Файл с 18.08 не переписывался (mtime 18.08 11:54).

### Край срока проверен исполнением

Чистая функция `transition._meta_age_ok` (машинное состояние не трогается), оба плеча
достигнуты:

```
_meta_age_ok(2026-08-18) сегодня -> (True, 28)
возраст 35 -> (True, 35)      возраст 36 -> (False, 36)
последний годный день -> 2026-09-22
первый день отказа    -> 2026-09-23
```

Дата предупреждения совпадает с фактическим краем: **22.09 — последний годный день,
23.09 — первый день отказа**. Осталось 7 дней.

### Что именно встанет 23.09 — и что НЕ встанет

Прослежено по коду, кто читает замер:

* **`selfcheck_v192.py`** (ворота выпуска) — подставляет машинные `ADDFUT_MARGINS/REGISTRY/
  ACCOUNT` и зовёт `T._live_margins()`; Incident ловится широким `except`, срок
  `_MRG_DEADLINE` = 2026-08-20 давно истёк, значит `chk(False)` → ИТОГ: ПРОВАЛ →
  `release.py` «ВЫПУСК НЕ СОСТОЯЛСЯ». **Выпуск пакета с 23.09 невозможен** — подтверждено.
* **`transition.preflight_margin_orders`** (строка 429) зовёт `_live_margins()` напрямую:
  с 23.09 **переход Ф↔Е тоже заблокирован** (Incident «замеру 36 дней — устарел»).
  Предупреждение автопилота про это не говорит, оно называет только выпуск; лечение то же
  самое и срок тот же, поэтому это уточнение к тексту, а не дефект.
* **`ib_broker._release_by_measure`** (строка 1242) — Incident перехвачен, ветка честно
  возвращает False с причиной «замер маржи недоступен», решение падает обратно на порог
  О-3-Е. Аварийный выход не запирается молча.
* **Ежедневная торговля НЕ затронута:** `session.py`, `daily.py`, `margin_check.py` замер
  не читают вовсе (грепом по `margins_live|_live_margins`). `worm_anchor.py` читает файл
  только чтобы сверить ПОКОЛЕНИЕ пары «реестр ↔ замер» (серии и con_id) и по решению
  45-го круга расхождение лишь ЗАВЕРЯЕТ, а не роняет замыкание; возраст он не смотрит.

Итог назначенной проверки: **предупреждение работает как задумано, счётчик верен, срок
подтверждён кодом**. Действие (перегенерация `first_connect` утром, пока whatIf отдаёт
маржу) — за заказчиком; сам не трогаю, режим только на чтение.

### ОТКЛОНЕНИЕ: напоминание на 21.09 не заведено ни одним из двух каналов

В §0а записано «до 23.09 — штатная перегенерация first_connect (по добру заказчика;
**21.09 напоминание**)». Проверено:

* ждущих процессов `napominanie_v_srok.py` нет (`ps -eo pid,etime,args`, выборка по имени
  файла, не pgrep);
* второй канал — файл `~/.addfut/napominanie-<дата>.txt`, который уходит с пульсом в 09:07;
  в каталоге лежат только `napominanie-2026-09-11.txt` и `napominanie-2026-09-14.txt`,
  **файла на 2026-09-21 нет**.

То есть автоматического напоминания к сроку сейчас НЕ существует; страховкой остаются
ежедневная строка WARN в журнале автопилота (её никто не шлёт письмом) и понедельничная
сводка 21.09, то есть за один-два дня до края. Завести напоминание сам не могу: это запись
в `~/.addfut`, а сессия — только чтение. Записано как отклонение, письмо отправлено.

---

## 2. Пин журнала МР — БОЕВОЙ

```
cd r33build && ../.venv/bin/python -c "import mr_engine as M; print(M._read_deploy()['journal'])"
-> /home/alex/claude-projects/vol-down12/r33build/mr_journal.csv
```
Путь боевой, не временный. Расхождения нет.

---

## 3. Быстрый статус контура

**Сессия №23 (пн 14.09) прошла штатно.** Шлюз поднимался сам («шлюз не отвечает — запускаю»
16:45:04 → «шлюз поднят, API отвечает» 16:45:25), NAV 980 347,96, позиции
`{'MESZ26': 6, 'ESZ26': 2}`, решение — плечо 1,0135, книга 26/0, **заявок нет**;
«торговля 2026-09-14: ок». Замыкание: NLV закрытия **983 955,46**, плечо закрытия
**1,0053**. Снимок+WORM `addfut-2026-09-14-160529.tgz`, якорь `worm-2026-09-14.txt`,
`зеркало ok / github ok / drive ok`, «замыкание 2026-09-14: ок».

**Тревоги:** активных **0** (файлов `ALARM*.txt` без суффикса нет; все шесть `.resolved-*`,
свежайший `ALARM-signal-2026-09.resolved-01.09`). `WARN-margins-*` — предупреждение, не
тревога.

**Тик жив:** `tick-heartbeat` 15.09 08:55:01, возраст **145 с** при пороге почтового
сторожа 65 мин.

**Книга** `book-F.json` (15.09 00:05:28): маршрут **F**, `session_no` = **23**,
`last_session` = **2026-09-14**, `close_provisional` = false, `roll_pending` = false,
`n_e` = **26**, `n_b` = **0**, `unit_is_mes` = true, `es_held` = **2**, `ser_a` = `ser_b` =
**Z26**, `prev_close_lev` = **1,0052731451889092**, `prev_st_eq` = true, `prev_st_bd` =
false, `d_fix` = 7,9291, digest `226c81da…`. Позиция брокера по вчерашнему тику —
2 ESZ26 + 6 MESZ26 = 2×10+6 = **26 MES** = `n_e`; ноги Б нет ни там, ни там. Расхождений нет.

**Календарь сходится:** `feed.prev_session(2026-09-15)` = **2026-09-14** = `last_session`;
`feed.prev_session(2026-09-16)` = 2026-09-15. Отметки `traded-2026-09-14` и
`closed-2026-09-14` на месте; сегодня — сессия №24 днём.

**Журнал §7** `journal-F.csv`: последняя строка
`2026-09-14 … ИТОГ … 980347.96 … итог сессии 23: строк 0`, цепочка хэшей непрерывна
(`0491e365…` → `47672f2f…`). **Сделок с ролла 26.08 не было ни одной.**

**Сигнал:** строка месяца действия `2026-09-30` = **(1, 0)** — нога А включена, нога Б
выключена. Сайдкар уровней заканчивается августом (92,74 / 767,05), поколение одно.
`sigup-2026-09` на месте.

**Маржа по книге** (замер 18.08): начальная 2×35 050,37 + 6×3 506,29 = **91 138,48** =
**9,26%** счёта; поддерживающая **65 302,06** = **6,64%**, запас к ней **15,1×**. Очень
большой.

**Почта:** пульс уходил 12, 13 и 14.09 («ПУЛЬС 14.09.2026: жив — NLV 987 687,13, тревог
нет»), сегодняшний в 09:07 впереди. В `pochta-storozh.log` новых записей с 04.09 17:03 нет.
Напоминание о переводе на Е ушло 14.09 08:00 (код 0); новых ожидающих напоминаний нет.

**Копии и диск:** `wip-push` работает каждые полчаса без пропусков, последний срез
**15.09 08:37:04** (2 файла на Drive). `~/.addfut-backups` и `~/state-mirror` — обе с
`addfut-2026-09-14-160529.tgz`, 23 архива. Якорь `anchors/worm-2026-09-14.txt`.
Диск: свободно **10 ГБ (занято 83%)** — против 9,1 ГБ (85%) вчера, запас вырос.

**Git:** незапушенных коммитов **0**, HEAD `61e0182`. Незакоммиченными висят только
`docs/ADD-FUT-STATUS.pdf/.txt` — рабочий вывод `status_stranitsa.py`, не состояние.
Действующий выпуск — **2ece058c** (11.09).

**Маршрут:** `~/.addfut/route.txt` = **F**. Перевод на Е по-прежнему не состоялся
(4 попытки: 04.09, 08.09, 11.09, 14.09).

**H19:** новых завершённых месяцев нет (записан 1, за 2026-08) — журнал ведётся, задачи нет.

---

## Письма

* **Событий из закрытого списка п.3б не было:** 14.09 — обычная сессия без сделок, ролла,
  перехода, замыкания месяца, смены упаковки, восстановления и обслуживания. Письма о
  событии нет.
* **Письмо об отклонении отправлено** — незаведённое напоминание на 21.09 при сроке замера
  маржи 22.09.
* Понедельничной сводки сегодня нет: сегодня вторник, сводка ушла вчера.

## Вердикт

**Изменений в production-коде не требуется. Paper pilot продолжается.**

Назначенная проверка пройдена: предупреждение о старении замера маржи работает штатно,
край срока подтверждён исполнением (22.09 годен, 23.09 отказ), встают только ВЫПУСК и
ПЕРЕХОД Ф↔Е — ежедневная торговля, замыкание и WORM-якорь от возраста замера не зависят.
Контур в норме: тревог 0, тик 145 с, книга 26/0 сходится с брокером, журнал §7 цел, сделок
нет, плечо закрытия 1,0053 при потолке 2,00, маржа 9,26% счёта, копии и пуш свежие,
диск 10 ГБ, пин журнала МР боевой. Дефектов классов A–F не обнаружено.

**На заказчика (не на меня):**
1. **Перегенерировать `first_connect` утром до 22.09 включительно** — иначе с 23.09 встанут
   выпуск пакета и переход Ф↔Е (торговля не встанет). Осталось 7 дней.
2. **Напоминание на 21.09 не заведено** ни ждущим процессом, ни файлом
   `~/.addfut/napominanie-2026-09-21.txt` — сам завести не могу (запись в `~/.addfut`).
3. Перевод на маршрут Е остаётся невыполненным после четырёх попыток; route = F.
