# Наблюдение paper pilot — 19.09.2026 (суббота)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись. Четвёртый день
пилота на маршруте **Е**. Суббота — торговой сессии нет, докладывается пятничная (№27).

## Задача на сегодня

По графику (PROJECT-STATE §0а) особой задачи на 19.09 нет: ближайшие даты — **21.09**
напоминание, **до 22.09** перегенерация `first_connect`, затем **20.11** расчёт даты
ноябрьского ролла. Значит сегодня — быстрый статус и обязательная строка про пин журнала
МР. Scope не расширялся.

---

## 1. Пин журнала МР — БОЕВОЙ

```
cd r33build && ../.venv/bin/python -c "import mr_engine as M; print(M._read_deploy()['journal'])"
-> /home/alex/claude-projects/vol-down12/r33build/mr_journal.csv
```
Путь боевой, не временный. Расхождения нет.

---

## 2. ОТКЛОНЕНИЕ ПРОДОЛЖАЕТСЯ: Drive не переавторизован — отказ 1 из 3

Вчерашний прогноз подтвердился фактом, а не расчётом. Замыкание 18.09 в 18:35 прошло, но
drive-плечо отказало, и счётчик впервые поднялся:

```
2026-09-18 18:35:29 зеркало ok
2026-09-18 18:35:31 github ok
2026/09/18 18:35:31 CRITICAL: ... couldn't fetch token: invalid_grant: maybe token expired?
2026-09-18 18:35:49 drive ОШИБКА выгрузки — копия вне машины НЕ обновлена
2026-09-18 18:35:49 +03 | внешняя выгрузка копий не удалась (1 подряд)
2026-09-18 18:35:49 +03 | замыкание 2026-09-18: ок (копия состояния снята)
```
`~/.addfut/push-fails` = **1** (вчера файла не было вовсе). Страховочный `wip_push.sh`
отказывает каждые полчаса по той же причине: `wip: ОТКАЗ выгрузки на Drive (2 файлов НЕ
покрыты)` — последняя запись 19.09 08:37.

**Остаток срока — два замыкания:**

| замыкание | что будет | `push-fails` |
|---|---|---|
| 18.09 пт — **уже было** | drive ОШИБКА | **1** |
| 21.09 пн 18:35 | drive ОШИБКА | 2 |
| **22.09 вт 18:35** | **ALARM-push -> автопилот остановлен** | **3** |

Суббота и воскресенье счётчик не двигают — замыканий нет. Сессия **23.09 не состоится**,
если токен не восстановить **до вечера вторника 22.09**.

**Лечение прежнее и только в браузере заказчика:**
`~/bin/rclone config reconnect vol-down12-drive:`. Безголового пути нет — сервер Google
отвергает обновление по refresh-токену (проверено живой пробой вчера, сегодня повторно
подтверждено отказами в журналах). Сам сделать не могу: это живая авторизация в браузере
(правило 13).

**Карточки дефекта по-прежнему нет.** Классы A–F не возникают: позиция, заявки и торговая
арифметика не затронуты; остановка на третьем отказе — предусмотренный нормативом
fail-closed (пятнадцатый круг, №7), а не необоснованная остановка класса D; локальные
точки восстановления, git-зеркало и GitHub свежие. Это ограничение версии, уже записанное
в BACKLOG 22б (OAuth-приложение rclone в статусе Google «Testing», токен живёт 7 суток).
Production-код не трогаю.

---

## 3. Сессия №27 (пятница 18.09) — штатно, заявок нет

```
счёт DUR833748: NAV 980,824.25; позиции {'CSPX': 1219.0}; сессия №27
вход: CSPX=(823.04, '2026-09-17'), CBU0=(150.68, '2026-09-17'), signal_date=2026-09-30, signal_age_d=18
решение: плечо 1.0208, книга -/-
  причина: БУМАЖНЫЙ ЭТАП: порог §8 относится к маршруту Ф и к маршруту Е не применяется
заявки: нет
2026-09-18 16:46:00 +03 | торговля 2026-09-18: ок
```
Сверка с брокером сошлась: позиция `{'CSPX': 1219.0}` = книга (`n_eq` 1219, `n_bd` 0).
Отметки `traded-2026-09-18` (16:46) и `closed-2026-09-18` (18:35) на месте.

**Замыкание 18.09:**
```
замкнуто: NLV закрытия 980,604.84, вход {'CSPX': (822.74, '2026-09-18'), 'CBU0': (150.07, '2026-09-18')}
плечо закрытия 1.0228 — триггер капа следующей сессии
```
Плечо **1,0228** при потолке **2,00**. Строка «триггер капа следующей сессии» — штатная
подпись `prev_close_lev` (`session.py:264`), она печатается каждым замыканием, а не признак
срабатывания капа.

**Счёт:** NLV закрытия 980 604,84 против 980 880,06 накануне — **−275,22 (−0,03%)** за
день, движение рынка, сделок не было. С начала пилота (1 000 000) — **−19 395,16 (−1,94%)**.

---

## 4. Быстрый статус

**Маршрут:** `~/.addfut/route.txt` = **E**.

**Книга** прочитана штатным загрузчиком `state.load` — исключения нет, значит хэш состояния
сошёлся: маршрут **E**, сессия **№27**, `n_eq` = **1219,0** долей CSPX, `n_bd` = **0**,
`roll_pending` = false, `prev_close_lev` = **1,0227565876586946**, `prev_st_eq` = true,
`prev_st_bd` = false, `last_session` = **2026-09-18**, `close_provisional` = false.

**Календарь сходится:** `feed.prev_session(2026-09-19)` = **2026-09-18** = `last_session`.
`feed.common_window(2026-09-19)` честно поднимает `FeedError: 2026-09-19 — выходной: общего
окна LSE/CME нет` — сегодня сессии быть и не должно. Ближайшая торговая сессия —
**понедельник 21.09**, окно **02:00–10:30 Чикаго**.

**Тревоги:** активных **0** (все шесть `ALARM*` с суффиксом `.resolved-*`). `autopilot.STOP`
нет. `WARN-margins-2026-09-19` — предупреждение, не тревога.

**Тик жив:** `tick-heartbeat` 19.09 08:55, возраст **~120 с** при пороге почтового сторожа
65 мин.

**Журнал §7.** `journal-E.csv`: четыре строки, добавилась
`2026-09-18 … ИТОГ … nav 982 823,43 … итог сессии 27: строк 0`; цепочка хэшей непрерывна
(`prev_hash` строки 18.09 = `row_hash` строки 17.09, `3c0ae284…`). Сделок не было — строк
исполнений нет, и это верно.

**WORM-якорь 18.09** (`anchors/worm-2026-09-18.txt`, коммит `02471b1`) сверен вручную:
digest книги `be32eaf3…` совпадает с `book-E.json`, корень цепочки журнала `b644e0ab…`
совпадает с последней строкой `journal-E.csv`, архив `addfut-2026-09-18-103529.tgz`.

**Сигнал:** строка месяца действия `2026-09-30` = **(1, 0)** — нога А включена, нога Б
выключена, позиция ей соответствует; возраст сигнала 19 дней. Сайдкар уровней
заканчивается августом (92,74 / 767,05), поколение одно. Ближайший месячный переход —
вторая сессия октября.

**Маржа:** новых замеров со вчера нет; действуют цифры предполёта перехода — начальная
**287 316 ≈ 29% счёта**, поддерживающая 249 840, свободные средства 688 669, запас
**3,90×**.

**Замер маржи стареет штатно:** `WARN-margins-2026-09-19.txt` — «ему **32 дня**, до предела
35 осталось **3**». Край прежний: **22.09 — последний годный день, 23.09 — первый день
отказа**. Встанут выпуск пакета и переход Ф↔Е; ежедневная торговля, замыкание и WORM-якорь
от возраста замера не зависят.

**Два срока по-прежнему сошлись на 22.09:** последний годный день замера маржи — и третье
подряд неудачное замыкание, дающее ALARM-push. Разные механизмы, одна дата.

**Копии и диск:** `~/.addfut-backups` и `~/state-mirror` — обе с
`addfut-2026-09-18-103529.tgz` (локальные точки восстановления свежие). Внешняя копия на
Drive заморожена на состоянии 17.09 19:07 — см. §2. Диск: свободно **13 ГБ (занято 79%)**.

**Git:** незапушенных коммитов **0**, HEAD `02471b1`. Незакоммиченными висят только
`docs/ADD-FUT-STATUS.pdf/.txt` — рабочий вывод статус-страницы, не состояние.

**Выпуск:** действующий пакет — **45de3d90** от 15.09, изменений нет.

**H19:** новых завершённых месяцев нет (записан 1, за 2026-08).

**Процессы (недельный счётчик, аптайм 2 сут 16 ч):** зависших тиков `autopilot.sh tick` —
**2** (pid 3013, 64,2 ч — с самой загрузки; pid 191094, 16,2 ч — от вчерашней торговой
сессии). Вчера был **1**, то есть за сутки прибавился один, как и предполагала карточка
16.09. Наших JVM шлюза — **2** (440 + 565 МБ); третья JVM в `ps` принадлежит соседнему
проекту (`.ibc-trend2.ini`) и к нашему контуру отношения не имеет. Доступно памяти
**4 837 МБ** (вчера 5 163), своп занят 0. Порог переоткрытия карточки — четыре тика при
свободной памяти менее ~2 ГБ (docs/nablyudenie/2026-09-16-processy.md §5) — **не достигнут
ни по одной из двух величин**. Запись, счётчик, перепроверка; правки кода нет.

**Ждущих процессов напоминания — 0**, файла `~/.addfut/napominanie-2026-09-21.txt` нет:
напоминание на 21.09 по-прежнему не заведено ни одним из двух каналов. Письмо об этом
уходило 14.09, не повторяю. (Счёт сделан питоном с исключением собственного PID — `ps` по
тексту команды даёт ложное попадание на обёртку, грабли среды из CLAUDE.md.)

**Известный шум IBKR, не дефект:** при подъёме шлюза журнал печатает
`Error 322 … Maximum number of account summary requests exceeded` (четыре строки 18.09).
Строка появляется при каждом запуске шлюза с 19.08, и каждый раз NAV и позиции после неё
прочитаны успешно, включая вчерашний тик.

---

## 5. Открытое (на заказчика, не на меня)

1. **СРОЧНО, осталось два замыкания: переавторизовать Drive до вечера вторника 22.09** —
   `~/bin/rclone config reconnect vol-down12-drive:`. Счётчик уже 1 из 3.
2. **Перегенерировать `first_connect` утром до 22.09 включительно** — осталось **3 дня**;
   иначе с 23.09 встанут выпуск пакета и возможный возврат Е→Ф.
3. **Напоминание на 21.09 не заведено** ни процессом, ни файлом. Завести не могу — это
   запись в `~/.addfut`.
4. Обновить в PROJECT-STATE §0а базлайн (фактический — **45de3d90** от 15.09, в тексте
   стоит 2c6e76ca) и статус карточки класса F про `transition_id` (закрыта правкой 15.09).
5. Наблюдение за фактической стоимостью заёмных средств на Е продолжается.

## Письма

* **Письмо отправляется по пункту 5 задания: вердикт нештатный.** Вчерашнее письмо несло
  прогноз, сегодняшнее — факт: первый отказ состоялся, счётчик 1 из 3, до остановки два
  замыкания, и впереди выходные, когда наблюдатель предупредить уже не успеет раньше
  понедельника.
* Событий из закрытого списка п.3б со вчерашнего отчёта **нет**: сделок нет, ролла нет,
  месячного перехода нет, замыкания месяца нет, смены упаковки нет, восстановления нет.
* Сегодня суббота — еженедельной сводки нет (она по понедельникам).

## Вердикт

**Вердикт НЕ штатный — одно отклонение, правки кода не требует.**

Токен Google Drive по-прежнему отозван. Прогноз вчерашнего отчёта подтверждён фактом:
замыкание 18.09 дало первый отказ выгрузки, `push-fails` = 1. Остаются два замыкания —
21.09 (станет 2) и 22.09 (станет 3, ALARM-push, автопилот остановлен). Это предусмотренный
нормативом fail-closed, а не дефект: классов A–F нет, карточка не заводится, production-код
не трогаю. Нужна переавторизация в браузере заказчика.

**Сам контур в норме:** активных тревог 0, сессия №27 (18.09) прошла штатно без заявок,
книга 1219/0 сходится с позицией брокера, плечо закрытия 1,0228 при потолке 2,00, NLV
закрытия 980 604,84 (−0,03% за день, −1,94% с начала пилота), журнал §7 цел и сверен с
WORM-якорем, тик свежий, календарь сходится (суббота честно без окна), пин журнала МР
боевой, локальные точки восстановления и GitHub свежие, диск 13 ГБ. Счётчик зависших тиков
2 из 4 при 4,8 ГБ свободной памяти — ниже порога переоткрытия карточки.

**Paper pilot продолжается — но без переавторизации Drive до вечера 22.09 он остановится.**
