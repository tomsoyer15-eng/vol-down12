# Наблюдение paper pilot — 12.09.2026 (суббота)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись.

## Задача на сегодня

По графику (PROJECT-STATE §0а) на 12.09 особой задачи нет: суббота докладывает пятничную
сессию, замкнутую ночью. Значит — быстрый статус (тревоги, тики, книга и `last_session`,
wip-push, диск) плюс обязательная строка про пин журнала МР. Scope не расширялся.

## 1. Пятничная сессия 11.09 — штатная, сделок нет

Сессия **№22**, счёт DUR833748, NAV **988 492,13**, позиции `{ESZ26: 2, MESZ26: 6}`.
Вход: `es_close=(7664.5, '2026-09-10')`, `spy_close=757.83`, базис **+1,137%**,
`yield_10y_pct=4.944`, `dref=7.8152`, `signal_date=2026-09-30` (возраст 11 дней),
серия Z26. Решение — плечо **0,9966**, книга **26/0**, **заявок нет**; причина:
«БУМАЖНЫЙ ЭТАП: порог §8 (3 000 000) не применён при NLV 988 492; механический пол
соблюдён». Торговля отработала в 16:45:41, шлюз поднимать не понадобилось («шлюз прогрет,
проба 1»).

Замыкание: NLV закрытия **987 687,13**, вход `spy_close=764.29`, `es_close=7729.25`,
`yield_10y_pct=4.975`, `dref=7.8038`; плечо закрытия **1,0060** при потолке 2,00.
Ночью 12.09 00:05–00:06: снимок+WORM `addfut-2026-09-11-160529.tgz`, якорь
`worm-2026-09-11.txt`, `зеркало ok`, `github ok`, **`drive ok`** — внешняя выгрузка снова
проходит третьи сутки подряд, счётчика `push-fails` нет (=0).

## 2. Перевод на маршрут Е 11.09 — ТРЕТЬЯ ПОПЫТКА НЕ СОСТОЯЛАСЬ, контур закрылся правильно

`~/.addfut/route.txt` = **F**. Файл попытки `~/.addfut/perehod-2026-09-11.json` (11.09 16:31):

```
"postponed": 1, "done": [], "executed_usd": 0.0, "order_ids": [],
"snapshot": {"MESZ26": 6.0, "ESZ26": 2.0},
"log": ["preview_отказ", "CSPX: шлюз не вернул начальную маржу (пустой ответ whatIf) —
        предпросмотр недоказуем"]
```

То есть **ни одной заявки не ушло, позиция не тронута, книга осталась F-шной** —
предпросмотр не смог доказать маржу и переход был отложен целиком. Это ровно то поведение,
которого норматив требует от недоказуемого предпросмотра (fail-closed), а не дефект.

Причина найдена и закрыта заказчиком в живой сессии 11.09 вечером: IBKR не принимает
**дробные** заявки по фондам через API; предпросмотр теперь округляет до целых долей
(коммит `53d6368`, выпуск `2ece058c` — `releases/paket-r33-20260911-200121-2ece058c.zip`).
Отложенное напоминание на **четвёртую попытку 14.09 (понедельник) 08:00** уже стоит в
очереди (`napominanie-v-srok.log`: «жду до 2026-09-14 08:00 (57,0 ч)»), текст готов
(`~/.addfut/napominanie-2026-09-14.txt`). Акты заказчика в журнале МР от 04.09 не менялись,
переделывать их не нужно.

## 3. Пин журнала МР — БОЕВОЙ

```
cd r33build && ../.venv/bin/python -c "import mr_engine as M; print(M._read_deploy()['journal'])"
-> /home/alex/claude-projects/vol-down12/r33build/mr_journal.csv
```
Путь боевой, не временный. Расхождения нет. В журнале те же 4 строки от 04.09:
`SWITCH_SIGNAL E|2026-09-04#1`, `OWNER_APPROVE`, `GRANULARITY_EXCEPTION 387 026` и `450 000`.

## 4. НОВОЕ ЗА СУТКИ: пошло предупреждение о старении замера маржи (штатное, не тревога)

Впервые появились файлы `WARN-margins-2026-09-11.txt` (12.09 03:05) и
`WARN-margins-2026-09-12.txt` (12.09 08:05):

```
ADDFUT-VERDICT NO замер маржи от 2026-08-18: ему 25 дней, до предела 35 осталось 10 —
после этого ВЫПУСК ПАКЕТА НЕВОЗМОЖЕН. Освежить first_connect утром, пока whatIf отдаёт маржу.
```

Это **предупреждение по построению**, а не отклонение: `r33build/live/autopilot.sh:1045-1066`
печатает его раз в чикагские сутки, как только `left = 35 - age <= 10`; замер снят 18.08,
возраст 25 дней — порог достигнут именно сегодня. Торговле возраст замера не мешает,
контур не останавливается (что и видно: тик жив, сессия 11.09 прошла). Мешает он только
**выпуску пакета — с 23.09**. График §0а этот момент предусматривал («15.09 утро —
предупреждение о старении замера маржи (протухает 23.09)»; «до 23.09 — штатная
перегенерация first_connect, 21.09 напоминание») — механизм сработал на три дня раньше
наблюдательной даты просто потому, что его порог 25 дней, а не 28. **Действие остаётся за
заказчиком, ничего не чиню.**

## 5. Остальной статус

**Тревоги:** активных **0** — файлов `ALARM*.txt` нет, все шесть с суффиксом `.resolved-*`,
свежайший `ALARM-signal-2026-09.resolved-01.09`. `WARN-margins-*` — предупреждение, не
тревога (см. п.4).

**Тик жив:** `tick-heartbeat` = 12.09 **08:55:02**, возраст **242 с** при пороге почтового
сторожа 65 мин. Отметки дня на месте: `traded-2026-09-11`, `closed-2026-09-11`.

**Книга** `book-F.json` (12.09 00:05): маршрут **F**, `session_no` = **22**,
`last_session` = **2026-09-11**, `close_provisional` = false, `roll_pending` = false,
`n_e` = **26**, `n_b` = **0**, `unit_is_mes` = true, `es_held` = **2**,
`ser_a` = `ser_b` = **Z26**, `prev_close_lev` = **1,0059632952795488**, `prev_st_eq` = true,
`prev_st_bd` = false, `d_fix` = 7,9291, digest `003c9699…`.
Сверка с брокером по последним живым данным (11.09): 2×10 + 6 = **26 MES** = `n_e`; ноги Б
нет ни в книге, ни у брокера. **Расхождений нет.**

**Стык календаря сходится:** `feed.prev_session(2026-09-12)` = **2026-09-11** =
`last_session` в книге. Сегодня суббота — сессии нет; следующая сессия **понедельник
14.09** (`feed.prev_session(2026-09-15)` = 2026-09-14).

**Журнал §7** `journal-F.csv`: последняя строка
`2026-09-11 … ИТОГ … 988492.13 … итог сессии 22: строк 0`, цепочка хэшей непрерывна
(`226560cf…` → `0491e365…`). Сделок за неделю нет вовсе.

**Сигнал:** строка месяца действия `2026-09-30` = **(1, 0)** — нога А включена, нога Б
выключена. Сайдкар уровней заканчивается августом (92,74 / 767,05), поколение одно.
Отметка `sigup-2026-09` на месте.

**Маржа по книге** (замер `margins_live.json` от 18.08 08:54 UTC): начальная
2×35 050,37 + 6×3 506,29 = **91 138,48** = **9,23%** счёта; поддерживающая
2×25 116,17 + 6×2 511,62 = **65 302,06** = **6,61%**. Запас очень большой.

**Почта:** пульс за 11.09 ушёл («ПУЛЬС 11.09.2026: жив — NLV 980 143,55, тревог нет»),
напоминание о переводе ушло 11.09 в 08:00 (код 0); сегодняшний пульс в 09:07 впереди.
Новых записей в `pochta-storozh.log` с 04.09 17:03 нет.

**Копии и диск:** `wip-push` работает каждые полчаса, последний срез **12.09 08:37:05**
(2 файла на Drive). Точки восстановления `~/.addfut-backups` и `~/state-mirror` — обе с
`addfut-2026-09-11-160529.tgz`; якорь `anchors/worm-2026-09-11.txt`. Диск: свободно
**9,8 ГБ (занято 83%)** — та же полка.

**Git:** незапушенных коммитов **0**, HEAD `fb918a3`. Незакоммиченными висят только
`docs/ADD-FUT-STATUS.pdf/.txt` — рабочий вывод `status_stranitsa.py`, не состояние.
Действующий выпуск — **2ece058c** (11.09).

**H19:** новых завершённых месяцев нет (записан 1, за 2026-08) — журнал ведётся, задачи нет.

## Письма

Событий из закрытого списка п.3б (исполненные заявки, ролл, месячный переход, замыкание
месяца, смена упаковки, восстановление после сбоя контура, обслуживание) со времени отчёта
11.09 не было: 11.09 — обычная сессия без заявок, замыкание штатное. Сорванная третья
попытка перевода на Е произошла **в живой сессии с заказчиком**, им же в тот вечер
разобрана, починена и выпущена, а напоминание на 14.09 уже поставлено в очередь — письмо
об этом было бы пересказом того, что он сделал сам. Предупреждение о замере маржи —
штатное и предусмотрено графиком, до предела 10 дней. Суббота — еженедельная сводка не
положена (понедельник). **Письмо не отправляется.**

## Вердикт

**Изменений не требуется. Paper pilot продолжается.**

Контур в норме: активных тревог 0, пятничная сессия №22 штатная (плечо 0,9966, книга 26/0,
заявок нет), замыкание прошло, плечо закрытия 1,0060 при потолке 2,00, книга 26/0 сходится
с позицией брокера, `last_session` = 2026-09-11 сходится с календарём, журнал §7 цел, тик
свежий (242 с), копии вне машины свежие третьи сутки, незапушенного нет, маржа 9,23% счёта,
диск 9,8 ГБ, пин журнала МР боевой.

Сорванный 11.09 перевод на Е — **не дефект**: предпросмотр не смог доказать маржу и
отложил переход целиком, не отправив ни одной заявки; причина (дробные заявки по фондам)
найдена и исправлена заказчиком в тот же вечер, четвёртая попытка — 14.09.

Два пункта **на заказчика, не на меня**: (1) 14.09 — четвёртая попытка перевода на Е,
напоминание в очереди на 08:00; (2) до 23.09 — перегенерация `first_connect`, иначе с
23.09 встанет ВЫПУСК пакета (торговля не встанет). Классов A–F нет ни в одном пункте.
