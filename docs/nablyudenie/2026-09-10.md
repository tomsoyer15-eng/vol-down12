# Наблюдение paper pilot — 10.09.2026 (четверг)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись.

## Задача на сегодня

По графику (PROJECT-STATE §0а) особой проверки на 10.09 не назначено — ближайшая 15.09
(предупреждение о старении замера маржи). Поэтому быстрый статус плюс контроль двух
незакрытых отклонений вчерашнего отчёта. Scope не расширялся.

## 1. ОТКЛОНЕНИЕ СРОЧНОЕ: сегодня ПОСЛЕДНИЙ день восстановить Drive — ночью автопилот встанет

Токен по-прежнему отозван. Живая проба в 08:57 этой сессии:
`~/bin/rclone lsd vol-down12-drive:` → `couldn't fetch token: invalid_grant: maybe token
expired?` Тот же отозванный refresh-токен (OAuth-приложение в статусе Testing; BACKLOG 22б,
чек-лист живого старта п.4).

**Счётчик дошёл до двух — как и было предсказано вчера.** `~/.addfut/push-fails` = **2**
(файл записан 10.09 00:05). В журнале за минувшую ночь:

```
2026-09-10 00:05:29  снимок+WORM: копия addfut-2026-09-09-160529.tgz; якорь worm-2026-09-09.txt
2026-09-10 00:05:29  зеркало ok
2026-09-10 00:05:31  github ok
2026-09-10 00:05:47  drive ОШИБКА выгрузки — копия вне машины НЕ обновлена
2026-09-10 00:05:47  внешняя выгрузка копий не удалась (2 подряд)
```

Порог перечитан по коду сегодня — `r33build/live/autopilot.sh:1408`: `if [ "$pf" -ge 3 ]`
→ `alarm_write ALARM-push.txt` → `diagnose.py` → `log "автопилот остановлен"` → `return 1`.

| замыкание 00:05 | за сессию | `push-fails` | что происходит |
|---|---|---|---|
| 09.09 | вт 08.09 | 1 | «внешняя выгрузка копий не удалась (1 подряд)» |
| 10.09 (сегодня ночью было) | ср 09.09 | **2** | то же |
| **11.09** | чт 10.09 | **3** | **ALARM-push, автопилот остановлен** |

**Практическое последствие.** Сегодня ночью, в 00:05 11.09, замыкание четверга даст третий
отказ подряд, встанет тревога `ALARM-push` и автопилот остановится целиком. Пятничная
сессия 11.09 не состоится; снятие тревоги — ручное (доктрина О-5). **Это не дефект:**
механизм работает ровно как задуман — три замыкания без копий вне машины означают стоп до
ручного разбора. Классов A–F нет, правка кода не требуется и не предлагается.

Отказов выгрузки в `wip-push.log` уже **221** (вчера 173), первый — 05.09 18:37:01,
последний — 10.09 08:37:01; ни одного успеха в промежутке.

Сам себе помочь не могу — перепроверено опытом сегодня, а не взято из вчерашней записи:
обновление по refresh-токену отвергается сервером Google, `rclone config reconnect` требует
живого согласия в браузере под аккаунтом владельца.

**Действие заказчика — одна команда, срок до вечера СЕГОДНЯ:**
`~/bin/rclone config reconnect vol-down12-drive:`

**Локальные точки восстановления целы и свежие:** `~/.addfut-backups` и `~/state-mirror` —
оба с `addfut-2026-09-09-160529.tgz` (20 архивов), git-зеркало и GitHub актуальны
(**0** незапушенных коммитов), WORM-якорь `worm-2026-09-09` в HEAD (коммит `5a23d01`).
Вне машины не обновляются только копии — их свежесть застыла на **05.09**.

## 2. ОТКЛОНЕНИЕ ПРОДОЛЖАЕТСЯ: перевод на маршрут Е не состоялся, новой даты нет

`~/.addfut/route.txt` = **F**. За сутки ничего не изменилось: в `~/.addfut/nablyudatel.log`
подстрока `перевод-Е` по-прежнему встречается только двумя строками за 04.09 (135 и 151);
за 08.09 и 09.09 её нет. Промежуточного состояния перехода нет, незакрытых намерений нет
(`intent`-файлов в `~/.addfut` нет вовсе). Акты заказчика в `r33build/mr_journal.csv` от
04.09 не протухают (проверено вчера по `mr_engine.journal_rows`).

Для новой попытки нужны заготовка с новой сторожевой датой и строка крона на неё — это акт
заказчика. Сам не создаю и дату не переношу: режим сессии только читающий. Классов A–F нет.

## 3. Контур: вчерашняя сессия и текущее состояние

**Сессия 09.09 прошла штатно.** Счёт DUR833748, NAV **986 562,46**, позиции
`{ESZ26: 2, MESZ26: 6}`, сессия **№20**; вход `es_close=(7747.25, '2026-09-08')`,
`spy_close=765.96`, базис +1,144%, `yield_10y_pct=4.806`, `dref=7.8665`,
`signal_date=2026-09-30` (возраст 9 дней), серия Z26. Решение — плечо **1,0093**, книга
**26/0**, **заявок нет**; причина: «БУМАЖНЫЙ ЭТАП: порог §8 (3 000 000) не применён при
NLV 986 562; механический пол соблюдён». Замыкание: NLV закрытия **985 482,46**, плечо
закрытия **1,0057** при потолке 2,00.

**Книга** `book-F.json` (10.09 00:05): `session_no` = **20**, `last_session` =
**2026-09-09**, `close_provisional` = false, `roll_pending` = false, `n_e` = **26**,
`n_b` = **0**, `unit_is_mes` = true, `es_held` = **2**, `ser_a` = `ser_b` = **Z26**,
`prev_close_lev` = **1,005720588877858**, `prev_st_eq` = true, `prev_st_bd` = false,
digest `dbbaf3b2…`. Сверка с брокером по последним живым данным (09.09):
2×10 + 6 = **26 MES** = `n_e`; ноги Б нет ни в книге, ни у брокера. **Расхождений нет.**

**Стык календаря сходится:** `feed.prev_session(2026-09-10)` = **2026-09-09** =
`last_session` в книге. Сегодня четверг, торговый день; окно, спрошенное только у
`feed.common_window(2026-09-10)`, — **02:00–10:30 по Чикаго**.

**Тревоги:** активных **0**. Все шесть файлов `ALARM*` несут суффикс `.resolved-*`,
свежайший — `ALARM-signal-2026-09.resolved-01.09`.

**Тик жив:** `tick-heartbeat` 10.09 **08:55:01**, возраст **2,3 минуты** при пороге
почтового сторожа 65 минут. Отметки дня на месте: `traded-2026-09-09`, `closed-2026-09-09`.

**Журнал §7** `journal-F.csv`: **33** строки, последняя —
`2026-09-09 … ИТОГ … 986562.46 … итог сессии 20: строк 0`, цепочка хэшей непрерывна
(`f8c2bf92…` → `275be8d5…`).

**Сигнал:** строка месяца действия `2026-09-30` = **(1, 0)** — нога А включена, нога Б
выключена. Сайдкар уровней заканчивается августом (92,74 / 767,05), поколение одно.
Отметка `sigup-2026-09` на месте.

**Маржа по книге** (замер `margins_live.json` от 18.08 08:54 UTC): начальная
2×35 050,37 + 6×3 506,29 = **91 138,48** = **9,25%** счёта; поддерживающая
2×25 116,17 + 6×2 511,62 = **65 302,06** = **6,63%**. Запас очень большой. Замеру
**23 дня из 35**, протухает **23.09**; предупреждение по графику — 15.09, файлов
предупреждения пока нет.

**Почта:** пульс за 09.09 ушёл («ПУЛЬС 09.09.2026: жив — NLV 989 385,60, тревог нет»);
в `pochta-storozh.log` новых записей с 04.09 17:03 нет. Сегодняшний пульс в 09:07 впереди.

**Диск:** свободно **9,3 ГБ (занято 84%)** — та же полка четвёртые сутки.
**Пакет:** действующий — **568930d7** (`paket-r33-20260904-201716-568930d7.zip`).
**Git:** незакоммиченными висят только `docs/ADD-FUT-STATUS.pdf/.txt` — рабочий вывод
`status_stranitsa.py`, не состояние.

## Письма

Событий из закрытого списка п.3б (сделки, ролл, месячный переход, замыкание месяца, смена
упаковки, восстановление, обслуживание) со времени отчёта 09.09 не было: 09.09 — обычная
сессия без заявок. Сегодня четверг — еженедельная сводка не положена. Письмо отправляется
по пункту 5 задания: вердикт нештатный.

## Вердикт

**Вердикт НЕ штатный — два отклонения, оба без правки кода; первое становится срочным.**

1. **Токен Drive не восстановлен пятые сутки, счётчик = 2 из 3.** Сегодня ночью в 00:05
   замыкание четверга даст третий отказ подряд → `ALARM-push` → автопилот остановлен,
   пятничная сессия 11.09 не состоится. Нужна переавторизация в браузере **до вечера
   сегодня**: `~/bin/rclone config reconnect vol-down12-drive:`. Безголового пути нет —
   перепроверено живой пробой в этой сессии.
2. **Перевод на маршрут Е по-прежнему не состоялся**, новой сторожевой даты и строки крона
   нет. Переход не начинался, промежуточного состояния нет, акты заказчика от 04.09 не
   протухают. Требуется решение заказчика по новой дате.

**Сам контур в норме:** активных тревог 0, сессия 09.09 прошла штатно (№20, заявок нет),
книга 26/0 сходится с позицией брокера, плечо закрытия 1,0057 при потолке 2,00, журнал §7
цел, тик свежий, локальные точки восстановления свежие (09.09), незапушенного нет, маржа
9,25% счёта, диск 9,3 ГБ.

**Правок production-кода не требуется: классов A–F ни в одном из двух отклонений нет.
Paper pilot продолжается — но без переавторизации Drive сегодня он остановится ночью.**
