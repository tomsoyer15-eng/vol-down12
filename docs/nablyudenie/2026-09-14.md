# Наблюдение paper pilot — 14.09.2026 (понедельник)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись.

## Задача на сегодня

По графику (PROJECT-STATE §0а) на 14.09 наблюдательной задачи нет (ближайшая — 15.09,
старение замера маржи). Значит — быстрый статус (тревоги, тики, книга и `last_session`,
wip-push, диск) плюс обязательная строка про пин журнала МР и **еженедельная сводка письмом
(понедельник)**. Scope не расширялся.

За выходные торговых сессий не было: последняя сессия — пятница 11.09 (№22), она доложена
отчётом 12.09. Ниже — то, что изменилось с тех пор, и недельная картина.

## 1. Пин журнала МР — БОЕВОЙ

```
cd r33build && ../.venv/bin/python -c "import mr_engine as M; print(M._read_deploy()['journal'])"
-> /home/alex/claude-projects/vol-down12/r33build/mr_journal.csv
```
Путь боевой, не временный. Расхождения нет. В журнале те же 4 строки от 04.09:
`SWITCH_SIGNAL E|2026-09-04#1`, `OWNER_APPROVE`, `GRANULARITY_EXCEPTION 387 026` и `450 000`.

## 2. Что изменилось за выходные

**Ничего торгового.** Суббота и воскресенье — сессий нет, отметок `traded-`/`closed-` за
12–14.09 нет и быть не должно. Последние отметки — `traded-2026-09-11` и `closed-2026-09-11`.

Единственные новые записи в `autopilot.log` за двое суток — **предупреждение о старении
замера маржи**, по одному разу в чикагские сутки:

```
2026-09-13 08:05:02 | ВНИМАНИЕ: … замер маржи от 2026-08-18: ему 26 дней, до предела 35 осталось 9 …
2026-09-14 08:05:02 | ВНИМАНИЕ: … замер маржи от 2026-08-18: ему 27 дней, до предела 35 осталось 8 …
```

Счётчик идёт как положено (25 → 26 → 27 дней), файлы `WARN-margins-2026-09-13.txt` и
`WARN-margins-2026-09-14.txt` на месте. Это **предупреждение, а не тревога**: торговле
возраст замера не мешает, мешает только ВЫПУСКУ пакета — с 23.09. Действие (перегенерация
`first_connect`) остаётся за заказчиком, ничего не чиню.

**Напоминание о четвёртой попытке перевода на Е ушло сегодня в срок:**
```
2026-09-14 08:00:06 отправка: код 0; письмо отправлено:
  НАПОМИНАНИЕ: сегодня перевод на маршрут Е — откройте сессию
```
`~/.addfut/route.txt` = **F**; файл попытки за 14.09 не заводился (перевод делается днём, в
торговое окно, и требует живой сессии либо строки крона — это на заказчике).

## 3. Статус контура

**Тревоги:** активных **0** — файлов `ALARM*.txt` нет, все шесть с суффиксом `.resolved-*`,
свежайший `ALARM-signal-2026-09.resolved-01.09`. `WARN-margins-*` — предупреждение (п.2).

**Тик жив:** `tick-heartbeat` = 14.09 **08:55:01**, возраст **142 с** при пороге почтового
сторожа 65 мин.

**Книга** `book-F.json` (12.09 00:05, не менялась): маршрут **F**, `session_no` = **22**,
`last_session` = **2026-09-11**, `close_provisional` = false, `roll_pending` = false,
`n_e` = **26**, `n_b` = **0**, `unit_is_mes` = true, `es_held` = **2**,
`ser_a` = `ser_b` = **Z26**, `prev_close_lev` = **1,0059632952795488**, `prev_st_eq` = true,
`prev_st_bd` = false, `d_fix` = 7,9291, digest `003c9699…`.
Сверка с брокером по последним живым данным (11.09): 2×10 + 6 = **26 MES** = `n_e`; ноги Б
нет ни в книге, ни у брокера. **Расхождений нет.**

**Стык календаря сходится:** `feed.prev_session(2026-09-14)` = **2026-09-11** =
`last_session` в книге; `feed.prev_session(2026-09-15)` = 2026-09-14. То есть сегодня —
очередная торговая сессия (№23), она пройдёт днём в 16:45 по машине.

**Журнал §7** `journal-F.csv`: последняя строка
`2026-09-11 … ИТОГ … 988492.13 … итог сессии 22: строк 0`, цепочка хэшей непрерывна
(`226560cf…` → `0491e365…`). Сделок с 26.08 (ролл) не было ни одной.

**Сигнал:** строка месяца действия `2026-09-30` = **(1, 0)** — нога А включена, нога Б
выключена. Сайдкар уровней заканчивается августом (92,74 / 767,05), поколение одно.
Отметка `sigup-2026-09` на месте.

**Маржа по книге** (замер `margins_live.json` от 18.08 08:54 UTC): начальная
2×35 050,37 + 6×3 506,29 = **91 138,48** = **9,23%** счёта; поддерживающая
2×25 116,17 + 6×2 511,62 = **65 302,06** = **6,61%**. Запас очень большой.

**Почта:** пульс уходил оба выходных дня («ПУЛЬС 12.09: жив — NLV 987 687,13, тревог нет»,
«ПУЛЬС 13.09: … то же»), сегодняшний в 09:07 впереди. Новых записей в
`pochta-storozh.log` с 04.09 17:03 нет.

**Копии и диск:** `wip-push` работает каждые полчаса без пропусков, последний срез
**14.09 08:37:04** (2 файла на Drive). Точки восстановления `~/.addfut-backups` и
`~/state-mirror` — обе с `addfut-2026-09-11-160529.tgz`, по 22 архива; якорь
`r33build/anchors/worm-2026-09-11.txt`. Внешняя выгрузка проходит с вечера 10.09
(`зеркало ok` / `github ok` / `drive ok`), счётчика `push-fails` нет.
Диск: свободно **9,1 ГБ (занято 85%)** против 9,8 ГБ (83%) 12.09 — минус 0,7 ГБ за двое
суток, растут git-бандлы в `~/git-mirrors`, каталоги состояния по 540 КБ. Запаса с избытком.

**Git:** незапушенных коммитов **0**, HEAD `3efeacd`. Незакоммиченными висят только
`docs/ADD-FUT-STATUS.pdf/.txt` — рабочий вывод `status_stranitsa.py`, не состояние.
Действующий выпуск — **2ece058c** (11.09).

**H19:** новых завершённых месяцев нет (записан 1, за 2026-08) — журнал ведётся, задачи нет.

## 4. Неделя 08.09–11.09 в цифрах (для сводки)

| | |
|---|---|
| NLV закрытия пт 11.09 | **987 687,13** |
| NLV закрытия пт 04.09 | 994 161,84 |
| Дельта недели | **−6 474,71 (−0,65%)** |
| С начала пилота (1 000 000) | **−12 312,87 (−1,23%)** |
| Сессии | №19–22 (пн 07.09 — Labor Day, биржи закрыты) |
| Сделки | **нет ни одной** |
| Плечо закрытия пт | **1,0060** при потолке 2,00 |

## Письма

**Еженедельная сводка (понедельник) — ОТПРАВЛЕНА** (см. п.3а поручения).

Событий из закрытого списка п.3б (исполненные заявки, ролл, месячный переход, замыкание
месяца, смена упаковки, восстановление после сбоя, обслуживание) со времени отчёта 12.09
не было: выходные без сессий. Отдельное письмо о событии не отправляется.

## Вердикт

**Изменений не требуется. Paper pilot продолжается.**

Контур в норме: активных тревог 0, тик свежий (142 с), книга 26/0 сходится с позицией
брокера, `last_session` = 2026-09-11 сходится с календарём, журнал §7 цел, сделок за неделю
нет, плечо закрытия 1,0060 при потолке 2,00, маржа 9,23% счёта, копии вне машины свежие,
незапушенного нет, диск 9,1 ГБ, пин журнала МР боевой. Отклонений классов A–F нет.

Два пункта **на заказчика, не на меня**: (1) сегодня 14.09 — четвёртая попытка перевода на
маршрут Е, напоминание ушло в 08:00 (код 0), route по-прежнему F; (2) до 23.09 —
перегенерация `first_connect`, иначе с 23.09 встанет ВЫПУСК пакета (торговля не встанет),
осталось 8 дней.
