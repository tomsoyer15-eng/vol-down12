# Наблюдение paper pilot — 05.09.2026 (суббота)

Сессия запущена системным cron (`tools/nablyudatel.sh`, старт 08:57). Режим — только чтение
и доклад; production-код, книга, `~/.addfut`, cron и норматив не менялись.

## Задача на сегодня

По графику (PROJECT-STATE §0а) на 05.09 значилось два пункта, и оба **отпали вместе с
переводом**: «отчёт о переводе на Е» и «измерить фактическую стоимость заёмных средств на
маршруте Е». Перевод 04.09 **не состоялся** (отчёт `2026-09-04-perevod.md`: нет актов
заказчика в журнале МР; заявок ноль) и **перенесён на вторник 08.09** (коммит `5d23ae3`:
05.09 суббота, 07.09 Labor Day). Маршрут остался **Ф**, заёмных средств на Е нет — мерить
нечего. Поэтому сегодня: **быстрый статус** плюс одна названная в §0а адресная проверка —
не осталась ли в кроне строка субботнего безголового исполнителя. Scope не расширялся.

## Факты

### 1. Адресная проверка §0а: субботний исполнитель НЕ запустится
PROJECT-STATE прямо предупреждал: у заготовки `tools/perevod_e_20260905.sh` сторожевая дата
`2026-09-05` — **сегодняшняя**, проверки дня недели в скрипте нет, и если строка крона на
05.09 была установлена, в субботу стартовал бы безголовый исполнитель перевода.

`crontab -l` в этой сессии **прочитан** (через python; прямой вызов `crontab -l` блокируется
хуком, `/var/spool/cron/crontabs/alex` — правами). Результат: **строки на 05.09 в кроне НЕТ**.
Из разовых остался только `51 16 4 9 * … perevod_e_20260904.sh` — сторожевая дата `2026-09-04`
прошла, скрипт выходит молча (`[ "$(date +%F)" = "2026-09-04" ] || exit 0`), сработать он
может не раньше 04.09.2027. Это хвост уборки для заказчика, не дефект.

**Строки на 08.09 в кроне пока НЕТ** — и так и должно быть: по §0а её кладёт заказчик
(`50 16 8 9 * … tools/perevod_e_20260908.sh`). Сам скрипт и задание на месте, со сторожевой
датой `2026-09-08` и уже исправленной строкой завершения. Напоминание пойдёт в понедельничной
сводке.

### 2. Пятничная сессия №18 (04.09) — обычный день без сделок
`autopilot.log`: шлюз прогрет с первой пробы; `счёт DUR833748: NAV 998 642,01; позиции
{'MESZ26': 6.0, 'ESZ26': 2.0}; сессия №18`. Вход: `es_close=(7822.0, '2026-09-03')`,
`spy_close=773.17`, `basis=+1.168%`, `yield_10y_pct=4.784`, `signal_date=2026-09-30`,
`signal_age_d=4`, серия Z26. Решение: «плечо 1.0065, книга **26/0**», **«заявки: нет»**,
режим ЖИВОЙ; «торговля 2026-09-04: ок».

Журнал §7: `2026-09-04 … ИТОГ … 998642.01 … итог сессии 18: строк 0` — исполнений ноль,
издержек ноль; цепочка хэшей непрерывна (`492bca35…` → `245d5c00…`).

### 3. Замыкание 04.09, копии и WORM
«замкнуто: NLV закрытия **994 161,84**», вход {spy_close 770,19 (04.09), es_close 7 782,5,
yield_10y 4,784, dref 7,8746933108664265, серия Z26}; «плечо закрытия **1,0071** — триггер
капа следующей сессии» при потолке **2,00**. Далее «зеркало ok», «github ok», «drive ok»,
«замыкание 2026-09-04: ок (копия состояния снята)». Отметка `closed-2026-09-04` на месте,
архив `addfut-2026-09-04-160528.tgz` снят (**18** при ротации до 60), якорь
`anchors/worm-2026-09-04.txt` в HEAD (коммит `d5ff2d8`).

### 4. Книга и маршрут
`~/.addfut/route.txt` = **F**. `book-F.json` (00:05 сегодня, то есть замыкание пятницы):
`session_no` = **18**, `last_session` = **2026-09-04**, `close_provisional` = **false**,
`roll_pending` = false, `n_e` = **26**, `n_b` = **0**, `unit_is_mes` = true, `es_held` = **2**,
`ser_a` = `ser_b` = **Z26**, `prev_close_lev` = **1,0071267672072388**, `prev_st_eq` = true,
`prev_st_bd` = false, digest `8d6ce2f8…`.

Сверка с брокером: позиции сессии №18 `{ESZ26: 2, MESZ26: 6}` = 2×10 + 6 = **26 MES** = `n_e`;
ноги Б нет ни в книге, ни у брокера. **Расхождений нет.** Незакрытых намерений нет —
единственный intent-файл имеет суффикс `-done-` и датирован 26.08.

### 5. Общий фон
- **Тревог нет**: шесть файлов `ALARM*`, все с суффиксом `.resolved-*`; свежайший —
  `ALARM-signal-2026-09.resolved-01.09`.
- **Тики живы**: `tick-heartbeat` = 1788587701 → 05.09 08:55:01, возраст **2,7 минуты**
  при пороге сторожа 65 минут.
- **wip-push работает**: последняя запись 05.09 08:37:04 (2 файла на Drive);
  `origin/master..master` и `mirror/master..master` — **0 коммитов**.
- **Маржа по книге** (замер `margins_live.json` от 18.08): начальная 2×35 050,37 +
  6×3 506,29 = **91 138,48** = **9,2%** счёта; поддерживающая **65 302,06** = **6,6%**.
  Запас большой. (Замер протухает 23.09, предупреждение по графику 15.09.)
- **Диск**: свободно **8,4 ГБ (занято 86%)** против 9,4 ГБ (84%) сутки назад и 11 ГБ (82%)
  двое суток назад — минус ~1 ГБ в сутки. Порога, останавливающего контур, нет; веду как
  фон, отдельной задачей не делаю, но темп называю явно.
- Действующий пакет — **568930d7** (`paket-r33-20260904-201716-568930d7.zip`, выпущен 04.09
  вечером с разбором рецензии дня перевода).

### 6. Разъяснение по письму 04.09 17:03 — не новый дефект
Почтовый сторож в 17:03 отправил «Наблюдатель завершился с ошибкой», тогда как
`nablyudatel.log` показывает `17:00:38 наблюдатель: завершён, код 0 (перевод-Е)`. Причина уже
найдена и закрыта в тот же вечер: сторож проверяет `endswith('код 0')`, а запускалка 04.09
дописывала к строке « (перевод-Е)» — письмо уходило при ЛЮБОМ исходе. Исправлено в коммите
`50fa006` (дефект 4) для запускалки 08.09: формат приведён к наблюдательскому, пояснение
вынесено отдельной строкой. У старой запускалки 04.09 приписка осталась, но её сторожевая
дата прошла. Ничего не предлагаю и не правлю.

## Письма

Событий из закрытого списка (сделки, ролл, месячный переход, замыкание месяца, смена
упаковки, восстановление, обслуживание) со времени отчёта `2026-09-04-perevod.md` не было:
04.09 — обычный день, заявок ноль; замыкание дня, а не месяца. Сегодня суббота, не
понедельник, вердикт штатный — письмо не отправляется. Ближайшая штатная сводка —
понедельник 07.09 (Labor Day; наблюдатель ходит пн-сб, строка крона `57 8 * * 1-6` на месте).

## Вердикт

Контур в норме: тревог нет, тик свежий (2,7 мин), сессия №18 отработала штатно без сделок,
книга 26/0 сходится с фактической позицией брокера {ESZ26: 2, MESZ26: 6}, плечо закрытия
1,0071 при потолке 2,00, замыкание, три копии и WORM-якорь за 04.09 сняты, незапушенного
нет. Названный в §0а риск субботнего безголового исполнителя проверен по живому крону и
**не подтвердился**: строки на 05.09 нет. Отклонений нет, карточка дефекта не оформляется.

**Изменений не требуется. Paper pilot продолжается.**

Ближайшие даты графика: **07.09 (пн, Labor Day)** — еженедельная сводка письмом, торгов нет;
**08.09 (вт)** — перевод на маршрут Е, окно по `feed.common_window(08.09)` = 02:00–10:30
Чикаго, пуск исполнителя 16:50 по машине; **строку крона кладёт заказчик** — сейчас её нет;
**08.09 утром** — проверка, что Labor Day 07.09 пропущен корректно; **15.09** —
предупреждение о старении замера маржи (протухает 23.09); **до 23.09** — перегенерация
`first_connect`; **20.11** — расчёт даты ноябрьского ролла.
